# 艾莉森的房间 · 设计与工程实现 v0.1

> 状态：待审核。审核通过后按里程碑顺序开发，每片完成后演示确认再进入下一片。
> 代码仓库：`D:\AIGameBuild`（main 分支），Godot 项目目录 `成为艾莉森的一天/`。
> 本文档所有 `res://` 路径均相对 Godot 项目根目录。
> **工程约束：本文档的工程接入（§3）以《场景重构_工程文档_v0.2》的壳 ABI 契约为准**——房间按 **v0.2 §2.6 加区契约** 加为 Areas 下的新区，不动 Player / UI CanvasLayer 根结构。
> 相关文档：`场景重构_工程文档_v0.2.md`、`玩法扩展_设计与工程实现_v0.1.md`（M1–M5 底座）、`天控全局_第一回合切片_设计文档.md`、`《成为艾莉森的一天》游戏策划案.md`。

---

## 0. 意图澄清结论

### 0.1 首轮澄清（2026-09-03；D7 执行方式 2026-09-06 校准）

| # | 决定 | 结论 |
|---|---|---|
| D1 | 房间的第一身份 | **玩家基地/枢纽**：把"家里该干的事"聚合，功能优先，叙事随内容生长 |
| D2 | 形态与入口 | **独立室内场景，走进去**：树屋门从「租房 UI」升级为「进家」（未租房仍租房 UI） |
| D3 | 身份证 | **随天数/关键剧情解锁的档案**：买屋首进房间发放一次，之后在背包内查看（细化见 D11） |
| D4 | 日界 | **接管**：有房后清晨在床上醒来开始一天、回家入睡结束今天（现全局午夜入睡面板退役） |
| D5 | 委托事务清单 | **私有「我的一天」+ 已接委托汇总**；广场告示牌保留（公共委托仍在那接） |
| D6 | 药水制作台 | **第二版**（切 R-D），第一版不做可用制作 |
| D7 | 独立场景文件 | 意图：房间是**独立 .tscn 场景文件**、走进去。执行方式：**不做 change_scene/常驻壳**，按场景重构 v0.2 **加区契约**把 `scenes/levels/alison_room.tscn` 实例化进 Areas（与三外景同构），Player/UI 根结构不动 |
| D8 | 家具密度 | **功能件 + 少量氛围物**（可点出小文案），不追求全屋可交互 |
| D9 | 「我的一天」内容 | **混合制**：系统自动整理为骨架 + 关键日/主线日由 天(AI)/文案 定制，离线回落骨架 |
| D10 | 下一步 | 先出本设计文档，审核后再排 R-A 实现 |

> **D1 × D3 的内在张力（已共识）**：房间"第一天就能用，内容随时间生长"，因此房间不是一次性做完的场景，而是随版本迭代、每一片都有演示验收的长期承载。
>
> **D7 校准说明**：09-03 曾倾向"独立 .tscn + change_scene + 常驻壳（上提 Player/UI）"。09-05 场景重构 v0.2 冻结后该路径被取代——v0.2 已把三外景做成 `levels/*.tscn` 实例化进 Areas 的成熟机制（M1/M2a 零回归验证），并明确"改 Player/UI CanvasLayer 根结构"为非目标。故房间走 **加区契约**：既有"独立场景文件、编辑器易编辑"的好处，又零换壳、Player/UI 天然跨"区"存活（日界接管 D4 因此不存在换场景丢状态问题）。

### 0.2 二轮细化（2026-09-06，owner 补充）

| # | 决定 | 结论 |
|---|---|---|
| D11 | 身份证发放与查看 | 身份证是**背包道具**（items.json 新增 kind=`document`）；**买屋后首次走进房间**即解锁发放一次（flag 防重复），此后**在背包里随时翻看**（右侧详情"翻看"按钮开证件面板）；证件卡片本体含 **名字 / 身份 / id / 住址** 四项必填，条目随 day/clue 生长仍走 R-B |
| D12 | 占卜时机与出门门控 | **有房后**每天在小屋醒来，占卜从"清晨自动弹"改为**玩家选择**：坐到房间**占卜桌**前触发；**当天未占卜不得出小屋**（房门软锁）。第一天 / 未租房维持现状（清晨自动塔罗、广场醒） |
| D13 | 房间唯一性 | **无论租下哪一栋树屋，门后都是同一间艾莉森专属小屋**——单一 `alison_room` 区，不按 house_id 分实例；"每扇门通向同一间屋"本身是世界观伏笔（见 §4.4） |
| D14 | 主线三推（日期未定） | 驱动剧情推进的三条关键主线，作为「我的一天」L3 关键日候选与结局门控上游（见 §6.3）：①去大学听课→学习"天"的事；②秘密寻找其他女巫→在广场公开暴露身份；③探索石巢塔楼→触发与"天"的直接对话 |

---

## 1. 现状盘点（已存在、可直接复用；含场景重构 v0.2 执行结果）

写方案前已核对仓库：

| 已有 | 位置 | 复用方式 |
|---|---|---|
| 三外景 = 实例化新区机制 | `main.tscn`：`Areas/Plaza·TreehouseDistrict·StoneNestTower = instance of scenes/levels/*.tscn`；`switch_area` 按 `area.name.to_snake_case()==area_id` 切区 | **房间照抄此机制**：加第 4 个实例，零改壳逻辑 |
| 壳 ABI 契约束（v0.2，M0–M2b 已执行） | 碰撞层 `world/player/npc` 已写入 project.godot；三区根 y_sort；`terrain_query.gd`；塔楼地面已换 `tower_groud.tres` | 房间区骨架逐条对齐 v0.2 §2.1–§2.5 |
| 树屋租赁状态 | `game_manager.gd` 的 `treehouse_rented` / `treehouse` | 树屋门按它分流：未租→RentalUI，已租→切进 `alison_room` |
| 门语义（跨区切屏） | `scripts/systems/door.gd`：Area2D，`@export target_area/target_spawn` → `door_entered` → `game.gd.switch_area` | 房间出门 = 一个 `Door`，`target_area="treehouse_district"` |
| 树屋门（跳板） | `scripts/systems/treehouse_door.gd`：目前按 E → `game_action "rental:<house_id>"` | R-A 改：未租→原租房 UI；已租→发 `door_entered` 进房间 |
| 背包系统（M1 已落地） | `scripts/autoload/inventory.gd` + `resources/data/items.json`（kind=forage/herb/potion）+ `inventory_panel.gd`（右详情区 名称/描述/单价） | 身份证进背包：items.json 增 kind=`document`；详情区加"翻看"按钮开证件面板 |
| 入睡两阶段（天复盘→真正入睡） | `game.gd._do_sleep/_run_judgment/_finish_sleep` + `HeavenSession` | 挪到房间床交互；同一棵树，**代码原地复用** |
| 午夜面板 & 入睡门控 | `game.gd._build_midnight_panel`（未租禁睡） | R-A 后退役：入睡只发生在房间床上 |
| 塔罗抽牌 | `TarotUI`（main.tscn 常驻 CanvasLayer）+ `resources/tarot/major_arcana.json`，`game.gd._on_day_started` 现自动打开 | 有房后**不自动开**：改由房间占卜桌触发；`daily_luck` 沿用 |
| 单日字段 | `GameManager` `daily_luck / daily_prophecy`（reset_loop 重置） | 新增 `today_divined` 单日字段：占卜后置 true，房门软锁读它 |
| 线索系统 | `GameManager.discover_clue / clues_found`；**`ai_hint_whitelist` 已有 `clue_id_card`**（关键词：身份证/树屋里的照片/照片） | 身份证查看/获得 = 发现 `clue_id_card` + `record_heaven_event("clue", …)` |
| 条件求值器 | `game_manager.gd` 对话条件（day_min / rented / clue / flag / affection） | 身份证条目解锁、daily_notes 关键日复用同一套 |
| 天的每日预言 | `GameManager.daily_prophecy` + 态度结算 `apply_heaven_rules()`（obey/defy） | 「我的一天」顶部"天的建议"直接读它 |
| 订单系统 | `quest_manager.gd` + `quest_board_ui.gd` + `quests.json`（广场告示牌） | 「我的一天」的"已接委托"汇总只读展示，不重复造系统 |
| 房间地面素材 | **`assets/tilesets/tile_forest/wooden_floor.tres`**（木地板，terrain autotile 已验证，v0.2 D5/§6.1 已拍板归艾莉森的房间） | 房间 `Ground` 直接用，无需等新美术 |
| 时段 | `time_manager.gd`（morning→midnight 五段） | 床入睡只能在午夜触发；清单按时段提示 |
| 事件总线 | `event_bus.gd`（door_entered / game_action / day_started / midnight_reached / loop_reset / toast / tarot_drawn 等） | 新交互一律发信号，不直接互调 |
| UI 弹窗范式 | `rental_ui.gd`（CanvasLayer 覆盖层范式，面板在 main 常驻层） | 身份证、清单、床确认面板照搬，房间区共享 |
| 家具式交互 | `treehouse_door.gd` / `notice_board.gd`（Area2D 感应 + 按 E）+ `interaction_hint_show/hide` | 房间家具交互复用同一感应模式 |
| 跨天持久化（内存态） | `reset_loop()` 不清背包/任务/好感 | 身份证已解锁条目、「我的一天」打勾都挂 Manager，随存档系统排期 |
| 现有回归 | `autotest.gd` + `scenes/tests/*`（test_sleep_no_house / sim_multi_day 等） | R-A 后全量回归；sleep 语义测试改指房间/门分流 |

**已知限制：当前无存档系统**（GameManager 注释已标明 user:// 持久化随存档排期）。"随天数解锁"在同一局内跨天成立；重启不保留属已知限制。

---

## 2. 一页产品概念（房间 v1 的可玩回路）

```
[第一天 / 未租房] 清晨照旧在广场醒，自动抽塔罗
    │  白天去树屋区租下一栋树屋
    ▼
[买屋后首进房门] 树屋门：已租房？→ 推门进家（切进房间区）
    ├─ 进门即获道具「艾莉森的证件」（只发一次）→ 之后在背包随时翻看
    ▼
[艾莉森的小屋] 独立室内场景文件（实例化新区，木地板）—— 无论租哪栋，都是同一间
    ├─ 床 ──────── 午夜时「入睡」：先天的复盘审判，再结束今天
    ├─ 占卜桌 ──── 坐到桌前自主占卜（日运塔罗）；当天不占卜 → 房门软锁，出不去
    ├─ 清单板 ──── 「我的一天」：今日预言 + 个人事务/目标 + 已接委托汇总
    ├─ 床头柜 ──── 身份证就摆在上面（首进发放道具的地方）
    ├─ 书桌 ────── 氛围点读：旧信 / 照片 / 草药笔记（小文案）
    └─ 烛台/旧书等 氛围物
    │  一觉醒来 —— 清晨在小屋床上醒来（day≥2 且有房）
    ▼
[又是同一天] 先占卜，再出房门 → 回树屋区（树屋门口）
```

- 树屋门**分叉**：`treehouse_rented == false` → 原租房 UI；`true` → 切进 `alison_room`。
- 广场告示牌**不动**：公共委托仍在广场接，「我的一天」只是把已接委托汇总到家里看。
- 药水制作台位置预留，但 R-A–R-C 不实现可用制作（见 §8 R-D）。

---

## 3. 工程接入（R-A 核心）：遵 v0.2 加区契约加"第 4 区"

### 3.1 方案变更：为什么不用"跨场景壳"

09-03 首版曾设计"常驻壳 + change_scene 跨世界"（上提 Player 与全局 UI）。**该方案已废弃**，原因：

1. 场景重构 v0.2（09-05 冻结）已把"改 Player / UI CanvasLayer 根结构"列为**非目标**（v0.2 §0.3），并确立**唯一合规加区方式是 §2.6 加区契约**。
2. M1/M2a 已验证的更省机制：三外景各自 `scenes/levels/*.tscn`、在 `Areas` 下 `instance`，运行时是**同一棵树**；`switch_area` 靠根名 snake 切区。房间照抄即得"独立场景文件"，**零换壳**。
3. 日界接管（D4）原本担心的"换场景 Player/UI 丢"根本不存在——本来就在同一棵树，床交互直接复用 `game.gd` 现成的入睡两阶段。

### 3.2 目标运行时结构

```
scenes/main.tscn（游戏主场景，start_menu 过渡进入）—— 不变，只是 Areas 多一个实例
├─ Player（instance player.tscn）            ← 原位，不动
├─ DaylightModulate                          ← 原位，不动
├─ Areas
│  ├─ Plaza              instance scenes/levels/plaza.tscn
│  ├─ TreehouseDistrict  instance scenes/levels/treehouse_district.tscn
│  ├─ StoneNestTower     instance scenes/levels/stone_nest_tower.tscn
│  └─ AlisonRoom         instance scenes/levels/alison_room.tscn   ← R-A 新增
└─ （全局 UI CanvasLayer：DialogueUI/TarotUI/RentalUI/StatusPanel/HUD/
    SellPanel/EndingUI/TarotDivinationUI/InventoryUI/IdCardPanel…） ← 原位，房间区共享
```

### 3.3 房间区节点骨架（对照 v0.2 §2.1 契约①，逐条对齐）

`scenes/levels/alison_room.tscn` 根 = Node2D，名 `AlisonRoom`（`to_snake_case()` → area_id `alison_room`），`y_sort_enabled = true`：

| 节点路径（相对区根） | 类型 | 必需 | 说明 |
|---|---|---|---|
| `AlisonRoom`（根） | Node2D，`y_sort_enabled=true` | 必 | 区根；`switch_area` 按名命中 |
| `Ground` | TileMapLayer，`tile_set = res://assets/tilesets/tile_forest/wooden_floor.tres` | 必 | 木地板地面，autotile 铺好；**可走地面 → 无碰撞**（v0.2 §2.2） |
| `Props` | Node2D | 必 | 房间边界墙/床体等挡人物件 = StaticBody2D（world 层，碰撞层见 §3.4） |
| `Overlay` | TileMapLayer | 预留 | 本轮可空 |
| `BedroomDoorIn` | Marker2D | 按区 | 进门落点（玩家从树屋门切进来站这） |
| `WakeUp` | Marker2D | 按区 | 清晨醒来落点（床边） |
| `DoorOut` | Area2D + `door.gd`，`target_area="treehouse_district"`，`target_spawn="SpawnFromRoom"` | 按区 | 出房门；**受 D12 门控**（当天未占卜则禁用并提示） |
| 床 / 占卜桌 / 清单板 / 床头柜(证件) / 书桌 | Area2D 感应交互（触发 `room:xxx` 动作） | 按区 | 功能件，详见 §4 |
| 烛台/旧书等 | Area2D 感应 或 纯摆件 | 按区 | 氛围物，可点读小文案 |

> 配套：`treehouse_district.tscn` 增一个出生 Marker `SpawnFromRoom`（树屋门旁），供房间 `DoorOut` 指回——与三区现有的 `SpawnFromPlaza/SpawnFromEast` 命名习惯一致。

### 3.4 碰撞层（对照 v0.2 §2.5，已写入 project.godot）

| 层 | 名 | 承载 |
|---|---|---|
| 1 | `world` | 房间边界墙/挡人家具的 StaticBody2D（玩家 mask 含 1 → 撞墙不穿模） |
| 2 | `player` | 玩家（不新增） |
| 3 | `npc` | 房间无 NPC，不用 |

- 交互家具（床/占卜桌/柜/清单板）用 Area2D 感应检测 **player 层**、按 E 触发，**不参与物理阻挡**；若某件家具又要挡人又要能交互（如床），拆成「Props 下 StaticBody2D 挡人」+「独立 Area2D 感应」，两层解耦（同 v0.2 §2.3 精神：阻挡与交互分离）。
- 不做任何砍伐型可交互物 → 房间本轮**不引入 Interactable 组件**（那是外景树/石的方向）。

### 3.5 行为层改动面（应改的最小集，全部在 game.gd / treehouse_door.gd / 小部分 UI，不碰壳）

| 改动 | 位置 | 性质 |
|---|---|---|
| 树屋门分流 | `treehouse_door.gd` `_input` | 行为 |
| 首进房间发放身份证 | `game.gd` 切进 `alison_room` 时检测 `flag id_card_obtained` → `Inventory.add("id_card")` + discover `clue_id_card` | 行为 |
| 入睡门控与午夜面板退役 | `game.gd` 午夜相关函数 | 行为（现全局面板 → 仅床触发） |
| 有房日清晨不自动弹塔罗 | `game.gd._on_day_started`：`treehouse_rented` 时不开 TarotUI（改由占卜桌触发） | 行为 |
| 房门软锁（D12） | `door.gd` 或 `DoorOut`：当天 `today_divined == false` 时拒绝通过 + 提示 | 行为 |
| 清晨醒来落点 | `game.gd._on_loop_reset`：有房 → `alison_room/WakeUp`；未租 → 保持 `plaza/PlayerSpawn` | 行为 |
| 房间家具动作路由 | `game.gd._on_game_action` 增 `room:xxx` 分支（sleep / tarot / my_day / id_card / read） | 行为 |
| UI 面板（身份证/清单） | main 层新增 CanvasLayer；inventory_panel 详情区加"翻看"按钮 | 内容，壳不动 |

> **R-A 确认点**：①醒来默认落点有房=房间 `WakeUp`，未租=广场——符合 D4/D12；②首进房间判定用 `GameManager.flags["id_card_obtained"]` 防止循环重置后再发；③D12 门控只作用于"有房日在房间醒来且当天未占卜"，若 owner 想让"每次出家门都要先确认占卜"变成硬门控，实现时把判定从 `today_divined` 换成"今日尚未查看过日运"亦可，默认取"一天占一次放行一天"。

---

## 4. 房间场景与家具（内容映射）

### 4.1 家具 → 交互 → 机制

| 家具 | 玩家动作 | 触发机制 | 切片 |
|---|---|---|---|
| 床 | 午夜按 E | 两阶段入睡（天复盘→睡），`midnight_reached` 后可用；兼清晨醒来落点 | R-A |
| 占卜桌 | 按 E（有房日清晨） | 坐定 → TarotUI 自主抽牌 → 置 `today_divined=true`，房门放行 | R-A |
| 床头柜（证件） | 首进房间触发 | **发放道具** `id_card` 进背包一次（详见 §5） | R-A |
| 清单板 | 按 E | `my_day` 面板（§6） | R-C |
| 书桌 | 按 E | 氛围点读：一封旧信/照片/草药笔记（文案小卡片） | 散落各片 |
| 烛台/旧书/斗篷架 | 按 E | 氛围文案或「再平常不过的…」无实义回应 | 散落各片 |

- 交互形态：**Area2D 感应 + 按 E**（同 `treehouse_door.gd`/`notice_board.gd`），触发 `EventBus.game_action("room:xxx")`，由 `game.gd._on_game_action` 路由到对应面板（与现有动作路由同构）。
- 挡人件（边界墙、床体、柜体）放 `Props` 下 StaticBody2D（world 层）；感应区独立，互不耦合。
- 家具动作统一发 `EventBus.game_action`，房间与外部区共享同一套路由，不另立控制器。

### 4.2 房间布局（占位即可，正式家具美术入清单）

- 入口在房间左/下侧接 `DoorOut`；床在右（`WakeUp` 在床边）；**占卜桌在床与门之间**（形成"醒来→占卜→出门"动线）；清单板挂墙；床头柜在床边放证件。
- 地面已定 `wooden_floor.tres`；墙/家具占位可先用现有瓦片/矩形（与 `bake_terrain.gd`/编辑器手摆套路一致），正式 sprite 走美术清单。

### 4.3 氛围物文案

书桌旧信/照片/草药笔记为首次点读内容，**文案走文案组，程序先放占位文本并标注**（同 `玩法扩展` §4.3 惯例）。

### 4.4 "每一扇门通向同一间屋"（D13 的世界观伏笔）

不论租下广场树屋区的哪一栋，推门走进的都是**这一间**艾莉森的小屋。玩家迟早会意识到"门对不上号"——这是一个克制的暗示：小屋不是树屋区的一部分，而是为她（或为"她应该有个家"）单独搭出来的。**本轮不点破**，仅保留"永远是同一间"的既定事实与一张可以慢慢回味的位置差异；是否在后期剧情（如大学/"天"）中解释，留待文案/主线排期。

---

## 5. 身份证档案（背包道具，D3 + D11）

### 5.1 发放与查看（D11）

- 身份证 = **背包道具**：`resources/data/items.json` 新增：
  ```json
  { "id": "id_card", "name": "艾莉森的证件",
    "kind": "document", "stack_max": 1, "base_price": 0,
    "icon": { "atlas": "res://assets/tilesets/tile_forest/tileset_forest.png", "region": [0, 0, 0, 0] },
    "desc": "贝戈尼亚森林的居民证。上面的信息与你完全相符——可你从未登记过。选中并「翻看」。", "sellable": false }
  ```
  （`kind="document"` 为新增枚举；icon region 待美术切图。）
- **发放时机**：`treehouse_rented` 后**首次切进 `alison_room`**，检测 `flags["id_card_obtained"]` 未置位 → `Inventory.add("id_card")` + `GameManager.discover_clue("clue_id_card")` + `set_flag("id_card_obtained")` + toast「你在一只抽屉里翻出了自己的证件……」。循环重置不重发（flag 跨天保留）。
- **查看入口**：背包详情区选中 `id_card` → 「翻看」按钮 → `IdCardPanel`。房间床头柜是"发现地"道具化后的叙事残留，首进即触发发放，无需单独常驻交互。

### 5.2 卡片数据：`resources/data/id_card.json`（初稿，待 owner 校）

卡片本体含 **名字 / 身份 / id / 住址** 四项（D11 必填）：

```json
{
  "card": {
    "fields": [
      { "label": "名字",   "value": "艾莉森" },
      { "label": "身份",   "value": "居民（打印的职务栏被一行不属于登记处的小字划掉，勉强能辨认是「…女巫」。）" },
      { "label": "id",     "value": "BEL-0-1313（末位数字被火燎去，边缘焦黄）" },
      { "label": "住址",   "value": "贝戈尼亚森林 · 树屋区（户主：艾莉森）" }
    ],
    "photo_note": "照片栏空白，只压着一朵未开的野百合标本。",
    "issuer_note": "发证机关被墨水涂去；背面只有一句手写——「你总会找回来的。」"
  },
  "entries": [
    { "id": "back",    "text": "证件背面贴着一张泛黄的纸：凯尔特山地。父母早逝，被邻家姐姐带大。你记得来处，却想不起自己何时登记过。", "unlock": { "rented": true } },
    { "id": "teacher", "text": "你十二岁那年，在森林采浆果时遇见她。此后你的安静多了一处去处。", "unlock": { "day_min": 3 } },
    { "id": "burn",    "text": "大火从背上烧起来那夜，你曾拼命护住这张纸。纸角还留着当时的指印。", "unlock": { "clue": "clue_burn_scar" } },
    { "id": "padwin",  "text": "你救过一个人。他用你的秘密换了自己的命——然后把名字，写进了你不想再翻的一页。", "unlock": { "clue": "clue_padwin_no_memory" } },
    { "id": "soraya",  "text": "总有人一遍遍对你说「欢迎来到贝戈尼亚森林」。你在第几次，开始觉得这句话其实不是对你说的？", "unlock": { "clue": "clue_soraya_repeat" } },
    { "id": "origin",  "text": "有人在你名字下方用铅笔写了一句：「她不属于这个时代。」", "unlock": { "clue": "clue_forest_fake" } },
    { "id": "heaven_u","text": "你在大学的第一课：这森林所谓的法律，来自一个你还没见过的「天」。（这一页的字迹比其它页都要新。）", "unlock": { "flag": "univ_heaven_lesson" } },
    { "id": "tower_t", "text": "「猜猜我是谁。」——你把这句话抄在证件最后一页。可抄完才发现，字迹不是你的。", "unlock": { "flag": "tower_tian_talk" } }
  ]
}
```

- 首版即发放；卡片本体固定。`entries` 由 R-B 逐条解锁（条件语法复用 GameManager 求值器：day_min / rented / clue / flag）。
- **文案为初稿，需 owner/文案校**；`univ_heaven_lesson` / `tower_tian_talk` 两个 flag 对应 D14 主线三推的落点（命名待定）。

### 5.3 查看 UI

- `scripts/ui/id_card_panel.gd` + main 层 CanvasLayer（rental_ui 范式）：卡片本体四项 + 照片/发证机关备注；下方为已解锁条目，未解锁显示"…"。
- 新条目解锁 toast「证件上似乎多了些字……」。
- 解锁状态存 `GameManager`（`flags["idcard_entry_<id>"]` 或 `clues_found` 派生），跨天保留。

---

## 6. 「我的一天」清单（基地功能，D5 + D9）

### 6.1 混合制内容分层（D9）

| 层 | 来源 | 说明 | 离线 |
|---|---|---|---|
| L1 天的预言 | `GameManager.daily_prophecy` | 每日顶部一行（obey/defy 态度已在结算） | 无则隐藏该行 |
| L2 系统自动骨架 | quest_manager 未完成委托 / 可用任务 / 当日可做事件 / 关键 flag 缺口 | 自动汇总为"今天可以做的事"，不依赖 AI | 恒有 |
| L3 关键日定制 | 文案逐日模板 + 天(AI) 建议 + **主线三推落点（§6.3）** | 主线日/关键日插入，格式同 L2 | 回落 L2 |

- 已接委托汇总 = `quest_manager.active` 只读列出（与广场告示牌同数据，此面板不接新单）。
- 建议数据结构可挂 quests.json 同目录新文件或 quest_manager 内（实现时定）：`resources/data/daily_notes.json`——`{ "day": 1, "title": "…", "items": ["…"] }` 逐日文案表，缺省回落 L2。
- 文案量待文案组：首批关键日文案（第 1/3/5/10/20 天等 + 主线日）。

### 6.2 UI

- `scripts/ui/my_day_panel.gd` + main 层 CanvasLayer：竖排三区（今日预言 / 我的一天 / 已接委托），可勾选打勾（仅本地状态，跨天 reset）。
- 打开：清单板交互，或状态面板可并入（二选一，倾向独立，不动 status_panel——同 `玩法扩展` M1 决策惯性）。

### 6.3 主线三推（D14，关键日/结局门控上游，日期未定）

三条驱动剧情的关键主线，**具体日期未定**，先冻结"类别 + 推动 + 落点"，作为 daily_notes / L3 与结局门控的候选上游，日期后补：

| 主线 | 内容 | 剧情推动 | 落点（flag/线索方向） |
|---|---|---|---|
| ① 大学听课 | 学习"天"的事 | 提升对循环真相的理解 | 新 flag（如 `univ_heaven_lesson`，门控 `id_card.heaven_u`） |
| ② 秘密寻巫 | 寻找其他女巫 | 推动→**广场公开暴露身份**（触发被带去石巢塔楼） | 与 soraya_confronted / 暴露剧情节拍衔接 |
| ③ 探石巢塔楼 | 主动探索塔楼 | 触发与"天"的**直接对话**（22 结局关键） | 新 flag（如 `tower_tian_talk`，门控 `id_card.tower_t`） |

> 实现上：`daily_notes.json` 只排 L3 的具体"某天"文案；主线落点本身由 `day_min + flag` 条件求值，与具体日期解耦——日期想好后再补表即可，不动机制。

---

## 7. 日界接管流程（D4 + D12，同一棵树内落地）

```
[未租房 / 第一天]  清晨：reset_loop → plaza/PlayerSpawn 醒 → day_started 自动抽塔罗（现行为不变）
[有房（day≥2）]  清晨：reset_loop → alison_room/WakeUp（床边）醒 → day_started 不开塔罗
                   → 玩家坐占卜桌按 E → TarotUI 自主抽牌 → today_divined = true
                   → DoorOut 软锁解除，出门（target=treehouse_district/SpawnFromRoom）

白天在森林奔走；傍晚/午夜回树屋区
  → 午夜 midnight_reached：若不在家 HUD 弱提示「该回去了」（全局面板退役）
  → 回房间 → 床按 E：
      阶段1 「天在审视这一天…」（HeavenSession 复盘，AI 断联回落固定文案）
      阶段2 确认 → apply_heaven_rules() → reset_loop()
  → 次晨按上表醒来
```

- **D12 门控细节**：`today_divined` 为单日字段（reset_loop 置 false）。房门软锁只在 `treehouse_rented && current_area=="alison_room" && !today_divined` 时生效：按 E 过门给提示「今天的塔罗还没抽。」→ 抽完放行。
- 未租房的玩家进不了房间（树屋门只给租房 UI）→ 现 `test_sleep_no_house` 的语义由门控天然保证，测试改为校验门分流。
- 全局午夜入睡面板退役；入睡判定只在房间床触发。sim_multi_day / test_history / test_heaven_session 等日界测试需回归验证（§9）。

---

## 8. 里程碑拆解（每片演示确认，参照 `玩法扩展` §8 任务卡风格）

### R-A 新区地基（加区契约，先打这刀）

1. 建 `scenes/levels/alison_room.tscn`：骨架对齐 v0.2 §2.1（`Ground=wooden_floor.tres` autotile 铺好、`Props` 边界墙、`Overlay` 空、`BedroomDoorIn/WakeUp/DoorOut`）
2. `main.tscn` 的 `Areas` 挂 `instance = alison_room.tscn`（根名 `AlisonRoom` → `alison_room`，零改行为代码接入）
3. 树屋门分流：`treehouse_door.gd` 已租 → 切 `alison_room`；房间 `DoorOut` → `treehouse_district/SpawnFromRoom`（树屋区补该 Marker）
4. **首进房间发放身份证**：`id_card` 进背包（items.json kind=document）+ flag 防重 + discover `clue_id_card`
5. **背包翻看证件**：inventory_panel 详情区"翻看"按钮 → `id_card_panel`（卡片本体四项 + 首条 `back` 文案）
6. 床入睡两阶段（搬运天复盘，同一棵树原地复用）+ 全局面板退役
7. 有房日清晨醒来落 `WakeUp` + **不自动弹塔罗**
8. 占卜桌家具 + 自主触发 TarotUI + `today_divined` 置位 + **DoorOut 软锁**
9. 回归全绿（§9）
10. **演示验收：租屋→进门得证件→背包能翻看→夜里床上睡→清晨在床醒→不占卜出不了门→占卜后出门**

### R-B 档案生长

1. `id_card.json` 条目数据化 + 条件求值解锁（day_min/affection/clue/flag）
2. 条目 UI 完整化（解锁/未解锁态、toast 新条目）
3. 文案补全条目文本（校 §5.2 初稿）
4. 回归 + 演示：多天回房/触发主线后翻证件看到条目增多

### R-C 「我的一天」清单

1. `my_day_panel.gd` + 清单板家具
2. L1 预言行、L2 系统骨架（quest_manager 未完成/可用 + 当日事件）、委托汇总
3. L3 关键日文案表 `daily_notes.json` + 主线三推（§6.3）落点 + 天(AI)建议接口（走现有 ai 会话/回落）
4. 回归 + 演示

### R-D 药水制作台（D6 第二版）

按 `玩法扩展` §6.2 定价算法落地：台子 → 投料 → 酿制（时段推进）→ 定价；出口复用索拉雅收购。台子作为房间 `Props`/家具内容节点，无需新区。

---

## 9. 测试策略

- **回归**：`autotest.gd` 全量；重点 `test_sleep_no_house`（改门分流语义）、`sim_multi_day`、`test_history`、`test_heaven_session`、`test_quest`、`test_inventory`、`test_status_panel`。
- **新增**：
  - `scenes/tests/test_room_door.tscn`：未租→租房 UI；租后门分流进/出房间、落点正确、`current_area` 正确、区 id snake 注册无需改 game.gd。
  - `scenes/tests/test_sleep_home.tscn`：午夜床上两阶段（judgment→sleep）、清晨有房落 `WakeUp`/未租落 plaza、day+1。
  - `scenes/tests/test_id_card.tscn`：首进发放一次（flag 防重）、背包翻看开面板、条目按 day/clue/flag 逐条解锁。
  - `scenes/tests/test_tarot_gate.tscn`：有房日清晨不自动弹塔罗；DoorOut 软锁；占卜后放行一天。
  - `scenes/tests/test_my_day.tscn`（R-C）：L1/L2 内容正确、L3 关键日落回 L2、委托汇总与 quest_manager 一致。
- **加区契约回归点**（v0.2 §7 精神）：新区加入后 `switch_area` 无需新增分支即能切进/切出，契约束节点（Ground/Props）缺失时游戏不崩、优雅降级。

---

## 10. 资产与文案需求（走现有外包/文案流程，不阻塞程序）

### 程序可占位
| 用途 | 占位方案 |
|---|---|
| 房间地面 | **`wooden_floor.tres` 已就位**（D5 已拍板归房间），无需占位 |
| 墙/家具件（含占卜桌） | 现有 props 改色 / 占位矩形；正式家具 sprite 入美术清单 |
| 证件 item 图标 | 先复用 tileset_forest 某格占位（region 待切），正式图标入美术清单 |
| 证件/清单面板 UI | rental_ui 同款底 |

### 需要文案
| 内容 | 量级 | 切片 |
|---|---|---|
| 身份证卡片本体四项 + 条目 8 条（初稿见 §5.2） | 校稿 | R-A/R-B |
| 家具/氛围物点读（旧信/照片/草药笔记） | 3–5 段 | R-A–R-C |
| 「我的一天」关键日模板 + 主线三推逐日文案 | 首批若干（日期待定） | R-C |
| 门控提示（「今天的塔罗还没抽」等） | 2–3 句 | R-A |

### 需要美术
| 资产 | 切片 | 备注 |
|---|---|---|
| 房间家具 sprite（床/占卜桌/清单板/床头柜/书桌/烛台等） | R-A 起 | 优先级高（R-A 就要床/桌/柜） |
| 证件 item 图标 / 卡片美术 | R-A | 可与 UI 面板外包合一 |

---

## 11. 风险与待定

1. **守壳 ABI 是硬约束**：房间区契约束节点名（`AlisonRoom/Ground/Props/Overlay`）冻结，改名前必须同步 game.gd 并回写 v0.2 §2.1 表；新增"内容"节点随意。**Player/UI CanvasLayer 根结构不动**。
2. **接入面虽小但都落在行为层**：`treehouse_door.gd` 分流、`game.gd` 午夜/醒来/塔罗自动逻辑/动作路由、inventory_panel 翻看按钮是现有回归的重灾区，R-A 需全量回归（§9），严禁夹带。
3. **无存档**：身份证解锁、「我的一天」打勾、`today_divined` 只跨天不跨重启；数据按可序列化设计，等存档系统补齐 serialize。
4. **AI 依赖（R-C L3）**：天的建议是增强非必需；回落系统骨架，与现有 AI 断联策略一致。
5. **文案/美术依赖**：占位先进游戏，到位后替换 json/贴图即可，不动代码。身份证条目初稿（§5.2）待 owner/文案校。
6. **待 owner 拍板**：
   - §5.2 身份证条目初稿文案是否 OK（可逐条改）；
   - D14 主线三推的**具体日期**与落点 flag 命名（大学→`univ_heaven_lesson`、探塔→`tower_tian_talk` 为占位）；
   - 「我的一天」关键日清单；
   - 房间具名家具名单（占卜桌已按 D12 加入）；
   - D12 门控宽严：默认"一天占一次放行一天"；是否要改成更软的提示 / 更硬的绝对门控。
7. **审核节点**：R-A 完成后先演示等确认，再进 R-B；后续每片同。

---

## 12. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1.0 | 2026-09-03 | 首版。意图澄清 D1–D10；§3 曾按"跨场景壳 + change_scene"设计。 |
| v0.1.1 | 2026-09-06 | 依《场景重构_工程文档_v0.2》校准：§3 重写为遵 §2.6 加区契约加第 4 区，废弃常驻壳/change_scene，Player/UI 根结构不动；地面 wooden_floor.tres 已就位；R-A 降险重写。 |
| v0.1.2 | 2026-09-06 | owner 二轮细化 D11–D14：身份证改为**背包道具**（首进房间发放一次、背包翻看、卡片四项）；占卜改**占卜桌自主触发 + 当天不占卜不出门**（D12，有房日起）；房间**唯一性**（D13，不按 house_id 分实例）；主线三推（D14，大学/寻巫暴露/探塔）作关键日与结局门控上游；§5.2 附身份证条目初稿、§7 日界流程含门控、R-A 任务扩至 10 条。 |
