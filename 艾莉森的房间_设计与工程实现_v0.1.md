# 艾莉森的房间 · 设计与工程实现 v0.1

> 状态：待审核。审核通过后按里程碑顺序开发，每片完成后演示确认再进入下一片。
> 代码仓库：`D:\AIGameBuild`（main 分支），Godot 项目目录 `成为艾莉森的一天/`。
> 本文档所有 `res://` 路径均相对 Godot 项目根目录。
> **工程约束：本文档的工程接入（§3）以《场景重构_工程文档_v0.2》的壳 ABI 契约为准**——房间按 **v0.2 §2.6 加区契约** 加为 Areas 下的新区，不动 Player / UI CanvasLayer 根结构。
> 相关文档：`场景重构_工程文档_v0.2.md`、`玩法扩展_设计与工程实现_v0.1.md`（M1–M5 底座）、`天控全局_第一回合切片_设计文档.md`、`《成为艾莉森的一天》游戏策划案.md`。

---

## 0. 意图澄清结论（2026-09-03 与 owner 对齐；D7 执行方式 2026-09-06 校准）

新场景「艾莉森的房间」的定位与边界，通过两轮澄清确认如下：

| # | 决定 | 结论 |
|---|---|---|
| D1 | 房间的第一身份 | **玩家基地/枢纽**：把"家里该干的事"聚合，功能优先，叙事随内容生长 |
| D2 | 形态与入口 | **独立室内场景，走进去**：树屋门从「租房 UI」升级为「进家」（未租房仍租房 UI） |
| D3 | 身份证 | **随天数/关键剧情解锁的档案**：租房即见基础信息，条目随 day/flag 生长 |
| D4 | 日界 | **接管**：清晨在床上醒来开始一天、回家入睡结束今天（现全局午夜入睡面板退役） |
| D5 | 委托事务清单 | **私有「我的一天」+ 已接委托汇总**；广场告示牌保留（公共委托仍在那接） |
| D6 | 药水制作台 | **第二版**（切 R-D），第一版不做可用制作 |
| D7 | 独立场景文件 | 意图：房间是**独立 .tscn 场景文件**、走进去。执行方式校准：**不做 change_scene/常驻壳**，按场景重构 v0.2 **加区契约**把 `scenes/levels/alison_room.tscn` 实例化进 Areas（与三外景同构），Player/UI 根结构不动 |
| D8 | 家具密度 | **功能件 + 少量氛围物**（可点出小文案），不追求全屋可交互 |
| D9 | 「我的一天」内容 | **混合制**：系统自动整理为骨架 + 关键日/主线日由 天(AI)/文案 定制，离线回落骨架 |
| D10 | 下一步 | 先出本设计文档，审核后再排 R-A 实现 |

> **D1 × D3 的内在张力（已共识）**：房间"第一天就能用，内容随时间生长"，因此房间不是一次性做完的场景，而是随版本迭代、每一片都有演示验收的长期承载。
>
> **D7 校准说明**：09-03 曾倾向"独立 .tscn + change_scene + 常驻壳（上提 Player/UI）"。09-05 场景重构 v0.2 冻结后该路径被取代——v0.2 已把三外景做成 `levels/*.tscn` 实例化进 Areas 的成熟机制（M1/M2a 零回归验证），并明确"改 Player/UI CanvasLayer 根结构"为非目标。故房间走 **加区契约**：既有"独立场景文件、编辑器易编辑"的好处，又零换壳、Player/UI 天然跨"区"存活（日界接管 D4 因此不存在换场景丢状态问题）。

---

## 1. 现状盘点（已存在、可直接复用；含场景重构 v0.2 执行结果）

写方案前已核对仓库：

| 已有 | 位置 | 复用方式 |
|---|---|---|
| 三外景 = 实例化新区机制 | `main.tscn`：`Areas/Plaza·TreehouseDistrict·StoneNestTower = instance of scenes/levels/*.tscn`；`switch_area` 按 `area.name.to_snake_case()==area_id` 切区 | **房间照抄此机制**：加第 4 个实例，零改壳逻辑 |
| 壳 ABI 契约束（v0.2，M0–M2b 已执行） | 碰撞层 `world/player/npc` 已写入 project.godot；三区根 y_sort；`terrain_query.gd`；塔楼地面已换 `tower_groud.tres` | 房间区骨架逐条对齐 v0.2 §2.1–§2.5 |
| 树屋租赁状态 | `game_manager.gd` 的 `treehouse_rented` / `treehouse` | 树屋门按它分流：未租→RentalUI，已租→切进 `alison_room` |
| 门语义（跨区切屏） | `scripts/systems/door.gd`：Area2D，`@export target_area/target_spawn` → `door_entered` → `game.gd.switch_area` | 房间出门 = 一个 `Door`，`target_area="treehouse_district"` |
| 树屋门（跳板） | `scripts/systems/treehouse_door.gd`：目前按 E → `game_action "rental:<house_id>"` | R-A 改：未租→原租房 UI；已租→发 `door_entered` 进房间 |
| 入睡两阶段（天复盘→真正入睡） | `game.gd._do_sleep/_run_judgment/_finish_sleep` + `HeavenSession` | 挪到房间床交互；因同一棵树，**代码原地复用**，无跨场景搬运问题 |
| 午夜面板 & 入睡门控 | `game.gd._build_midnight_panel`（未租禁睡） | R-A 后退役：入睡只发生在房间床上 |
| 每日塔罗抽牌 | `TarotUI`（main.tscn 常驻 CanvasLayer）+ `resources/tarot/major_arcana.json`，`game.gd._on_day_started` 打开 | 醒来抽牌逻辑不动（同一棵树，面板随时可开） |
| 线索系统 | `GameManager.discover_clue / clues_found`；**`ai_hint_whitelist` 已有 `clue_id_card`**（关键词：身份证/树屋里的照片/照片） | 身份证查看 = 发现 `clue_id_card` + `record_heaven_event("clue", …)` |
| 条件求值器 | `game_manager.gd` 对话条件（day_min / rented / clue / flag） | 身份证条目解锁复用同一套条件语法 |
| 天的每日预言 | `GameManager.daily_prophecy` + 态度结算 `apply_heaven_rules()`（obey/defy） | 「我的一天」顶部的"天的建议"直接读它 |
| 订单系统 | `quest_manager.gd` + `quest_board_ui.gd` + `quests.json`（广场告示牌） | 「我的一天」的"已接委托"汇总只读展示，不重复造系统 |
| 房间地面素材 | **`assets/tilesets/tile_forest/wooden_floor.tres`**（木地板，terrain autotile 已验证，v0.2 D5/§6.1 已拍板归艾莉森的房间） | 房间 `Ground` 直接用，无需等新美术 |
| 时段 | `time_manager.gd`（morning→midnight 五段） | 床入睡只能在午夜触发；清单按时段提示 |
| 事件总线 | `event_bus.gd`（door_entered / game_action / day_started / midnight_reached / loop_reset / toast 等） | 新交互一律发信号，不直接互调 |
| UI 弹窗范式 | `rental_ui.gd`（CanvasLayer 覆盖层范式，面板在 main 常驻层） | 身份证、清单、床确认面板照搬，挂在 main 层即可被房间区共享 |
| 家具式交互 | `treehouse_door.gd` / `notice_board.gd`（Area2D 感应 + 按 E）+ `interaction_hint_show/hide` | 房间家具交互复用同一感应模式 |
| 跨天持久化（内存态） | `reset_loop()` 不清背包/任务/好感 | 身份证已解锁条目、「我的一天」打勾都挂 Manager，随存档系统排期 |
| 现有回归 | `autotest.gd` + `scenes/tests/*`（test_sleep_no_house / sim_multi_day 等） | R-A 后全量回归；sleep 语义测试改指房间/门分流 |

**已知限制：当前无存档系统**（GameManager 注释已标明 user:// 持久化随存档排期）。"随天数解锁"在同一局内跨天成立；重启不保留属已知限制。

---

## 2. 一页产品概念（房间 v1 的可玩回路）

```
白天在森林四处奔走
    │  傍晚/午夜回树屋区 —— 树屋门：已租房？→ 推门进家（切进房间区）
    ▼
[艾莉森的房间] 独立室内场景文件（实例化新区，木地板）
    │
    ├─ 床 ──────── 午夜时「入睡」：先天的复盘审判，再结束今天
    ├─ 清单板 ──── 「我的一天」：今日预言 + 个人事务/目标 + 已接委托汇总
    ├─ 柜子/抽屉 ─ 身份证档案：租房即见基础信息，随 day/flag 长新条目
    ├─ 书桌 ────── 氛围点读：旧信 / 照片 / 草药笔记（小文案）
    └─ 烛台/旧书等 氛围物
    │  一觉醒来 —— 清晨在房间床上醒来，一天从家开始
    ▼
[又是同一天] 出房门 → 回到树屋区（树屋门口）
```

- 树屋门**分叉**：`treehouse_rented == false` → 原租房 UI；`true` → 切进 `alison_room`。
- 广场告示牌**不动**：公共委托仍在广场接，「我的一天」只是把已接委托汇总到家里看。
- 药水制作台位置预留，但 R-A–R-C 不实现可用制作（见 §8 R-D）。

---

## 3. 工程接入（R-A 核心）：遵 v0.2 加区契约加"第 4 区"

### 3.1 方案变更：为什么不用"跨场景壳"

09-03 首版曾设计"常驻壳 + change_scene 跨世界"（上提 Player 与全局 UI）。**该方案已废弃**，原因：

1. 场景重构 v0.2（09-05 冻结）已把"改 Player / UI CanvasLayer 根结构"列为**非目标**（v0.2 §0.3），并确立**唯一合规加区方式是 §2.6 加区契约**。
2. M1/M2a 已验证的更省机制：三外景各自 `scenes/levels/*.tscn`、在 `Areas` 下 `instance`，运行时是**同一棵树**；`switch_area` 靠根名 snake 切区。房间照抄即得"独立场景文件"，**零换壳**。
3. 日界接管（D4）原本担心的"换场景 Player/UI 丢"根本不存在——本来就在同一棵树，床交互直接复用 `game.gd` 现成的入睡两阶段。

### 3.2 目标运行时结构

```
scenes/main.tscn（游戏主场景，start_menu 过渡进入）—— 不变，只是 Areas 多一个实例
├─ Player（instance player.tscn）            ← 原位，不动
├─ DaylightModulate                          ← 原位，不动
├─ Areas
│  ├─ Plaza              instance scenes/levels/plaza.tscn
│  ├─ TreehouseDistrict  instance scenes/levels/treehouse_district.tscn
│  ├─ StoneNestTower     instance scenes/levels/stone_nest_tower.tscn
│  └─ AlisonRoom         instance scenes/levels/alison_room.tscn   ← R-A 新增
└─ （全局 UI CanvasLayer：DialogueUI/TarotUI/RentalUI/StatusPanel/HUD/
    SellPanel/EndingUI/TarotDivinationUI…）   ← 原位，房间区共享
```

### 3.3 房间区节点骨架（对照 v0.2 §2.1 契约①，逐条对齐）

`scenes/levels/alison_room.tscn` 根 = Node2D，名 `AlisonRoom`（`to_snake_case()` → area_id `alison_room`），`y_sort_enabled = true`：

| 节点路径（相对区根） | 类型 | 必需 | 说明 |
|---|---|---|---|
| `AlisonRoom`（根） | Node2D，`y_sort_enabled=true` | 必 | 区根；`switch_area` 按名命中 |
| `Ground` | TileMapLayer，`tile_set = res://assets/tilesets/tile_forest/wooden_floor.tres` | 必 | 木地板地面，autotile 铺好；**可走地面 → 无碰撞**（v0.2 §2.2） |
| `Props` | Node2D | 必 | 房间边界墙/床等挡人物件 = StaticBody2D（world 层，碰撞层见 §3.4） |
| `Overlay` | TileMapLayer | 预留 | 本轮可空 |
| `BedroomDoorIn` | Marker2D | 按区 | 进门落点（玩家从树屋门切进来站这） |
| `WakeUp` | Marker2D | 按区 | 清晨醒来落点（床边） |
| `DoorOut` | Area2D + `door.gd`，`target_area="treehouse_district"`，`target_spawn="SpawnFromRoom"` | 按区 | 出房门 |
| 床 / 清单板 / 柜子(身份证) / 书桌 / 氛围物 | Area2D 感应交互 / StaticBody2D | 按区 | 内容节点，可增删（v0.2 §2.1 维护规则：内容随便加，契约束节点名别改） |

> 配套：`treehouse_district.tscn` 增一个出生 Marker `SpawnFromRoom`（树屋门旁），供房间 `DoorOut` 指回——与三区现有的 `SpawnFromPlaza/SpawnFromEast` 命名习惯一致。

### 3.4 碰撞层（对照 v0.2 §2.5，已写入 project.godot）

| 层 | 名 | 承载 |
|---|---|---|
| 1 | `world` | 房间边界墙/挡人家具的 StaticBody2D（玩家 mask 含 1 → 撞墙不穿模） |
| 2 | `player` | 玩家（不新增） |
| 3 | `npc` | 房间无 NPC，不用 |

- 交互家具（床/柜/清单板）用 Area2D 感应检测 **player 层**、按 E 触发，**不参与物理阻挡**；若某件家具又要挡人又要能交互（如床），拆成「Props 下 StaticBody2D 挡人」+「独立 Area2D 感应」，两层解耦（同 v0.2 §2.3 精神：阻挡与交互分离）。
- 不做任何砍伐型可交互物 → 房间本轮**不引入 Interactable 组件**（那是外景树/石的方向）。

### 3.5 接入步骤（R-A 任务卡，见 §8）

1. 建 `scenes/levels/alison_room.tscn`（§3.3 骨架 + `wooden_floor.tres` autotile 铺好）。
2. `main.tscn` 的 `Areas` 加 `instance = alison_room.tscn`，节点名 `AlisonRoom` → 区 id `alison_room` 自动注册（`switch_area` snake 匹配，**零改行为代码即接入**）。
3. `treehouse_door.gd` 分流：`treehouse_rented` 为真时按 E → 发 `door_entered("alison_room","BedroomDoorIn")`（复用 Door 语义）；为假仍 `game_action "rental:…"`。
4. 房间区加 `DoorOut` → `treehouse_district/SpawnFromRoom`；树屋区补 `SpawnFromRoom` Marker。
5. 床交互：挂现成入睡两阶段（天复盘 → 睡）；`game.gd` 午夜门控从全局面板改到"床"（§7）。
6. 身份证 v0、塔罗照常（同一棵树，无需迁移 UI）。

### 3.6 行为层改动面（应改的最小集，全部在 game.gd / treehouse_door.gd，不碰壳）

| 改动 | 位置 | 性质 |
|---|---|---|
| 树屋门分流 | `treehouse_door.gd` `_input` | 行为 |
| 入睡门控与午夜面板退役 | `game.gd` 午夜相关函数 | 行为（现全局面板 → 仅床触发） |
| 清晨醒来落点 | `game.gd._on_loop_reset`：默认切区从 `plaza/PlayerSpawn` 改 `alison_room/WakeUp` | 行为 |
| 房间家具动作路由 | `game.gd._on_game_action` 增 `room:xxx` 分支 | 行为 |
| UI 面板（身份证/清单） | main 层新增 CanvasLayer | 内容，壳不动 |

> **R-A 确认点**：①醒来默认落点 = 房间 `WakeUp`（含每天开始），出门后才是树屋区——符合 D4"一天从家开始"；②循环重置后清晨塔罗在房间触发即可（面板同树，无位置概念）。实现时若发现 `loop_reset` 还要照顾"未租房开局/测试直接落广场"，用 `GameManager.current_area`/`treehouse_rented` 兜底分支。

---

## 4. 房间场景与家具（内容映射）

### 4.1 家具 → 交互 → 机制

| 家具 | 玩家动作 | 触发机制 | 切片 |
|---|---|---|---|
| 床 | 午夜按 E | 两阶段入睡（天复盘→睡），`midnight_reached` 后可用；兼清晨醒来落点 | R-A |
| 清单板 | 按 E | `my_day` 面板（§6） | R-C |
| 柜子/抽屉（身份证） | 按 E | `id_card` 面板（§5），首次查看 discover `clue_id_card` | R-A(基础) / R-B(生长) |
| 书桌 | 按 E | 氛围点读：一封旧信/照片/草药笔记（文案小卡片） | 散落各片 |
| 烛台/旧书/斗篷架 | 按 E | 氛围文案或「再平常不过的…」无实义回应 | 散落各片 |

- 交互形态：**Area2D 感应 + 按 E**（同 `treehouse_door.gd`/`notice_board.gd`），触发 `EventBus.game_action("room:xxx")`，由 `game.gd._on_game_action` 路由到对应面板（与现有动作路由同构）。
- 挡人件（边界墙、床体）放 `Props` 下 StaticBody2D（world 层）；感应区独立，互不耦合。
- 家具动作统一发 `EventBus.game_action`，房间与外部区共享同一套路由，不另立控制器。

### 4.2 房间布局（占位即可，正式家具美术入清单）

- 入口在房间左/下侧接 `DoorOut`；床在右（`WakeUp` 在床边）；清单板挂墙；身份证在床头柜/抽屉。
- 地面已定 `wooden_floor.tres`；墙/家具占位可先用现有瓦片/矩形（与 `bake_terrain.gd`/编辑器手摆套路一致），正式 sprite 走美术清单。

### 4.3 氛围物文案

书桌旧信/照片/草药笔记为首次点读内容，**文案走文案组，程序先放占位文本并标注**（同 `玩法扩展` §4.3 惯例）。

---

## 5. 身份证档案（叙事核心，D3）

### 5.1 设计

- 卡片本体：姓名 **艾莉森**、登记"贝戈尼亚森林居民"、发证机构模糊——基础信息租房即见，让玩家第一时间"满腹狐疑"（呼应策划案原文：艾莉森在树屋中发现与自己个人信息完全相符的身份证）。
- 条目**随天数 / 关键 flag 生长**：不同天回房看到不同内容，制造"这个身份是活的/在变化"的线索感。
- 与线索系统打通：查看身份证 = `GameManager.discover_clue("clue_id_card")`（白名单里已预留），并 `record_heaven_event("clue","…")`。

### 5.2 数据：`resources/data/id_card.json`（新建，样例）

```json
{
  "card": {
    "name": "艾莉森",
    "issued": "贝戈尼亚森林 · 居民登记处",
    "note": "证件边缘微微泛黄，像是替你保管了许多年。"
  },
  "entries": [
    { "id": "born", "text": "凯尔特山地。父母早逝，由邻家姐姐带大。",
      "unlock": { "rented": true } },
    { "id": "teacher", "text": "森林深处采浆果那年，遇见传授女巫知识的老师。",
      "unlock": { "day_min": 3 } },
    { "id": "burn", "text": "（纸面有被火燎过的焦痕，仿佛想抹掉什么。）",
      "unlock": { "clue": "clue_burn_scar" } },
    { "id": "padwin", "text": "……（这页曾在火光里救过一个人的命。）",
      "unlock": { "affection_min": { "padwin": 50 } } },
    { "id": "origin", "text": "有人在你名字下方用铅笔写了一句：'她不属于这个时代。'",
      "unlock": { "clue": "clue_forest_fake" } }
  ]
}
```

- `unlock` 条件语法复用 `GameManager` 对话条件求值器（day_min / rented / clue / affection / flags）。
- **文案量：条目数待 owner/文案定（首批建议 5–8 条）**；字段先按此结构占位。

### 5.3 查看 UI

- `scripts/ui/id_card_panel.gd` + main 层 CanvasLayer（rental_ui 范式）：左为证件正面（基础信息），下方/右为已解锁条目列表；未解锁条目显示为模糊的"…"占位。
- 新条目出现时 toast 提示「证件上似乎多了些字……」。
- 解锁状态存 `GameManager`（`flags["idcard_entry_<id>"]` 或 `clues_found` 派生），跨天保留。

---

## 6. 「我的一天」清单（基地功能，D5 + D9）

### 6.1 混合制内容分层（D9）

| 层 | 来源 | 说明 | 离线 |
|---|---|---|---|
| L1 天的预言 | `GameManager.daily_prophecy` | 每日顶部一行（obey/defy 态度已在结算） | 无则隐藏该行 |
| L2 系统自动骨架 | quest_manager 未完成委托 / 可用任务 / 当日可做事件 / 关键 flag 缺口 | 自动汇总为"今天可以做的事"，不依赖 AI | 恒有 |
| L3 关键日定制 | 文案逐日模板 + 天(AI) 建议 | 主线日/关键日插入，格式同 L2 | 回落 L2 |

- 已接委托汇总 = `quest_manager.active` 只读列出（与广场告示牌同数据，此面板不接新单）。
- 建议数据结构可挂 quests.json 同目录新文件或 quest_manager 内（实现时定）：`resources/data/daily_notes.json`——`{ "day": 1, "title": "…", "items": ["…"] }` 逐日文案表，缺省回落 L2。
- 文案量待文案组：首批关键日文案（第 1/3/5/10/20 天等 + 主线日）。

### 6.2 UI

- `scripts/ui/my_day_panel.gd` + main 层 CanvasLayer：竖排三区（今日预言 / 我的一天 / 已接委托），可勾选打勾（仅本地状态，跨天 reset）。
- 打开：清单板交互，或状态面板可并入（二选一，倾向独立，不动 status_panel——同 `玩法扩展` M1 决策惯性）。

---

## 7. 日界接管流程（D4，同一棵树内落地）

```
午夜（midnight_reached）
  → 玩家此刻不在家：HUD 弱提示「该回去了」（文案待定；旧全局面板不再强弹）
  → 玩家回到房间 → 床按 E：
      阶段1 「天在审视这一天…」（HeavenSession 复盘，AI 断联回落固定文案）
      阶段2 确认 → apply_heaven_rules() → reset_loop()
  → 清晨：reset_loop 把玩家落到房间 WakeUp（床边）醒来 → day_started → 塔罗抽牌（常驻 TarotUI）
  → 出门（DoorOut）→ treehouse_district / SpawnFromRoom（树屋门口）
```

- 未租房的玩家进不了房间（树屋门只给租房 UI）→ 现 `test_sleep_no_house` 的语义由门控天然保证，测试改为校验门分流。
- 全局午夜入睡面板退役；入睡判定只在房间床触发。sim_multi_day / test_history / test_heaven_session 等日界测试需回归验证（§9）。

---

## 8. 里程碑拆解（每片演示确认，参照 `玩法扩展` §8 任务卡风格）

### R-A 新区地基（加区契约，先打这刀）

1. 建 `scenes/levels/alison_room.tscn`：骨架对齐 v0.2 §2.1（`Ground=wooden_floor.tres` autotile 铺好、`Props` 边界墙、`Overlay` 空、`BedroomDoorIn/WakeUp/DoorOut`）
2. `main.tscn` 的 `Areas` 挂 `instance = alison_room.tscn`（根名 `AlisonRoom` → `alison_room`，零改行为代码接入）
3. 树屋门分流：`treehouse_door.gd` 已租 → 切 `alison_room`；房间 `DoorOut` → `treehouse_district/SpawnFromRoom`（树屋区补该 Marker）
4. 床入睡两阶段（搬运天复盘，同一棵树原地复用）+ 全局面板退役
5. 清晨醒来落 `WakeUp` + 塔罗（常驻 TarotUI 照常）
6. 身份证 v0：`id_card.json` 基础字段 + `id_card_panel.gd` 能看第一页；首查 discover `clue_id_card`
7. 回归全绿（§9）
8. **演示验收：能从家门进房、床上睡一觉、清晨在床醒来抽牌再出门**

### R-B 档案生长

1. `id_card.json` 条目数据化 + 条件求值解锁（day_min/affection/clue）
2. 条目 UI 完整化（解锁/未解锁态、toast 新条目）
3. 文案补全条目文本
4. 回归 + 演示：多天回房看到条目增多

### R-C 「我的一天」清单

1. `my_day_panel.gd` + 清单板家具
2. L1 预言行、L2 系统骨架（quest_manager 未完成/可用 + 当日事件）、委托汇总
3. L3 关键日文案表 `daily_notes.json` + 天(AI)建议接口（走现有 ai 会话/回落）
4. 回归 + 演示

### R-D 药水制作台（D6 第二版）

按 `玩法扩展` §6.2 定价算法落地：台子 → 投料 → 酿制（时段推进）→ 定价；出口复用索拉雅收购。台子作为房间 `Props`/家具内容节点，无需新区。

---

## 9. 测试策略

- **回归**：`autotest.gd` 全量；重点 `test_sleep_no_house`（改门分流语义）、`sim_multi_day`、`test_history`、`test_heaven_session`、`test_quest`、`test_inventory`、`test_status_panel`。
- **新增**：
  - `scenes/tests/test_room_door.tscn`：未租→租房 UI；租后门分流进/出房间、落点正确、`current_area` 正确、区 id snake 注册无需改 game.gd。
  - `scenes/tests/test_sleep_home.tscn`：午夜床上两阶段（judgment→sleep）、清晨在床醒（落 `WakeUp`）、day+1、塔罗触发。
  - `scenes/tests/test_id_card.tscn`：首查 discover `clue_id_card`；条件解锁条目按 day/affection/clue 逐条生效。
  - `scenes/tests/test_my_day.tscn`（R-C）：L1/L2 内容正确、L3 关键日落回 L2、委托汇总与 quest_manager 一致。
- **加区契约回归点**（v0.2 §7 精神）：新区加入后 `switch_area` 无需新增分支即能切进/切出，契约束节点（Ground/Props）缺失时游戏不崩、优雅降级。

---

## 10. 资产与文案需求（走现有外包/文案流程，不阻塞程序）

### 程序可占位
| 用途 | 占位方案 |
|---|---|
| 房间地面 | **`wooden_floor.tres` 已就位**（D5 已拍板归房间），无需占位 |
| 墙/家具件 | 现有 props 改色 / 占位矩形；正式家具 sprite 入美术清单 |
| 证件/清单面板 UI | rental_ui 同款底 |

### 需要文案
| 内容 | 量级 | 切片 |
|---|---|---|
| 身份证条目文本 | 5–8 条 | R-A/R-B |
| 家具/氛围物点读（旧信/照片/草药笔记） | 3–5 段 | R-A–R-C |
| 「我的一天」关键日模板 + 主线日 | 首批逐日若干 | R-C |

### 需要美术
| 资产 | 切片 | 备注 |
|---|---|---|
| 房间家具 sprite（床/柜/清单板/书桌/烛台等） | R-A 起 | 优先级高（R-A 就要床/柜/板） |
| 身份证/证件美术 | R-A | 可与 UI 面板外包合一 |

---

## 11. 风险与待定

1. **守壳 ABI 是硬约束**：房间区契约束节点名（`AlisonRoom/Ground/Props/Overlay`）冻结，改名前必须同步 game.gd 并回写 v0.2 §2.1 表；新增"内容"节点随意。**Player/UI CanvasLayer 根结构不动**。
2. **接入面虽小但都落在行为层**：`treehouse_door.gd` 分流、`game.gd` 午夜/醒来落点/动作路由是现有回归的重灾区，R-A 需全量回归（§9），严禁夹带。
3. **无存档**：身份证解锁、「我的一天」打勾、daily 文案都只跨天不跨重启；数据按可序列化设计，等存档系统补齐 serialize。
4. **AI 依赖（R-C L3）**：天的建议是增强非必需；回落系统骨架，与现有 AI 断联策略一致。
5. **文案/美术依赖**：占位先进游戏，到位后替换 json/贴图即可，不动代码。
6. **待 owner 拍板**：身份证条目数/解锁点清单；「我的一天」关键日清单；房间具名家具名单；**当前所有出租树屋共用同一间房间**（单切片先一个房间，若将来按 house_id 区分再加实例）；塔罗/审判是否全随房间（默认：塔罗在清晨醒来触发、天复盘在入睡前触发，位置无关——面板同树随处可开）。
7. **审核节点**：R-A 完成后先演示等确认，再进 R-B；后续每片同。

---

## 12. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1.0 | 2026-09-03 | 首版。意图澄清 D1–D10（基地枢纽/独立室内/身份证档案/接管日界/「我的一天」/制作台第二版/家具密度/混合制清单）；§3 曾按"跨场景壳 + change_scene"设计。 |
| v0.1.1 | 2026-09-06 | 依据《场景重构_工程文档_v0.2》校准：§3 重写为**遵 §2.6 加区契约加第 4 区**（`scenes/levels/alison_room.tscn` 实例化进 Areas），废弃常驻壳/change_scene，Player/UI 根结构不动；地面源改 **`wooden_floor.tres`**（D5 已就位）；§8 R-A 降险重写、§11 风险相应更新；加 §12 变更记录。 |
