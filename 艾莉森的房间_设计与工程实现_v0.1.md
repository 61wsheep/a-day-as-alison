# 艾莉森的房间 · 设计与工程实现 v0.1

> 状态：待审核。审核通过后按里程碑顺序开发，每片完成后演示确认再进入下一片。
> 代码仓库：`D:\AIGameBuild`（main 分支），Godot 项目目录 `成为艾莉森的一天/`。
> 本文档所有 `res://` 路径均相对 Godot 项目根目录。
> 相关文档：`玩法扩展_设计与工程实现_v0.1.md`（M1–M5 底座）、`天控全局_第一回合切片_设计文档.md`、`M4_系统设计_技术草稿.md`、`《成为艾莉森的一天》游戏策划案.md`。

---

## 0. 意图澄清结论（2026-09-03 与 owner 对齐）

新场景「艾莉森的房间」的定位与边界，通过两轮澄清确认如下：

| # | 决定 | 结论 |
|---|---|---|
| D1 | 房间的第一身份 | **玩家基地/枢纽**：把"家里该干的事"聚合，功能优先，叙事随内容生长 |
| D2 | 形态与入口 | **独立室内场景，走进去**：树屋门从「租房 UI」升级为「进家」（未租房仍租房 UI） |
| D3 | 身份证 | **随天数/关键剧情解锁的档案**：租房即见基础信息，条目随 day/flag 生长 |
| D4 | 日界 | **接管**：清晨在床上醒来开始一天、回家入睡结束今天（现全局午夜入睡面板退役） |
| D5 | 委托事务清单 | **私有「我的一天」+ 已接委托汇总**；广场告示牌保留（公共委托仍在那接） |
| D6 | 药水制作台 | **第二版**（切 R-D），第一版不做可用制作 |
| D7 | 工程形态 | **独立 .tscn 跨场景**：引入"常驻壳"，房间与外部世界作为世界场景切换 |
| D8 | 家具密度 | **功能件 + 少量氛围物**（可点出小文案），不追求全屋可交互 |
| D9 | 「我的一天」内容 | **混合制**：系统自动整理为骨架 + 关键日/主线日由 天(AI)/文案 定制，离线回落骨架 |
| D10 | 下一步 | 先出本设计文档，审核后再排 R-A 实现 |

> **D1 × D3 的内在张力（已共识）**：房间"第一天就能用，内容随时间生长"，因此房间不是一次性做完的场景，而是随版本迭代、每一片都有演示验收的长期承载。

---

## 1. 现状盘点（已存在、可直接复用）

写方案前已核对仓库：

| 已有 | 位置 | 复用方式 |
|---|---|---|
| 树屋租赁状态 | `game_manager.gd` 的 `treehouse_rented` / `treehouse` | 树屋门按它分流：未租→RentalUI，已租→进家 |
| 入睡两阶段（天复盘→真正入睡） | `game.gd._do_sleep/_run_judgment/_finish_sleep` + `HeavenSession` | 挪进房间床交互（R-A），天 AI 复盘保留、离线回落固定文案 |
| 午夜面板 & 入睡门控 | `game.gd._build_midnight_panel`（未租禁睡） | R-A 后退役：入睡只发生在房间床上 |
| 每日塔罗抽牌 | `TarotUI` + `resources/tarot/major_arcana.json`，`game.gd._on_day_started` 打开 | 醒来抽牌移到房间里/常驻层，逻辑不变 |
| 线索系统 | `GameManager.discover_clue / clues_found`；**`ai_hint_whitelist` 已有 `clue_id_card`**（关键词：身份证/树屋里的照片/照片） | 身份证查看 = 发现 `clue_id_card` + `record_heaven_event("clue", …)`，与主线谜团贯通 |
| 条件求值器 | `game_manager.gd` 对话条件（day_min / rented / clue / flag） | 身份证条目解锁复用同一套条件语法 |
| 天的每日预言 | `GameManager.daily_prophecy` + 态度结算 `apply_heaven_rules()`（obey/defy） | 「我的一天」顶部的"天的建议"直接读它 |
| 订单系统 | `quest_manager.gd` + `quest_board_ui.gd` + `quests.json`（广场告示牌） | 「我的一天」的"已接委托"汇总只读展示，不重复造系统 |
| 时段 | `time_manager.gd`（morning→midnight 五段 + `can_advance_to`） | 床入睡只能在午夜触发；清单按时段提示 |
| 事件总线 | `event_bus.gd`（door_entered / game_action / day_started / midnight_reached / loop_reset / toast 等） | 新交互一律发信号，不直接互调 |
| UI 弹窗范式 | `rental_ui.gd`（CanvasLayer 覆盖层范式） | 身份证、清单、床确认面板照搬 |
| 家具式交互 | `treehouse_door.gd`（Area2D 感应 + 按 E）+ `interaction_hint_show/hide` | 房间家具交互复用同一感应模式 |
| 跨天持久化（内存态） | `reset_loop()` 不清背包/任务/好感 | 身份证已解锁条目、每日笔记读/写都挂 Manager，随存档系统一起排期 |
| 现有回归 | `autotest.gd` + `scenes/tests/*`（test_sleep_no_house / sim_multi_day 等） | R-A 重构后全量回归；sleep 语义测试改指房间 |

**已知限制：当前无存档系统**（GameManager 注释已标明 user:// 持久化随存档排期）。"随天数解锁"在同一局内跨天成立；重启不保留属已知限制。

---

## 2. 一页产品概念（房间 v1 的可玩回路）

```
白天在森林四处奔走
    │  傍晚/午夜回树屋区 —— 树屋门：已租房？→ 推门进家
    ▼
[艾莉森的房间] 独立室内场景（原木/烛光/木质地板）
    │
    ├─ 床 ──────── 午夜时「入睡」：先天的复盘审判，再结束今天
    ├─ 清单板 ──── 「我的一天」：今日预言 + 个人事务/目标 + 已接委托汇总
    ├─ 柜子/抽屉 ─ 身份证档案：租房即见基础信息，随 day/flag 长新条目
    ├─ 书桌 ────── 氛围点读：旧信 / 照片 / 草药笔记（小文案）
    └─ 烛台/旧书等 氛围物
    │  一觉醒来 —— 清晨在房间床上醒来，一天从家开始
    ▼
[又是同一天] 出家门 → 回到树屋区外
```

- 树屋门**分叉**：`treehouse_rented == false` → 原租房 UI；`true` → 进家。
- 广场告示牌**不动**：公共委托仍在广场接，「我的一天」只是把已接委托汇总到家里看。
- 药水制作台位置预留，但 R-A–R-C 不实现可用制作（见 §8 R-D）。

---

## 3. 工程地基（R-A 核心）：跨场景"常驻壳"

### 3.1 为什么必须重构

现在**单场景**：`main.tscn`（根 = `scenes/main.tscn`）一棵树装着 Player、全部全局 UI（TarotUI / RentalUI / SellPanel / EndingUI / StatusPanel / HUD / quest_board_ui / history_panel / keybind_panel / 两塔罗面板）、世界（Areas/Plaza + NPC + 采集物 + 树屋门 + DaylightModulate）。`game.gd` 一身兼：区域切换、地形、蘑菇刷新、时段色调、午夜入睡、剧情动作路由、循环重置。

选型 D7（独立 .tscn 跨场景）+ D4（接管日界）后，"房间醒来 → 出门 → 白天 → 回家 → 床上入睡"要求 **Player 和全部覆盖 UI 必须同时在两个世界里存活**，否则第二次换场景 HUD/对话/背包就会消失。答案只有一个：把 Player 与全局 UI 从任意一张世界场景里抽出来，抬到一个**跨场景常驻层**上。

### 3.2 目标结构

```
scenes/game_root.tscn（新 boot，project main_scene 指向它）
└─ GameRoot（scripts/game_root.gd，原 game.gd 的世界无关职责收于此）
   ├─ Player（从 main.tscn 抽出，含相机/输入/采集判定引用）
   ├─ WorldRoot(Node2D 容器)   ← 世界场景在此换装
   │    ├─（当前实例）Forest：scenes/levels/forest.tscn   ← 现 main.tscn 的 Areas/Plaza/NPC/采集/树屋门/Daylight 抽出
   │    └─（切换后）AlisonRoom：scenes/levels/alison_room.tscn
   ├─ GlobalUI（CanvasLayer：HUD / dialogue / tarot / inventory / sell / status /
   │            quest_board / rental / history / keybind / ending / id_card / my_day）
   └─（Daylight 色调可留在各世界，或作为壳级按 GameManager.time 统一）
```

### 3.3 责任切分

| 层 | 职责 | 现状归属 | 去往 |
|---|---|---|---|
| Player | 移动、相机、锁定 | main.tscn 子节点 | 常驻壳 |
| 全局 UI 覆盖层 | 对话/塔罗/背包/售卖/状态/订单板/租房/历史/身份证/清单/结束 | main.tscn 子节点 | 常驻壳 GlobalUI |
| 世界内容 | 地面/墙/家具碰撞、NPC、采集物、房间物件、门 | main.tscn 内 Areas | 各世界场景（forest / alison_room） |
| 世界控制器 | 地形烘焙、蘑菇刷新、区域内部、家具交互路由、房间控制器 | `game.gd` | forest → 抽薄版；房间 → 新 `alison_room.gd` |
| 循环/状态 | reset_loop、apply_heaven_rules、current_area/进入点、时段色调 | `game.gd` + GameManager | GameManager / 壳级 |
| 跨世界门 | 门外树屋门（rented 分流）、房间出门、进房落点 | treehouse_door | 发 `change_world` 到壳 |

**跨世界切换原语**（新增，替代现有场景内 `switch_area` 的单例角色）：
`GameRoot.request_world(world_id, spawn_name)`：释放当前 WorldRoot 子场景 → 实例目标场景 → 置 Player 到 spawn → `current_area = world_id`，广播 `EventBus.area_changed`。

### 3.4 必须小心（风险集中区）

1. **门与落点**：树屋门现在 `E` → `game_action "rental:<house_id>"`；改后未租同款、已租 → `request_world("alison_room", "BedroomDoorIn")`。房间出门同理回 `"forest","TreehouseDoorOut"`（出门即树屋门口）。
2. **天复盘/入睡时序**：现 `_run_judgment`（HeavenSession 复盘，AI 断联回落）+ `_finish_sleep`（apply_heaven_rules → reset_loop）搬进房间床交互；终局/`should_end` 与 `HARD_DAY_CAP` 提示逻辑保持原语义。
3. **清晨塔罗**：day_started 在壳/房间床触发 TarotUI.open；不再依赖 forest 在场。
4. **`switch_area` 语义收窄**：壳内世界切换取代区域内门切换；区域内若还有别的门（如后续大学/塔楼内部门），统一走 `request_world`。
5. **引用清扫**：所有 UI 面板内部 `get_node` 依赖被抬入壳后路径变化；回归重点。

> **R-A 确认点（实现前拍板）**：① `main.tscn` 是改名当 forest 世界、还是原样保留当 boot 但把内容抽走（推荐后者：抽内容到 forest.tscn，main.tscn 收缩为 GameRoot）；② DaylightModulate 放壳还是留各世界。两条都有默认值，实现时按"改动最小"定。

---

## 4. 房间场景与家具（内容映射）

### 4.1 家具 → 交互 → 机制

| 家具 | 玩家动作 | 触发机制 | 切片 |
|---|---|---|---|
| 床 | 午夜按 E | 两阶段入睡（天复盘→睡），`midnight_reached` 后可用 | R-A |
| 清单板 | 按 E | `my_day` 面板（§6） | R-C |
| 柜子/抽屉（身份证） | 按 E | `id_card` 面板（§5），首次查看 discover `clue_id_card` | R-A(基础) / R-B(生长) |
| 书桌 | 按 E | 氛围点读：一封旧信/照片/草药笔记（文案小卡片） | 散落各片 |
| 烛台/旧书/斗篷架 | 按 E | 氛围文案或「再平常不过的…」无实义回应 | 散落各片 |

- 交互复用 `treehouse_door.gd` 的 Area2D 感应模式；抽象轻量基类 `scripts/systems/interactable.gd`（可选，先复制最小改动也行）。
- 家具动作统一发 `EventBus.game_action("room:xxx")`，由房间控制器 `alison_room.gd` 路由到对应面板——与现有 `game.gd` 动作路由同构。

### 4.2 房间布局（占位即可，正式美术入清单）

- 入口在房间左/下侧接「出门」；床在右；清单板挂墙；身份证在床头柜/抽屉。
- 室内贴图：**待美术**。占位可先用现有瓦片集改色/拼接（原木墙、地板），与 `bake_terrain.gd` 工具同套路；若刚入库的 `assets/tilesets/tile_forest/` 含室内件可优先用。

### 4.3 氛围物文案

书桌旧信/照片/草药笔记为首次点读内容，**文案走文案组，程序先放占位文本并标注**（同 `玩法扩展` §4.3 惯例）。

---

## 5. 身份证档案（叙事核心，D3）

### 5.1 设计

- 卡片本体：姓名 **艾莉森**、登记"贝戈尼亚森林居民"、发证机构模糊——基础信息租房即见，让玩家第一时间"满腹狐疑"（呼应策划案原文：艾莉森在树屋中发现与自己个人信息完全相符的身份证）。
- 条目**随天数 / 关键 flag 生长**：不同天回房看到不同内容，制造"这个身份是活的/在变化"的线索感。
- 与线索系统打通：查看身份证 = `GameManager.discover_clue("clue_id_card")`（白名单里已预留），并 `record_heaven_event("clue","…")`。

### 5.2 数据：`resources/data/id_card.json`（新建，样例）

```json
{
  "card": {
    "name": "艾莉森",
    "issued": "贝戈尼亚森林 · 居民登记处",
    "note": "证件边缘微微泛黄，像是替你保管了许多年。"
  },
  "entries": [
    { "id": "born", "text": "凯尔特山地。父母早逝，由邻家姐姐带大。",
      "unlock": { "rented": true } },
    { "id": "teacher", "text": "森林深处采浆果那年，遇见传授女巫知识的老师。",
      "unlock": { "day_min": 3 } },
    { "id": "burn", "text": "（纸面有被火燎过的焦痕，仿佛想抹掉什么。）",
      "unlock": { "clue": "clue_burn_scar" } },
    { "id": "padwin", "text": "……（这页曾在火光里救过一个人的命。）",
      "unlock": { "affection_min": { "padwin": 50 } } },
    { "id": "origin", "text": "有人在你名字下方用铅笔写了一句：'她不属于这个时代。'",
      "unlock": { "clue": "clue_forest_fake" } }
  ]
}
```

- `unlock` 条件语法复用 `GameManager` 对话条件求值器（day_min / rented / clue / affection / flags）。
- **文案量：条目数待 owner/文案定（首批建议 5–8 条）**；字段先按此结构占位。

### 5.3 查看 UI

- `scripts/ui/id_card_panel.gd` + CanvasLayer（rental_ui 范式）：左为证件正面（基础信息），下方/右为已解锁条目列表；未解锁条目显示为模糊的"…"占位。
- 新条目出现时 toast 提示「证件上似乎多了些字……」。
- 解锁状态存 `GameManager`（`flags["idcard_entry_<id>"]` 或 `clues_found` 派生），跨天保留。

---

## 6. 「我的一天」清单（基地功能，D5 + D9）

### 6.1 混合制内容分层（D9）

| 层 | 来源 | 说明 | 离线 |
|---|---|---|---|
| L1 天的预言 | `GameManager.daily_prophecy` | 每日顶部一行（obey/defy 态度已在结算） | 无则隐藏该行 |
| L2 系统自动骨架 | quest_manager 未完成委托 / 可用任务 / 当日可做事件 / 关键 flag 缺口 | 自动汇总为"今天可以做的事"，不依赖 AI | 恒有 |
| L3 关键日定制 | 文案逐日模板 + 天(AI) 建议 | 主线日/关键日插入，格式同 L2 | 回落 L2 |

- 已接委托汇总 = `quest_manager.active` 只读列出（与广场告示牌同数据，此面板不接新单）。
- 建议数据结构可挂在 quests.json 同目录新文件或 quest_manager 内（实现时定）：`resources/data/daily_notes.json`——`{ "day": 1, "title": "…", "items": ["…"] }` 逐日文案表，缺省回落 L2。
- 文案量待文案组：首批关键日文案（第 1/3/5/10/20 天等 + 主线日）。

### 6.2 UI

- `scripts/ui/my_day_panel.gd` + CanvasLayer：竖排三区（今日预言 / 我的一天 / 已接委托），可勾选打勾（仅本地状态，跨天 reset）。
- 打开：清单板交互，或状态面板可并入（二选一，倾向独立，不动 status_panel——同 `玩法扩展` M1 决策惯性）。

---

## 7. 日界接管流程（D4）

```
午夜（midnight_reached）
  → 玩家此刻不在家：HUD 弱提示「该回去了」（文案待定；旧全局面板不再强弹）
  → 玩家回到房间 → 床按 E：
      阶段1 「天在审视这一天…」（HeavenSession 复盘，AI 断联回落固定文案）
      阶段2 确认 → apply_heaven_rules() → reset_loop()
  → 清晨：在房间床上醒来 → day_started → 塔罗抽牌（TarotUI，常驻层）
  → 出门 → forest（树屋门口）
```

- 现 `test_sleep_no_house` 的语义迁移：**未租房根本进不了房间**（树屋门只给租房 UI）→ "无家可归不可入睡"由门控天然保证，测试改为校验门分流。
- sim_multi_day / test_history / test_heaven_session 等日界相关测试需回归验证（§9）。

---

## 8. 里程碑拆解（每片演示确认，参照 `玩法扩展` §8 任务卡风格）

### R-A 地基（先打这刀）

1. 常驻壳：抽 `scenes/game_root.tscn` + `game_root.gd`；Player、全局 UI 入壳；project main_scene 指向壳
2. 抽世界内容 → `scenes/levels/forest.tscn`（或 main.tscn 收缩为壳，二选一）；清 `game.gd` 里的引用
3. 树屋门分流：未租→RentalUI；已租→ `request_world("alison_room", …)`；房间出门回 forest
4. 房间场景骨架 `scenes/levels/alison_room.tscn` + `alison_room.gd`：地板/墙/碰撞、家具功能件占位、床入睡两阶段（搬运天复盘）
5. 醒来在床 + 清晨塔罗（常驻层触发）
6. 身份证 v0：`id_card.json` 基础字段 + `id_card_panel.gd` 能看第一页；首查 discover `clue_id_card`
7. 回归全绿（§9）
8. **演示验收：能从家门进房、床上睡一觉、清晨在床醒来抽牌再出门**

### R-B 档案生长

1. `id_card.json` 条目数据化 + 条件求值解锁（day_min/affection/clue）
2. 条目 UI 完整化（解锁/未解锁态、toast 新条目）
3. 文案补全条目文本
4. 回归 + 演示：多天回房看到条目增多

### R-C 「我的一天」清单

1. `my_day_panel.gd` + 清单板家具
2. L1 预言行、L2 系统骨架（quest_manager 未完成/可用 + 当日事件）、委托汇总
3. L3 关键日文案表 `daily_notes.json` + 天(AI)建议接口（走现有 ai 会话/回落）
4. 回归 + 演示

### R-D 药水制作台（D6 第二版）

按 `玩法扩展` §6.2 定价算法落地：台子 → 投料 → 酿制（时段推进）→ 定价；出口复用索拉雅收购。

---

## 9. 测试策略

- **回归**：`autotest.gd` 全量；重点 `test_sleep_no_house`（改门分流语义）、`sim_multi_day`、`test_history`、`test_heaven_session`、`test_quest`、`test_inventory`、`test_status_panel`。
- **新增**：
  - `scenes/tests/test_room_door.tscn`：未租→租房 UI；租后门分流进/出房间、落点正确、current_area 正确。
  - `scenes/tests/test_sleep_home.tscn`：午夜床上两阶段（judgment→sleep）、清晨在床醒、day+1、塔罗触发。
  - `scenes/tests/test_id_card.tscn`：首查 discover `clue_id_card`；条件解锁条目按 day/affection/clue 逐条生效。
  - `scenes/tests/test_my_day.tscn`（R-C）：L1/L2 内容正确、L3 关键日落回 L2、委托汇总与 quest_manager 一致。

---

## 10. 资产与文案需求（走现有外包/文案流程，不阻塞程序）

### 程序可占位
| 用途 | 占位方案 |
|---|---|
| 室内地板/墙 | 现有瓦片集拼接或 modulate（优先检查 `assets/tilesets/tile_forest/`） |
| 家具件 | 现有 props 改色 / 占位矩形 |
| 证件/清单面板 UI | rental_ui 同款底 |

### 需要文案
| 内容 | 量级 | 切片 |
|---|---|---|
| 身份证条目文本 | 5–8 条 | R-A/R-B |
| 家具/氛围物点读（旧信/照片/草药笔记） | 3–5 段 | R-A–R-C |
| 「我的一天」关键日模板 + 主线日 | 首批逐日若干 | R-C |

### 需要美术
| 资产 | 切片 | 备注 |
|---|---|---|
| 房间家具/室内贴图 | R-A 起 | 优先级高（R-A 就要床/柜/板） |
| 身份证/证件美术 | R-A | 可与 UI 面板外包合一 |

---

## 11. 风险与待定

1. **常驻壳重构是全项目迄今最大一刀**：UI/引用路径大挪移，回归面广。对策：R-A 范围压到"门走通 + 睡醒 + 身份证第一页"，其余内容绝不夹带。
2. **无存档**：身份证解锁、「我的一天」打勾、daily 文案都只跨天不跨重启；数据按可序列化设计，等存档系统补齐 serialize。
3. **AI 依赖（R-C L3）**：天的建议是增强非必需；回落系统骨架，与现有 AI 断联策略一致。
4. **文案/美术依赖**：占位先进游戏，到位后替换 json/贴图即可，不动代码。
5. **待 owner 拍板**：身份证条目数/解锁点清单；「我的一天」关键日清单；房间具名家具名单与室内美术来源（tile_forest 是否含室内件）；`main.tscn` 重构取向（§3.4 确认点）。
6. **审核节点**：R-A 完成后先演示等确认，再进 R-B；后续每片同。
