# 开场动画 · 美术资产需求 v0.1

> **项目：**《成为艾莉森的一天》| **用途：** 开场五拍（绳→苔→树→看→撞）从「纯文字旁白」升级为「有镜头运动的过场演出」
> **本文档要回答的问题：** 做这件事需要哪些美术资产、能不能复用现有的、大概多少工作量
> **配套：** 格式规范见《美工音乐工作格式要求》；文案原文见 `resources/data/opening.json` 与 `文案交付原件_文案同学_v0.1.md`

---

## 〇、先给结论

**技术上完全可行，而且不难。** Godot 2D 做这件事只需要三样东西，引擎原生支持、不需要插件：

| 能力 | 用什么做 | 现状 |
|------|---------|------|
| 镜头平移/缩放/旋转 | `Camera2D` + `Tween`（或挂在场景上的 `AnimationPlayer`） | 主场景已有 `Camera2D`，加时间轴即可 |
| 多层视差纵深 | 同一个 `Parallax2D` 下挂 2-3 层不同 `motion_scale` 的 `Sprite2D` | 引擎原生节点 |
| 拍与拍之间的黑场/叠化 | 一张全屏 `ColorRect` 做透明度 Tween | 开场 UI 已在做淡入 |

**真正的工作量全在美术上，不在程序上。** 程序侧是 3-5 人天一次性的（我来做，做完五拍共用一套运镜系统）；美术侧取决于选哪一档，**从 0 到 45 人天，差 10 倍以上**。

所以本文档的核心价值是下面这个分档 —— 建议先拍板选档，再谈细节。

| 档位 | 做法 | 新增美术 | 美术工作量 | 视觉收益 |
|------|------|---------|-----------|---------|
| **A 档** | 维持现状：纯黑底 + 文字，只加程序淡入淡出 | **0 张** | 0 | 无（现状） |
| **A+ 档**（推荐起步） | 每拍 1 张全屏整图，程序做平移/缩放/叠化（Ken Burns 式运镜） | **4 张**整图 | **约 6-12 人天** | 高 —— 从「读文字」变成「看画面」 |
| **B 档** | 每拍拆远/中/近三层做真视差，加局部动画（缆车、滴水、粒子） | **11 张背景层 + 10 张道具/特效** | **约 22-45 人天** | 最高 —— 真正的电影感 |

> **推荐路径：先做 A+ 档把「镜头动起来」这件事验证掉**，画面照样能看，成本只有 B 档的四分之一。B 档的视差分层可以在 A+ 的基础上**逐拍替换**（同一张整图拆成三层画即可，不浪费）。

---

## 一、五拍镜头脚本

先定镜头，再倒推资产 —— 每一拍「镜头怎么动」直接决定「要画多大的图、要拆几层」。

### 拍 1 ·「绳」

> 文案原文：*她记得的不是火，是绳子。……热是从脚底那堆柴上来的，先烧断的是绳子。*

| 项 | 内容 |
|----|------|
| 镜头 | 从手腕上那圈麻绳的**特写**起，缓慢**向上摇**（tilt up），经过围观人群的脸，停在上方空的天 |
| 层次 | 近景：手与绳（占满画面）｜中景：人群脸孔（虚化）｜远景：火刑柱与天 |
| 关键设计 | **不要出现火**。文案写的是「记得的不是火，是绳子」，画面只给绳、给人群、给天 —— 火只在音效里（柴火噼啪）与人脸的暖光反光里。这既是忠于文案，也顺手省掉最难画的一组火焰资产 |
| 资产需求 | 见 §二 · 拍 1 |

### 拍 2 ·「苔」（唯一的 POV 拍）

> 文案原文：*再有知觉的时候，她是趴着的。脸底下是苔，湿的，凉……*

| 项 | 内容 |
|----|------|
| 镜头 | **极低机位**，几乎贴地，横向**极缓平移**（slow pan），景深极浅 —— 焦点在苔，远处树根虚化 |
| 层次 | 前景：苔地（铺满、轻微失焦）｜中景：她的手臂与烧剩半边的袖子、踩烂的浆果｜远景：树根（重度虚化） |
| 关键设计 | 这一拍**镜头就是她的眼睛**，所以画面上不该出现她的脸。整拍的「动」靠苔的质感 + 一点点风（草叶微动）撑住，节奏要慢 |
| 资产需求 | 见 §二 · 拍 2 |

### 拍 3 ·「树」（最贵的一拍）

> 文案原文：*树是往上走的。……这些树看不见顶 —— 树干上开了窗，一层一层往上开……第七层还是第八层有个女人正把湿衣裳挂出来。*

| 项 | 内容 |
|----|------|
| 镜头 | **垂直向上摇**（tilt up）：脚下石板路 → 树干上的窗与花盆 → 看不见顶的树干 → 树与树之间的绳桥、缆车 |
| 层次 | 近景：两侧框景树（暗，带出景深）｜中景：树干 + 窗 + 花盆 + 晾衣女人｜远景：绳桥、缆车、天光 |
| 关键设计 | 这是全开场**唯一能「炫」的一拍**，也是叙事上第一次让玩家意识到「这里不是她原来的世界」。**动态是关键**：晾衣女人的湿衣裳在滴水（水珠落下要很久才落地 —— 这是文案里的细节，是「树有多高」的度量衡）、缆车在绳桥上缓慢移动 |
| 资产需求 | 见 §二 · 拍 3 —— **如果只做一拍，做这一拍** |

### 拍 4 ·「看」

> 文案原文：*有人从她跟前过去。一个提着藤篮的老太太……一个抱着一卷东西的年轻人……三个孩子跑过去。*

| 项 | 内容 |
|----|------|
| 镜头 | 从**树后**的视角，横向**缓慢平移**扫过广场，前景有树干与叶隙的黑色遮挡（她躲在树后，玩家和她一起「偷看」） |
| 层次 | 前景：树后遮挡（近黑剪影 + 叶隙透光）｜中景：广场与路过的三个人｜远景：广场对面的树屋 |
| 关键设计 | **这一拍最便宜** —— 中景广场可以直接用现有素材（`forest_plaza.png`），NPC 也可以复用现有角色精灵。唯一的新资产是那张前景遮挡层 + 三个龙套 |
| 资产需求 | 见 §二 · 拍 4 |

### 拍 5 ·「撞」

> 文案原文：*撞上的时候袋子先破，蘑菇滚了一地……她抬起头，看着对面那个人的脸，问了一句 ——「你是不是女巫？」*

| 项 | 内容 |
|----|------|
| 镜头 | 固定中景 → 撞击瞬间**镜头轻震**（camera shake）→ 缓慢**推近**到两人之间的地面，停在滚进石板缝的蘑菇上 |
| 层次 | 背景：广场（复用）｜中景：索拉雅 + 艾莉森（复用现有角色精灵，艾莉森用「惊讶」帧）｜前景：滚散的蘑菇（高分辨率剧情版） |
| 关键设计 | 开场在这里**戛然而止** —— 镜头停在蘑菇上，不进对话。这一拍是全开场的钩子，不要拖长 |
| 资产需求 | 见 §二 · 拍 5 |

---

## 二、资产清单

### 拍 1 · 绳

| 类别 | 资产名 | 尺寸 | 数量 | 说明 |
|------|--------|------|------|------|
| bg | `bg_rope_hand` | 1920×1080 | 1 | 手腕与麻绳特写（近景层，铺满画面） |
| bg | `bg_rope_crowd` | 1920×1080 | 1 | 围观众人的面孔（中景层，透明挖空） |
| bg | `bg_rope_pillar` | 1920×1080 | 1 | 火刑柱与天空（远景层，透明挖空） |
| fx | `fx_smoke_ember_spritesheet` | 32×32 ×8 帧 = 256×32 | 1 | 火星/烟，弱叠加，**不要大的火焰** |
| sfx | `sfx_rope_burn.wav` | ≤2s | 1 | 绳子烧断（麻纤维崩裂） |
| sfx | `sfx_fire_crackle.ogg` | 30s 循环 | 1 | 柴火声，压得很低 |

> **省成本选项（推荐）：** 这一拍**不做具象画面**，维持纯黑底 + 文字，只在音效里给柴火声与人声嘈杂。文案本身就说「她记得的不是火」，不给画面反而更狠，且**省掉拍 1 全部美术**。我倾向这一档。

### 拍 2 · 苔

| 类别 | 资产名 | 尺寸 | 数量 | 说明 |
|------|--------|------|------|------|
| bg | `bg_moss_ground` | 256×256（**无缝可平铺**） | 1 | 苔地近景，用于横向平移时循环拼接 |
| bg | `bg_moss_fore` | 1920×1080 | 1 | 中景：她的手臂、烧剩半边的袖子、踩烂的浆果（透明） |
| bg | `bg_moss_roots` | 1920×1080 | 1 | 远景：重度虚化的树根（透明） |
| bg | `bg_moss_blur` | 1920×1080 | 1 | 可选：散焦光斑叠加层，做景深用 |
| sfx | `sfx_bell_distant.wav` | ≤6s | 1 | 远处的钟声（文案：*不是教堂的钟……它只是响过*） |
| amb | `amb_forest_ground.ogg` | 2min 循环 | 1 | 贴地环境音：近处虫鸣 + 很轻的风 |

### 拍 3 · 树

| 类别 | 资产名 | 尺寸 | 数量 | 说明 |
|------|--------|------|------|------|
| bg | `bg_tree_trunk` | **2160×3840**（竖版 2 倍屏） | 1 | 中景：树干、逐层的窗、窗台花盆、晾衣女人与湿衣裳 |
| bg | `bg_tree_canopy` | **2160×3840**（竖版 2 倍屏） | 1 | 远景：绳桥、缆车轨道、天光（透明挖空） |
| bg | `bg_tree_frame` | 1920×1080 | 1 | 近景：两侧框景树（暗剪影，透明，左右各一可镜像） |
| fx | `fx_cable_car` | 128×64 ×4 帧 = 512×64 | 1 | 缆车（也可只给 1 张、程序做位移动画，省一半） |
| fx | `fx_drip_water` | 16×16 ×3 帧 = 48×16 | 1 | 晾衣滴水的水珠（**这张是「树有多高」的叙事道具，建议保留**） |
| sfx | `sfx_transport.wav` | ≤2s | 1 | 缆车滑过（**格式文档 §7.3 已列，可能已有**） |

> **这一拍是全套里最贵的**，两张竖版大图约 3-5 人天（单独计）。若要做减法，优先砍 `bg_tree_canopy`（远景改为程序绘制的渐变色 + 少量剪影），保留 `bg_tree_trunk`。

### 拍 4 · 看

| 类别 | 资产名 | 尺寸 | 数量 | 说明 |
|------|--------|------|------|------|
| bg | `bg_watch_fg` | 1920×1080 | 1 | 前景：树后遮挡，近黑剪影 + 叶隙透光（透明） |
| bg | — | — | **0** | 中景广场：**复用 `forest_plaza.png`**，无需新画 |
| npc | `npc_extra_elder_idle/walk` | 64×64 ×(2+6) = 128×… | 1 | 提藤篮的老太太 |
| npc | `npc_extra_youth_idle/walk` | 64×64 ×(2+6) | 1 | 抱一卷东西的年轻人 |
| npc | `npc_extra_child_idle/walk` | 64×64 ×(2+6) | 1 | 三个孩子（同一张换色复用即可，只算 1 人） |

> **龙套可以大幅砍价：** 这三人**只需要「朝右走」一个方向**（不是可操作 NPC，不会转向、不会对话），所以每人 1 张 8 帧单方向精灵表即可，规格从标准 NPC 的 `384×256` 砍到 `384×64`。若再省，可用**剪影处理**（只画背影轮廓），每人半天。

### 拍 5 · 撞

| 类别 | 资产名 | 尺寸 | 数量 | 说明 |
|------|--------|------|------|------|
| chr | `chr_alison_surprise` | 64×64 ×3 帧 = 192×64 | 1 | 艾莉森「惊讶」帧（**格式文档 §2.1 已列，属既有 P1 项**） |
| npc | — | — | **0** | 索拉雅：复用现有角色精灵 |
| bg | — | — | **0** | 广场：复用 |
| icon | `prop_mushroom_*` | **128×128** | 5 | 蘑菇散落用的**高分辨率剧情版**（现有 32×32 图标放大会糊） |
| icon | `prop_berry_*` | **128×128** | 2 | 同上，浆果 |
| icon | `prop_herb_*` | **128×128** | 1 | 同上，草药 |
| prop | `prop_mushroom_bag_intact` | 128×128 | 1 | 索拉雅的大蘑菇袋（完好） |
| prop | `prop_mushroom_bag_burst` | 128×128 | 1 | 同上（破口） |
| sfx | `sfx_bag_burst.wav` | ≤1s | 1 | 袋子崩破 |
| sfx | `sfx_mushroom_scatter.wav` | ≤2s | 1 | 蘑菇滚落石板 |

> 注意：**现有 32×32 采集品图标不能直接用于近景** —— 镜头推近后像素会糊。这一拍是近景，必须有 128×128 的剧情版。反过来，这 8 张画完后也能在背包 UI 里复用（保持 32×32 缩略图不变，美术源文件多一版）。

### 跨拍通用

| 类别 | 资产名 | 尺寸 | 数量 | 说明 |
|------|--------|------|------|------|
| bgm | `bgm_opening.ogg` | 60-90s | 1 | 开场 BGM。建议做**两段可交叉淡入**（拍 1-2 压抑/空、拍 3-5 明亮/开阔），运镜时会用 |
| ui | `fx_letterbox` | 1920×1080 | 1 | 上下黑边（或程序直接画 ColorRect，可 0 成本） |
| sfx | `sfx_heartbeat.wav` | ≤2s | 1 | 转场心跳，拍 1→2、拍 4→5 用 |

---

## 三、工作量估算

> **口径说明：** 这是给「1 名 2D 美术」的粗估，含概念设计、起草、上色、导出切层，**不含反复修改轮次**。实际通常要在此基础上留 30% 余量。数字用于判断量级，不是报价。

| 项目 | A 档 | A+ 档 | B 档 |
|------|------|-------|------|
| 全屏整图（1920×1080 级） | 0 | 4 张（拍 2/3/4/5 各 1） | 8 张（拆层） |
| 竖版大图（2160×3840） | 0 | 0（拍 3 用降级整图） | 2 张 |
| 可平铺材质 | 0 | 0 | 1 张（苔地） |
| 龙套 NPC 精灵表 | 0 | 0 | 3 张（单方向，可剪影） |
| 道具/图标（128×128） | 0 | 0 | 10 张（蘑菇/袋） |
| 2D 动画（缆车/滴水/火星） | 0 | 0 | 3-4 组 |
| 艾莉森惊讶帧 | 0 | 0 | 1 张（既有 P1 项） |
| **美术合计** | **0** | **约 6-12 人天** | **约 22-45 人天** |
| 音频（BGM 1 + 音效 6） | 0 | 约 3-4 人天 | 约 3-4 人天 |
| 程序（运镜系统 + 时间轴） | 约 2-3 天 | 约 3-5 天 | 约 3-5 天 |
| **总计** | **约 2-3 天** | **约 10-20 人天** | **约 28-54 人天** |

**分档读法：**
- **A 档**：几乎不花钱，但开场体验维持现状（文字 + 黑底）。只值得作为「运镜系统先搭起来」的技术铺垫。
- **A+ 档**：**性价比拐点**。4 张图就能让五拍全部有画面、有运动，是「从无到有」的那一跳。
- **B 档**：完整电影感，但成本是 A+ 的 3-4 倍。**建议等 A+ 上线、玩家反馈确认开场值得投入后再做**，且可以逐拍替换（A+ 的整图拆成三层就是 B 档的层，不浪费）。

---

## 四、给美工的补充格式要求

现有《美工音乐工作格式要求》没有覆盖「过场背景」，需要补三条。**建议这份文档定稿后，把下面内容回写进格式文档 §2/§8。**

### 4.1 过场背景画布尺寸

| 类型 | 尺寸 | 说明 |
|------|------|------|
| 全屏背景层（横） | **1920×1080** 或其整数倍（如 3840×2160） | 与 UI 画布同级的**全屏豁免** —— 不要求是 32 的倍数（现有 1080 本身就不是） |
| 竖版背景层 | **2160×3840** | 竖向运镜专用（拍 3）。横向 2160 = 32×67.5 ❌ 同样走全屏豁免 |
| 可平铺材质 | 32 的整数倍（256、512） | 与图块同规则，**必须无缝** |
| 层内元素 | 仍按 **32 网格** 对齐 | 全屏豁免只针对画布，不针对层内内容 |

**分层交付规则：** 每层导出为**独立 PNG**，同一拍所有层**尺寸一致、位置对齐**（即把它们叠起来 = 一张完整画面）。透明区域用 alpha，不要用白底。

> ⚠️ 若走 2 倍屏（3840×2160）路线，请美术**从一开始就按 2 倍出图**，不要后期放大 —— 运镜推近时会看出糊。

### 4.2 命名规范补充

现有类别缩写（`chr`/`npc`/`tile`/`ui`/`icon`/`fx`/`bgm`/`sfx`/`ui_sfx`/`amb`）需**新增一个 `bg`**：

```
bg   = 过场背景层 (background)

✅ 正确：
  bg_rope_hand.png          （拍1 绳 · 手部近景层）
  bg_moss_ground.png        （拍2 苔 · 地面层，可平铺）
  bg_tree_trunk.png         （拍3 树 · 树干中景层）
  bg_watch_fg.png           （拍4 看 · 前景遮挡层）
  npc_extra_elder_walk.png  （龙套 · 老太太行走）
  prop_mushroom_bag_burst.png（道具 · 破口的蘑菇袋）
```

**分层文件请在同一拍内用同一前缀**（`bg_rope_*`、`bg_moss_*`、`bg_tree_*`、`bg_watch_*`），程序按前缀自动归类到同一组。

### 4.3 交付时额外附一份「叠层预览」

每拍除分层 PNG 外，额外交付 **1 张把该拍所有层合起来的预览图**，用于程序对位校验。这一条是硬要求 —— 分层对不齐是过场美术最常见的返工点。

---

## 五、建议的推进顺序

```
1. 拍板选档（A / A+ / B）              ← 现在需要你定这一件事
2. 程序侧先搭运镜系统（我，3-5 天）      ← 与选档无关，可以先跑
   · 用现有的广场图 + 占位图跑通五拍时间轴
   · 美术交图后直接替换贴图，代码不用改
3. 美术按拍交付（优先级：拍3 > 拍4 > 拍5 > 拍2 > 拍1）
4. 交一拍接一拍，不等全套齐
```

**为什么程序可以先跑：** 运镜系统吃的是「一组图层 + 一张时间轴」，与图长什么样无关。用临时占位图先把时间轴和手感调好（每拍停多久、摇多快、黑场多久），美术出图后替换即可。这样美术和程序**并行**，不互相等。

**为什么拍 3 优先：** 它是唯一必须有的「惊叹点」，也是成本最高的。先做它，能最早暴露「竖版大图 + 运镜」这条链路上有没有坑。

---

## 附：与现有资产的关系

| 现有资产 | 开场能否复用 | 说明 |
|---------|------------|------|
| `forest_plaza.png`（广场图集） | ✅ **拍 4、拍 5 中景直接复用** | 俯视角，适合「她躲在树后看广场」的视角 |
| `npc_soraya_*` / `chr_alison_*` | ✅ 拍 5 复用 | 仅需补 `chr_alison_surprise`（格式文档已列） |
| `icon_mushroom_*`（32×32） | ❌ **近景不能用** | 会糊，需另出 128×128 剧情版 |
| 树屋区图集（128×128 树屋） | ❌ **拍 3 不能用** | 那是俯视角，拍 3 是仰视，视角冲突，必须新画 |
| `fx_time_transition_*.png` | ✅ 转场可复用 | 5 张时间过渡遮罩，拍与拍之间可直接当叠化层 |
| `sfx_transport.wav` | ✅ 拍 3 缆车音效可能已有 | 格式文档 §7.3 已列，需与音频确认是否已交付 |

> **一句话：** 目前能复用的主要是「广场 + 现有角色」，五拍里**拍 4 最省、拍 3 最贵**。开场动画的成败几乎全押在拍 3 那棵树画得好不好。
