# NPC 对话 AI 正式接入 —— 脑暴想法集

> 状态：**v2 · 已适配「无AI试玩Demo v1.1」数据驱动引擎** · 2026-08-19
> 目的：把 NPC 对话 AI 从"离线验证合格"推进到"正式接入游戏"的决策支持材料。
> 版本说明：v1 为基线存档（基于早期硬编码对话骨架）；2026-08-19 项目升级为「无AI试玩Demo v1.1」（JSON 数据驱动对话引擎），v2 为适配评估与调整。

---

## Context —— 为什么"测试合格"不等于"可以接入"

**项目现状（脑暴撰写时）**：
- 游戏骨架完成：玩家移动、采集、时间循环（5 时段）、硬编码对话、角色立绘已导入。
- AI 离线验证基本合格：V1 JSON 可靠性 PASS（100%）、V2 NPC 一致性自动规则 PASS、V3 塔罗质量待人工评分；Padwin 跨天记忆已跑到 day 18 / 好感 94 / 161 轮，人设不崩、跨天记忆生效——**AI 的能力验证是合格的**。
- 但 **Godot 游戏侧没有任何 AI 接入点**：对话硬编码，无 AI Bridge / 无 HTTP 请求；`change_affection()` 定义了但没人调用；`clues_found` 是空数组；`benevolence/engagement/truth_proximity` 连变量都不存在。

**总体判断（三句话）**：
1. **最大风险不是"AI 会不会崩"，而是"AI 会不会剧透"**。CLI 验证是"无门控全开放"——玩家在测试里已经逼出了"火刑细节、三百零一级台阶、咽喉剑伤"这类深水内容。照搬管线进游戏，NPC 第一天就可能说出第十天该说的真相，直接击穿石巢塔楼"猜猜我是谁"的高潮和 22 结局结构。**接入的第一步必须是给 AI 套剧情约束，而不是接 API。**
2. **最大的机会是"身体记忆"机制 + `hints_to_other_npcs` 交叉印证**——它们已经验证跑通了（帕德温番茄过敏/剑伤/面对艾莉森的熟悉感），是现成的、成本极低的探秘闭环入口，千万别用硬约束把它压死。
3. **最大的架构风险是"状态所有权"**——验证侧把好感/记忆/天数全存在 Python 的 JSON 里，游戏侧 GameManager 又有自己一套。正式接入前必须先定契约：**Godot 管数值方向、AI 只输出表情与文本**（这本来就是设计定论 2.1）。

---

## 一、差距全景（CLI 聊天 → 游戏内正式对话，缺什么）

按维度列缺口，优先级 ⭐⭐⭐ = 最高（不补就接不了/接了就是事故）。

| 维度 | 缺什么 | 为什么缺 | 优先级 |
|------|--------|---------|--------|
| **剧情限制** | 真相信息门控表（谁说/何时说/说到哪） | 验证只测"能不能"，没测"会不会超纲" | ⭐⭐⭐ |
| **剧情限制** | 每 NPC 每阶段"可说/不可说"清单 | 角色卡只有静态禁忌，无阶段化 | ⭐⭐⭐ |
| **剧情限制** | 剧情锚点事件接管（第3/7/12天、番茄试探、勒痕、塔楼） | 锚点文本散落在策划案里，未进 AI 资产 | ⭐⭐ |
| **剧情限制** | 对话节奏约束（单日轮次上限、时段联动收束） | CLI 是陪聊工具，游戏是事件驱动的叙事 | ⭐⭐⭐ |
| **任务驱动** | 对话→线索→交叉印证闭环（`hints_to_other_npcs` 字段已存在但没人消费） | 验证只跑单 NPC 独聊 | ⭐⭐⭐ |
| **任务驱动** | 主线四阶段状态注入（起始/发展/高潮/结局 flag） | S7 任务系统未实现 | ⭐⭐ |
| **任务驱动** | 每日塔罗/预言注入对话（今日主题） | 天命面具未接入 | ⭐⭐ |
| **天博弈** | benevolence / engagement / truth_proximity / prophecy_resistance 状态 | 全在设计文档附录里，Godot 零字段 | ⭐⭐ |
| **天博弈** | 天态度注入对话 + "天替 NPC 挡刀"护栏 | 从未定义"NPC 知识边界 = 玩家已知 ∩ 该NPC碎片" | ⭐⭐ |
| **状态所有权** | Godot↔AI 所有权契约（Godot 管数值、AI 管表情） | 验证侧把一切握在 Python 手里 | ⭐⭐⭐ |
| **状态所有权** | emotional_shift 的 Godot 侧校验结算 | `change_affection()` 无调用方 | ⭐⭐⭐ |
| **状态所有权** | 记忆存储归 Godot（per-NPC 本地 JSON） | 验证侧格式可直接移植为参照 | ⭐⭐⭐ |
| **体验** | 延迟/加载态/超时（验证 8-21s 一条，CLI 阻塞式） | 对话 UI 同步推进，从未考虑网络往返 | ⭐⭐⭐ |
| **体验** | 话题建议接入三入口（90% 玩家走话题建议，是基本盘） | UI 骨架已在，只差数据源 | ⭐⭐⭐ |
| **体验** | 断网/超时/解析失败的三级兜底文本库 | 内容资产排期靠后，完全没做 | ⭐⭐⭐ |
| **工程** | AI Bridge Node（HTTPRequest + 异步 + 超时 + 兜底链） | 第 1 步明令"无 AI" | ⭐⭐⭐ |
| **工程** | 请求组装层（把验证侧的 user_msg 模板移植进 Godot + 注入时段/主题/线索） | 组装逻辑全在 Python | ⭐⭐⭐ |
| **工程** | schema 契约复用纪律（原样进游戏，hints 建议升必填） | 无人做过"验证产物→游戏契约"映射 | ⭐⭐⭐ |
| **工程** | token 预算、并发/重入保护（切场景/快速连点/Esc 中断） | CLI 不在乎成本，游戏在乎 | ⭐⭐ |
| **内容资产** | Soraya / Cactus Bishop 角色卡（现在是零内容） | 验证只测了 Padwin 一个 | ⭐⭐ |
| **内容资产** | 塔罗剩 17 张牌、兜底文本库、话题 curriculum | 素材排期靠后 | ⭐⭐ |

---

## 二、方向 (a)：剧情限制 —— 如何给 AI 自由对话套剧情约束又不显得机械

> 设计红线：**门控是"角色边界"而不是"系统提示"**——玩家问超前问题时，NPC 要像真人有边界一样自然挡回，而不是跳出一个"该话题暂未解锁"。

**A1 · 真相信息门控表（truth_gate_table）** ⭐ 最推荐
- 机制：一张 JSON 表，键 = (NPC, 天数区间, 话题 tag)，值 = 泄露等级（0 绝不提 / 1 可暗示 / 2 可说 / 3 主动揭晓）。Godot 组装 prompt 时注入"本 NPC 当前可泄露清单"，AI 只在此范围内回答。
- 体验：问超前问题被自然挡回，像角色边界。
- 成本 S；风险：表太粗机械、太细爆炸 → 缓解：**只对 ~10 条核心事实建表**（循环本质、天的身份、塔楼、前世背叛、番茄过敏、勒痕、女巫身份、破循环条件、裂缝、索拉雅/卡克特斯立场），其余放手。

**A2 · 可说/不可说扮演指令**（can_say / must_not_say）
- 机制：不建布尔硬闸，写成扮演指令段："若玩家问 X，你真实想法是 Y，但你只能暗示 Z"。与 C4（挡刀）天然合并。
- 体验：NPC 的隐瞒有内在矛盾感（帕德温说"我不知道你在说什么"时右手无意识按向剑伤），不是冷拒绝。
- 成本 S-M；风险：AI 用暗语泄露 → Godot 关键词命中时替换兜底文本（双重护栏）。

**A3 · 剧情锚点强制接管**（scripted anchor）
- 机制：第 3/7/12 天关键事件、番茄试探、勒痕发现、塔楼答题——AI 暂停，先播作者文本/过场再交还。
- 体验：关键剧情节奏稳定，不赌 AI 发挥。
- 成本 S；风险：突然变硬 → 缓解：锚点也走 AI 管线（AI 负责 emotional_shift/memory 字段，文本用作者稿），接口统一。

**A4 · 话题 curriculum 出题系统**
- 机制：Godot 维护"今日话题方向队列"，由（天 × 好感段 × 已获线索 × truth_proximity）计算；AI 生成话题建议时携带 2 个目标方向标签。
- 体验：对话有方向感但不强迫，探秘线索自然浮现。
- 成本 M；风险：建议雷同 → 缓解：队列只给"方向标签"，具体措辞是 AI 的。

**A5 · AI 与剧本混合**（hybrid dialogue）
- 机制：日常闲谈 = AI 自由；关键节点 = 作者剧本；两类都走同一 AI 管线，剧本轮 AI 只生成情感与记忆字段。
- 体验：两头都稳——日常有 AI 的活、关键点有作者的准。
- 成本 M。

> **v1 倾向**：A1（门控表）+ A3（锚点接管）打底，A2/C4 合并成"挡刀机制"作为差异化卖点，A4/A5 在第 4-5 步再做。**第 3 步最小接入也必须带一个极简版门控（10 条 can't-say）**，否则剧透事故必然发生。

---

## 三、方向 (b)：任务驱动目标 —— 让"探秘"有具体的可玩抓手

### 统领思路：把抽象终极目标转译为"可作答的证据游戏"

"探秘揭示天最终的面目"作为**故事目标**是对的，但作为**每日可玩目标**太抽象。成熟的设计手法是拆成「远目标 + 近目标」两层：

> **远目标（不变）**：第 20 天（或软上限日）在石巢塔楼回答"天是谁"——匹配 22 结局，达成"世界"才破循环。
> **近目标（每日）**：收集/印证证据——对话线索 + 物件线索，午夜检验预言应验。

这样"探秘"变成经典的**证据收集 → 交叉印证 → 最终作答**推理循环，每天都有可执行、可检验的抓手，且直接服务现有结局判定 S10（塔楼回答 + 条件匹配）。下面的 B1-B6 都是"近目标"层可选的素材。

### 备选方案

**B1 · 线索物件 + 对话线索交叉印证** ⭐ 最推荐（成本最低、回报最高）
- 机制：线索分"物件线索"（勒痕、番茄、剑伤、烧伤疤）与"对话线索"（`hints_to_other_npcs` 已埋好，现在没人消费）。未证实线索拿到另一 NPC 面前验证。
- 体验：探秘有推理感，NPC 之间互相咬合（索拉雅说循环 → 问卡克特斯 → 他挡刀 → 侧面坐实）。
- 成本 M；风险：AI 对线索状态感知不足则印证断裂 → prompt 注入"玩家当前已知线索集合"。
- **这是第 3 步就该接的**：`hints_to_other_npcs` 升必填 → 写进 `clues_found` → 注入下次对话。

**B2 · 三重真相拼图**（trinity truth puzzle）
- 机制：索拉雅 = 循环与重启规则；帕德温 = 前世背叛与女巫事件；卡克特斯 = 保护者动机与塔楼身份。三碎片各挂在一个情感弧线节点上揭晓，每次揭晓加 truth_proximity。
- 体验：主线推进可被感知，"世界"结局路径清晰。
- 成本 M；风险：揭晓时机与好感/天数耦合卡死 → 每条碎片设 2 个触发路径（深问 / 事件锚点）。

**B3 · 每日塔罗任务**
- 机制：预言里埋 1-2 个"可应验"行为种子，玩家做/不做计入顺从/反抗，改变当天对话气氛与 benevolence/engagement。
- 体验：预言从装饰变成可玩筹码，"反抗天命"有了落脚点。
- 成本 M；风险：预言太准=剧透、太糊=无感 → 事件种子 AI 生成但 Godot 校验可行性（地点存在、NPC 在场）。

**B4 · "前世死亡痕迹"调查线** ⭐ 差异化潜力最大
- 机制：卡克特斯自缢勒痕、艾莉森烧伤疤、帕德温咽喉剑伤，三处"死亡痕迹"构成横贯线，每确认一处推进 truth_proximity 并解锁对应 NPC 深层对话。
- 体验：把"死亡"前置到日常，悬疑密度高；天然与"挡刀机制"（身体记忆）共鸣。
- 成本 L；风险：与结局判定耦合复杂 → 先做"发现/确认"两态，不做完整调查树。

**B5 · 石巢塔楼"猜猜我是谁"升级博弈**
- 机制：塔楼从"终点一次选择题"升级为"多日博弈"——每日午夜给一个局部判断，最终回答前可用对话"质询"修正判断。
- 体验：高潮被拉长，博弈感强。
- 成本 L；风险：与"午夜一次判定"原设定冲突 → 默认仍一次判定，质询是 P1 追加玩法。

**B6 · 身份之谜双线**
- 机制："我是艾莉森（女巫）"与"天是谁"两条疑问绑定；问错身份问题触发天的察觉（engagement 升、NPC 打太极）。
- 体验：身份即筹码，玩家在"装傻"与"摊牌"间走钢丝。
- 成本 M。

> **v1 倾向**：以"证据游戏"为统领，第 3 步先接 **B1（线索交叉印证）**——它是验证合格资产的最小闭环；第 4-5 步再上 **B3（每日塔罗）+ B4（死亡痕迹线）** 作为两大支柱。B5 是 P1 锦上添花，B6 与 B2 可合并。

---

## 四、方向 (c)：天与玩家的博弈 —— 天如何通过 NPC 介入

> 设计红线：**NPC 日常绝不破戏**。天的存在感靠"隐约的破绽"而非明说——这本身就是博弈：玩家在拼凑真相，天在观察玩家。

**C1 · benevolence/engagement 驱动语气矩阵**
- 机制：Godot 把两值映射为"天的态度标签"（冷酷/好奇/温柔/审判），每轮注入 prompt 的"天气"段，AI 让 NPC 的话在情绪底色上体现（不点破）。
- 体验：行为让世界温度变化，可感知不可量化。
- 成本 S-M；风险：玩家无感 → 配合午夜结算回看"今天天怎么对你"。

**C2 · 反抗系统的对话侧表现**
- 机制：玩家违抗预言当天，NPC 用反常态度回应（"你今天不对劲""你昨天……没有去"）；Godot 按 prophecy_resistance 分档注入"天的注意级别"（1 无感 / 2 午夜评注 / 3 裂缝概率↑）。
- 体验：反抗有即时回响，且"隐约感"本身就是天的存在感。
- 成本 M；风险：泄漏"天在监控" → 注意级别只在 NPC 反应里隐约体现，不说破。

**C3 · 裂缝借 NPC 之嘴**（差异化卖点）
- 机制：裂缝时刻不直接独白，而是"帕德温说出他不该知道的话"（一个词、一句侧写），玩家意识到天借了 NPC 的嘴。
- 体验：比直接独白更瘆人、更省 token（复用对话管线）。
- 成本 S；风险：玩家没意识到 → 午夜回顾点破"今天有一个人说了不该说的话"。

**C4 · 敏感问题"天替 NPC 挡刀"** ⭐ 与 A2 合并做，差异化最大
- 机制：玩家问出超纲问题时，AI 强制切到"身体反应而非语言"（帕德温按剑伤、索拉雅手抖、卡克特斯摸勒痕），不给信息；Godot 关键词命中时直接换兜底文本。
- 体验：真相的边界是可触摸的身体记忆——这正是"帕德温番茄过敏是唯一身体记忆"的机制化放大，是游戏最强素材。
- 成本 M；风险：指令失效 → 双重护栏（prompt 指令 + Godot 兜底）。

**C5 · 信息优势不对称设计**（天全知伪装 / NPC 半知 / 玩家拼凑）
- 机制：三层不对齐；Godot 维护"玩家已知集合"，注入 prompt 时只给 AI"该 NPC 知道 ∩ 玩家已知"的交集。
- 体验：没有全知者捷径，拼凑本身就是玩法。
- 成本 M；风险：玩家收集到矛盾信息 → 把矛盾设计成"裂缝的破绽"，顺理成章吃掉 bug。

**C6 · "天的观察者评注"暗流**
- 机制：每轮对话的 `internal_note`（AI 生成、玩家不可见）由 Godot 收集，午夜汇成"天对你的观察"（玩家可见但不知是天的视角）。
- 体验：多层叙事，重玩时读出完全另一个故事。
- 成本 M-L。

> **v1 倾向**：天博弈是"润物细无声"的——**C4 挡刀（接入时就做）+ C1 语气矩阵（第 4 步）是骨架，C3 借 NPC 之嘴（第 5 步裂缝面具有了再做）是护城河**。C2/C5/C6 都是低成本高回报的加分项，可穿插。

---

## 五、接入路径与推荐组合

### 路径 A · 薄切片快速接入（2-3 周）—— 默认推荐
- 只接 Padwin + 化身面具 + 最小 AI Bridge：AI Bridge Node（HTTPRequest+超时+三级兜底）→ Padwin 对话从硬编码切 AI → 话题建议接通三入口 → 记忆归 Godot → 只读注入（day/affection/时段）→ emotional_shift 校验结算 → **极简门控（10 条 can't-say）** → `hints_to_other_npcs` 升必填接 `clues_found`。
- 适合：最快回答"验证合格的东西在游戏里不减配吗、体验如何"。这是 v2.0 计划第 3 步的原意，别绕开。
- 风险最低、反馈最快。**唯一前置是 Padwin 卡（已合格）。**

### 路径 B · 并行补内容资产（文案侧 2-4 周，与 A 并行而非前置）
- 补 Soraya 卡（**最高优先**，她是最早发现循环的人）> 卡克特斯卡 > 塔罗 17 张 > 兜底文本库 > 话题 curriculum 方向标签。
- 让第 4/5 步素材齐备、接入一次到位，解决"技术等文案空转"。

### 路径 C · 架构先行四面具齐上（5-8 周）—— 不建议作为首步
- 一次建好四面具接口 + 三层记忆 + 伏笔追踪表。风险：同时开四个面具，出 bug 不知哪个。
- **折中**：做 A 时把接口按四面具契约预留（oracle/dialogue/judgment/crack 四个方法签名 + 状态层字段），只实现化身面具，其余返回兜底。第 5 步不必重写 Bridge。

### 推荐组合（一句话）
> **A 为主路径 + B 并行补素材 + C 的"接口契约与状态层"提前进 A。** 与螺旋计划"接一个面具、测一个面具"的铁律一致。

---

## 六、正式接入前还需拍板的工程问题

1. **API Key 与成本**：桌面游戏客户端里藏 Key 会被提取。要定：开发期直连 + 发布期改本地模型（NobodyWho）或自建代理？单日 token 预算？
2. **延迟策略**：验证 8-21s/条，游戏内不可接受。要定：加载态文案、打字机效果、或"进入对话前预生成话题/第一句"。
3. **记忆与存档**：per-NPC 记忆 + 跨循环保留 + 存档格式（验证侧 JSON 结构可移植）。
4. **收尾验证缺口**：V2/V3 人工评分表还是空白——**这是当前唯一卡住的"验证合格"证据**，建议先补完；V4 午夜审判 / V5 裂缝 / V6 跨天记忆一致性验证也待做。
5. **自由输入的内容安全**：全年龄红线 + Godot 侧敏感词兜底。
6. **"天的错误变 feature"**：AI 偶尔驴唇不对马嘴，设计文档已定调为"天的裂缝"——接入时要写进兜底策略，而不是当 bug 修。

---

## 七、v1 总结

1. **下一步（第 3 步）就做路径 A 的薄切片**，但**绝不裸接 API**——必须带"极简门控 + 挡刀机制 + hints 交叉印证"三件套，否则剧透事故必然发生。
2. **先补 V2/V3 人工评分**（半天工作量），让"验证合格"有真凭实据。
3. **第 3 步就接 `hints_to_other_npcs` → `clues_found` 闭环**，这是全项目性价比最高的一步：验证合格资产、字段已存在、直接服务探秘目标、让对话有了"目标感"。
4. **把"身体记忆/挡刀"当成护城河来设计**，门控是为了服务它而不是压死它。
5. 天命/审判/裂缝三面具按螺旋计划逐个接入，接口现在预留，内容第 4-5 步再补。

---

---

# v2 适配：对「无AI试玩Demo v1.1」数据驱动引擎的调整

> 2026-08-19 重新检查更新后的 Godot 项目。结论：**项目结构与脑暴撰写时已大不相同**——游戏从"硬编码对话骨架"升级为"完整可玩的确定性叙事 Demo（无 AI）"。脑暴的核心方向仍然成立，但**接入方式从"替换硬编码"变为"在确定性引擎上叠加 AI 表现层"**，且多个前提已变化。

## v2.1 架构变更摘要（对照 v1 前提）

新提交 `774bb36 feat: 无AI试玩Demo v1.1——三区域+3 NPC对话+塔罗+租房+午夜塔楼问答+循环结局`：

| 系统 | 现状（无AI版） | 对应文件 |
|------|---------------|---------|
| **对话引擎** | JSON 数据驱动：`dialogues[]` + `conditions`/`priority`/`lines`/`choices`/`effects`/`reply`/`goto`/`end`/`end_effects` | `scripts/npc/npc_base.gd` + `resources/dialogues/{padwin,soraya,cactus_bishop,tower_voice}.json` |
| **条件/效果** | `conditions_met()`（flag/flag_not/clue/clue_not/day_min/time/rented）、`apply_effects()`（set_flag/discover_clue/affection/gold/action） | `scripts/autoload/game_manager.gd` |
| **NPC** | 3 NPC + 塔楼之声（"天"）：索拉雅（intro/confront/awakened/daily）、帕德温（intro/daily，番茄测试）、卡克特斯（intro/daily，勒痕线）、塔楼（午夜"猜猜我是谁"） | `scenes/npc_*.tscn` |
| **线索** | 6 条线索 + `discover_clue()` + toast：帕德温无前世记忆 / 主教勒痕 / 自己烧伤疤 / 索拉雅机械重复 / 森林起源书 / 身份证 | `game_manager.gd` CLUE_NAMES |
| **剧情标记** | flags：met_soraya / soraya_awakened / tomato_test_done / cactus_scar_seen / self_burn_scar / tower_searched 等 | `game_manager.gd` |
| **塔罗** | **22 张大阿尔卡那完整**（每张带 luck + reading），每日清晨抽牌 | `resources/tarot/major_arcana.json` + `tarot_ui.gd` |
| **结局** | 3/22 已做（愚人/女祭司/节制），塔楼回答路由 `ending:*` / `pass_night` | `resources/endings/endings.json` + `ending_ui.gd` |
| **区域** | 三区域切换（广场/树屋区/石巢塔楼），门 + 出生点 | `scripts/game.gd` + `door.gd` |
| **租房** | 树屋租赁系统 | `rental_ui.gd` + `treehouse_door.gd` |
| **测试** | 自动化回归测试（全流程驱动 + 截图） | `scripts/autotest.gd` + `scenes/autotest.tscn` |

## v2.2 适配性评估（v1 脑暴 × 新架构）

| v1 假设/方案 | 新架构现状 | 结论 |
|-------------|-----------|------|
| 对话硬编码 5 句，需"替换为 AI" | 已是 JSON 数据驱动引擎（条件/选项/效果/跳转全确定） | **前提变了**。接入 = 叠加 AI 表现层，不是替换。A5（AI与剧本混合）从"一个方案"升级为**必然架构** |
| "三入口 UI 骨架已在"（预写/沉默/自由输入） | 新版 `dialogue_ui.gd` **已无自由输入框**，只有动态选项按钮 | **需要新增自由输入框 UI**，才能接"暗门" |
| 塔罗只有 5 张，需补 17 张 | **22 张已完整**（带 luck + reading） | B3 每日塔罗任务前提已具备，**不用再补内容** |
| 无塔楼/无结局/无任务 | 塔楼之声（猜猜我是谁）+ 3 结局 + 6 线索 + flags 条件系统**都已存在** | B1/B2/B5 有了落点；结局判定 S10 已有雏形 |
| A1 门控表（从零建） | `conditions`/`flags` 就是现成的门控基础设施 | **复用**：AI 门控 = 白名单 + 注入"当前可说清单"，无需另起炉灶 |
| A3 锚点接管（AI 暂停插作者文本） | 关键节点（intro/confront/awakened/tower）**已经是作者文本** | **已实现**，AI 不需要接管；AI 只负责"作者没写满"的地方 |
| 兜底文本库（从零建，⭐⭐⭐ 高优先） | JSON 对话树就是**天然兜底层**——AI 失败可回落作者文本 | **优先级降级**：不用新建兜底库，回落 JSON 即可（成本大降） |
| `hints_to_other_npcs` → 线索闭环（⭐⭐⭐） | 线索系统已存在，但 AI 字段仍无人消费 | **仍然成立**，是探秘闭环入口 |
| benevolence/engagement/truth_proximity 缺失 | 仍然缺失 | **仍然成立**，C 系列需要加状态层 |
| 记忆存储缺失 | 仍然缺失 | **仍然成立** |
| V2/V3 人工评分空白 | 仍然空白 | **仍然成立**（验证合格的证据缺口） |
| autotest（v1 未提） | 已有自动化回归测试 | **新资产**：AI 接入后可扩展覆盖"AI 成功/AI 回落"双路径 |

## v2.3 核心问题重构：AI 在确定性引擎上"开什么缝"

现在游戏已有一套完整、可玩、作者手写的确定性叙事骨架。AI 的接入不是替换它，而是在**确定性骨架的留白处**叠加表现层。候选缝隙（可组合，从高优先级排起）：

| 缝隙 | 机制 | 与 v1 方案的对应 | 优先级 |
|------|------|----------------|--------|
| **S1 · AI 日常变奏** | 现有 `daily` 分支很薄（3-4 句固定）。AI 按（天 × 好感段 × flags × 已获线索 × 今日塔罗 reading）生成每日新鲜对话，输出 `response_text` + `emotional_shift` + `memory_update` + `hints_to_other_npcs`，映射到现有 `apply_effects`（affection/clues/flags 白名单） | A5 + B1 + B3 + C1 | ⭐⭐⭐ |
| **S2 · 自由输入暗门** | 对话 UI **新增自由输入框**（当前不存在），玩家输入 → AI 以 NPC 人设响应（受门控），命中白名单线索/flag → `discover_clue`/`set_flag`；超纲问题走 C4 挡刀 | A1/A2/C4 | ⭐⭐⭐ |
| **S3 · 塔楼之声 AI 化** | "猜猜我是谁"目前 4 个固定选项 → 增加自由回答通道，AI 判定意图并路由到 22 结局（现有 3 个）或推进 | B2 + B5 | ⭐⭐ |
| **S4 · 天的存在层** | 加 benevolence/engagement 状态，午夜审判回顾、预言应验判定、裂缝借 NPC 之嘴 | C1/C2/C3/C6 | ⭐⭐ |
| **S5 · 塔罗预言注入对话** | 今日 reading 注入对话 prompt，让 NPC 日常回应呼应今日牌 | B3 | ⭐⭐ |

**状态所有权契约（v2 强调，比 v1 更具体）**：AI 永远只输出"表现"——`response_text` / `emotional_shift` / `memory_update` / `hints_to_other_npcs` / 意图标记。**一切数值/flag/线索变更都必须经 Godot 白名单校验**：`emotional_shift` → 校验后 `change_affection()`；`hints` → 命中白名单才 `discover_clue()`；AI 不能直接写 flags。这与现有 `apply_effects` 的设计完全一致，AI 只是"内容生产者"，不是"状态写入者"。

## v2.4 调整后的差距清单（在 v1 基础上增删）

**新增缺口（v1 未覆盖）**：
- AI Bridge 需与**事件驱动对话引擎**对接（`dialogue_line` / `dialogue_choices` / `dialogue_choice_made` / `dialogue_ended` / `game_action` 信号），而不是接硬编码流程。
- AI 输出 → 现有 effects 系统的**映射与白名单**（见 v2.3 契约）。
- **自由输入框 UI 需要从零新增**（v1 误以为已存在）。
- 记忆存储 + 每日对话轮次/时段收束约束（v1 已提，现更明确：JSON 对话天然有结束，AI 日常需要"聊够了"的收束）。

**已解决 / 降级的缺口（新架构已提供）**：
- 剧情锚点接管（A3）→ JSON 引擎确定性实现，AI 不需要接管。
- 塔罗内容补全 → 22 张已齐。
- 任务/线索系统骨架 → clues + flags + conditions 已存在。
- 兜底文本库 → JSON 对话树即天然兜底，降级为"AI 失败回落 JSON"。

## v2.5 调整后的推荐路径

### 路径 A'（原路径 A 调整）—— 在引擎上开缝，第一批 S1 + S2
1. AI Bridge Node：HTTPRequest + 异步 + 超时 + **回落 JSON 兜底**（失败/超时 → 走现有 `daily` 分支，玩家无感知）。
2. **S1 AI 日常变奏**：新增"AI 对话提供者"接入 `npc_base.gd` 的 daily 入口——AI 生成当日对话，`emotional_shift`→`change_affection`、`hints`→白名单 `discover_clue`。
3. **S2 自由输入暗门**：`dialogue_ui.gd` 新增自由输入框（复用 v1 曾有过的 LineEdit 设计），输入走 AI 响应 + 门控挡刀。
4. 门控：复用现有 flags/conditions，prompt 注入"当前可说/不可说清单"（极简版 ~10 条硬事实）。
5. 记忆：per-NPC 本地 JSON（移植验证侧 `memories/padwin.json` 结构），Godot 持有。
6. 契约：`dialogue_schema.json` 原样复用，`hints_to_other_npcs` 升必填。
7. 测试：扩展 `autotest.gd`，覆盖"AI 成功 / AI 回落"双路径。

### 路径 B'（原路径 B 调整）—— 内容资产
- Soraya / 卡克特斯 **AI 角色卡**（用于 S1/S2 的 AI 表现，现有 JSON 是"无 AI 剧本"不是"角色卡"）。
- 结局 3 → 22 补全（塔罗已齐，结局才 3 个）。
- AI daily 话题方向标签（curriculum）。
- **不再需要补塔罗内容**（v1 误判）。

### 路径 C'（接口契约预留）—— 不变
- 四面具接口（oracle/dialogue/judgment/crack）+ 状态层字段（benevolence/engagement/truth_proximity/prophecy_resistance）进 GameManager，只实现化身面具。

## v2.6 关键结论（v2 新增）

1. **新架构让 AI 接入更安全、更便宜**：确定性骨架 + JSON 作者文本兜底，即使 AI 全部失败，游戏仍完整可玩。**最大的接入成本顾虑（兜底库）被架构本身消解了。**
2. **核心风险从"AI 剧透击穿结局"降级为**：(a) AI 表现与手写剧情风格不一致；(b) AI 输出与现有 effects 系统衔接出错；(c) 新增自由输入引入的内容安全。
3. **最高性价比的接入缝是 S1（AI 日常变奏）+ S2（自由输入暗门）**——都在现有引擎上插，都能复用验证合格的角色卡/管线/JSON 兜底。
4. **AI 永远是"内容生产者"，不是"状态写入者"**——一切数值/flag/线索变更经 Godot 白名单校验，与现有 `apply_effects` 哲学一致。
5. v1 的"三件套（门控 + 挡刀 + hints 闭环）"仍然成立，但落地方式变了：门控复用 flags、挡刀用于自由输入、hints 闭环接入现成线索系统。

---

> 下一步：请策划从 v2 的方向中选择要推进的缝隙组合（推荐 **S1 + S2 起步**），我们据此制定具体开发计划并动代码。
