# 场景重构 · 工程文档 v0.2

> **状态：待审核。** 本版在 v0.1 基础上，依据「后期复刻星露谷 ~80% 经营玩法」这一长期意图重写，核心是把「场景壳」与「玩法系统」彻底分层：本文档只冻结**壳的 ABI（接口契约）**，玩法一律标为「非目标 / 另立文档」。
>
> **仓库/分支：** 代码 `D:\AIGameBuild`（main，Godot 工程在 `成为艾莉森的一天/`）；文档 `D:\AIGameBuild-DesignDocs`（docs/design）。本文档内所有路径相对 Godot 工程根目录。
>
> **引擎约束：** Godot 4.6 · 2D · GL Compatibility。游戏代码改动落 main / `feat/`；文档落 docs/design。

---

## 0. 意图澄清（先于一切）

### 0.1 一句话意图

**现在不写星露谷，现在只搭一个「壳」，让星露谷将来能长进去而不用拆壳。**

长期目标（后期有条件时）：复刻星露谷约 80% 的经营玩法（种地、工具、砍伐、采集、经济、NPC 作息、季节等）。

短期现实（当前没时间做拓展）：只把地形配好，结构不冗余、不崩、好维护、autotile 铺图顺手。

### 0.2 核心立论：80% 星露谷 ≈ 三个「钩子」

星露谷那 80% 玩法，**绝大部分是玩法系统（脚本层），不是场景**——作物生长、工具（斧/镐/锄/水壶/镰刀）、经济、NPC 作息、季节，全在脚本。场景壳（即本次「场景重构工程」）只需要为这些玩法预留**三个钩子**：

| 钩子 | 一句话 | 冻结成什么 |
|---|---|---|
| **① 地形语义** | 每格地面知道「是什么地 + 能不能走」 | 地面瓦片 = 视觉 + 可走性；提供 `terrain_type_at` 语义查询 |
| **② 可交互世界物体** | 树/石/树桩/杂草 = 独立可交互障碍，砍了 `free()` 底下自动通 | `Interactable` 组件契约（§2.3） |
| **③ 假高度 + 分层遮挡** | 高地 = 平面 + 悬崖碰撞 + 台阶缺口；三层 + y_sort | 节点骨架 `Ground/Props/Overlay`（§2.1）+ 假高度契约（§2.4） |

**只要现在把这三个钩子的契约冻结，将来的种地/砍树/盖房就全是「往钩子里填内容」，不碰壳。** 这就是「不冗余但能长大」：**冻结的是契约，不是玩法。**

### 0.3 目标 / 非目标

| 目标（本文档范围） | 非目标（明确不做，防止主线漂移） |
|---|---|
| 冻结场景壳的 ABI 契约（节点名 / 碰撞层 / 地形语义 / 可交互物 / 假高度 / autotile 约定） | 任何玩法系统（种地/工具/经济/NPC 作息/季节）→ 走《玩法扩展》文档 |
| 铺现有三区地面（autotile，一区一 .tres） | 室内场景（树屋内部/艾莉森房间另立文档） |
| 碰撞层命名 + 区根 y_sort 基建 | 拆三区成 levels/*.tscn（已执行 ✅ M1/M2a，D7 拍板纳入本轮，Pilot 先行） |
| 建立「契约束」保证换壳后行为零回归 | 改 Player / UI CanvasLayer 根结构 |
| 为「未来第 4 区（农场）」预留加区契约 | 实建农场区 / 画农场内容 |

### 0.4 本版相对 v0.1 的变更点

| v0.1 原方案 | v0.2 结论 |
|---|---|
| 把三区拆成 levels/*.tscn 是主线步骤 | 先「降级」后**恢复**：D7 最终拍板「先拆再铺 + Pilot 先行」，拆 .tscn 重新纳入本轮（§3） |
| 地形只有 `is_grass_tile` 一个语义 | **泛化**为 `terrain_type_at`（§2.2），`is_grass_tile` 成为其特例 |
| 可走性靠 Props 的 StaticBody2D 挡 | **改判**：可走性烘焙进地面瓦片碰撞（已拍板，§2.2） |
| 无 y_sort / 碰撞层 / 假高度 / 可交互物契约 | **新增**四个契约（§2.1/2.2/2.3/2.4/2.5） |

### 0.5 相关文档

| 文档 | 关系 |
|---|---|
| `玩法扩展_设计与工程实现_v0.1.md` | 经营玩法本体，**依赖本壳铺好的地面 + 三个钩子**；本壳不动其内容 |
| `美术音乐工作文档.md` / `美工音乐资产清单.md` | 素材规格；障碍地面（水体/密林/悬崖）素材缺口见 §6.3 |
| `艾莉森的房间_设计与工程实现_v0.1.md` | 室内另立，树屋门是外景→室内的跳板 |
| 本仓库 `CLAUDE.md` | 分支 / 提交规范，硬性遵守 |

---

## 1. 现状盘点（已核实事实，写契约的基线）

### 1.1 场景拓扑（main.tscn）

三外景都在 `Areas` 下，是**三个平铺 Node2D**（不是实例化子场景）。三个 Ground 目前共用一份**内嵌 TileSet**（`TileSet_y6deb`，含 terrain_set_0 石子路/土路/草地/小石子路 + terrain_set_1 tower groud1/groud2/step/wall），Plaza 的 Ground 已有烘焙好的 `tile_map_data`。

```
Areas/
├─ Plaza            (Node2D)  Ground/Props/PlayerSpawn/SpawnFromEast/West/DoorEast/DoorWest/Mushrooms/Berries/NoticeBoard/Soraya
├─ TreehouseDistrict(Node2D)  Ground/Props/SpawnFromPlaza/DoorWest/DoorOak/Cedar/Willow/Padwin/CactusBishop
└─ StoneNestTower   (Node2D)  Ground/Props/SpawnFromPlaza/DoorSouth/TianAltar
```

门拓扑 / NPC 归属 / 契约束子节点名**原样沿用 v0.1 §1.1 与 §2**（不改）。区名 ↔ `to_snake_case()` 映射冻结：`Plaza→plaza`、`TreehouseDistrict→treehouse_district`、`StoneNestTower→stone_nest_tower`。

### 1.2 地形与可走性现状（关键）

- `scripts/systems/scene_layout.gd`：`TILE_SIZE=32`、`WORLD_W=40`、`WORLD_H=24`（每区 1280×768）。
- 地形语义常量：`TERRAIN_STONE=0 / TERRAIN_DIRT=1 / TERRAIN_GRASS=2`，全在 `terrain_set 0`，`TERRAIN_MODE_MATCH_CORNERS_AND_SIDES`。
- 判定草坪：`is_grass_tile(ground, world_pos)` 查 `get_cell_tile_data(cell).terrain == TERRAIN_GRASS`（[scene_layout.gd:192](成为艾莉森的一天/scripts/systems/scene_layout.gd#L192)）。
- **可走性现状：地面瓦片零碰撞**。玩家 `CharacterBody2D`（[player_controller.gd](成为艾莉森的一天/scripts/player/player_controller.gd)）`move_and_slide()` 自由移动，唯一阻挡来自 Props 里烘焙的 `StaticBody2D`（圆碰撞体，[scene_layout.gd:286](成为艾莉森的一天/scripts/systems/scene_layout.gd#L286)）。
- 玩家移动无重力/无分层，靠相机 rect clamp 收边（[player_controller.gd:48](成为艾莉森的一天/scripts/player/player_controller.gd#L48)）。

### 1.3 三个待接 .tres（本轮主角）

| .tres | 语义 | 目标区 | 是否需碰撞（本轮） |
|---|---|---|---|
| `assets/tilesets/tower_tileset/tower_groud.tres` | 塔楼石板地面 | StoneNestTower | 否（可走地面） |
| `assets/tilesets/tile_forest/wooden_floor.tres` | 树屋木地板 | 艾莉森的房间（未来新区） | 否（可走地面） |
| `assets/tilesets/tile_forest/wooded_path.tres` | 草地小径 | 森林（未来新区）的可走小径 | 否（可走地面） |

> 三者均 `type="TileSet"`、`terrain_set 0`、`MATCH_CORNERS_AND_SIDES`、peering 真实接缝，**已实锤可配 autotile**。它们都是「可走地面」，所以本轮无需配碰撞多边形——碰撞多边形留给未来的障碍瓦片集（§6.3）。

### 1.4 项目配置（project.godot）现状

- **Autoload（7 个）**：`EventBus / GameManager / Inventory / TimeManager / AIBridge / QuestManager / DialogueLog`。
- **主场景**：`run/main_scene = res://scenes/ui/start_menu.tscn`（注意：不是 `scenes/main.tscn`；`scenes/main.tscn` 是游戏主场景，由 start_menu 过渡进入）。
- **输入**：`ui_accept`(E/Enter/Space)、`advance_time`(T)、`move_*(WASD/方向键)`、`open_inventory`(I)、`ui_cancel`(Esc)。
- **碰撞层**：`project.godot` **无任何 `layer_names` 配置**，全部物理对象默认 layer 1 / mask 1 → 玩家与所有东西互相碰撞。**这是 §2.5 契约要补的基建。**
- **无 y_sort / z_index**：全项目未用（靠子节点顺序遮挡）。

---

## 2. 冻结契约束（壳的 ABI，本文档的正文）

> **为什么要有这张表：** 行为层（game.gd / collectible.gd / door.gd / AI 层）靠**字符串路径 + 节点名 + 地形 id**工作。只要契约束冻结，之后「拆 .tscn」「加第 4 区」「换瓦片集」都是填内容，一行行为代码不用改。这就是壳的 ABI。

### 2.1 契约 ①：每区节点骨架（冻结名）

每张外景（无论现在平铺在 main.tscn，还是将来拆成 levels/*.tscn 实例化）根节点 = `Node2D`，名 = 区名，`y_sort_enabled = true`。

| 节点路径（相对区根） | 类型 | 必需 | 说明 | 谁读它 |
|---|---|---|---|---|
| `(根)` | Node2D，名=区名，`y_sort_enabled=true` | 必 | 区根 | game.gd `switch_area` |
| `Ground` | TileMapLayer | 必 | 地面视觉 + 可走性碰撞，`tile_set` 指向该区 .tres | `terrain_type_at`、`_random_grass_pos` |
| `Props` | Node2D | 必 | 可交互障碍物（树/石/家具），子项 = StaticBody2D | 物理阻挡 |
| `Overlay` | TileMapLayer | 预留 | 画在玩家之上的：树冠/屋顶/高处遮挡（本轮可空） | 未来遮挡 |
| `*Spawn*` | Marker2D | 按区 | 出生锚 | game.gd `switch_area` |
| `DoorXxx` | Area2D + door.gd | 按区 | 场景门 `target_area/target_spawn` | door → EventBus |
| NPC 实例 | PackedScene | 按区 | 名与 `npc_id` 原样；可选 `PatrolPath` 子节点（§2.8） | AI/对话层 |
| 采集池 | Node2D | 按区 | `Mushrooms/Berries`（子项 Area2D 组 `collectible`） | game.gd `_refresh_collectibles` |

**维护规则**：允许在区内部增删「内容」节点；禁止改/删契约束节点名，否则必须同步改 game.gd 并回写本表。

### 2.2 契约 ②：地形语义 + 可走性（两项，勿混）

**这是最容易搞混的一对，必须分开冻结：**

| | 移动阻挡（可走性） | 地形语义（类型查询） |
|---|---|---|
| 谁负责 | **瓦片的物理碰撞**（障碍瓦片才有碰撞多边形） | **`terrain_id`**（该区 tileset 的 terrain） |
| 谁消费 | 物理引擎自动挡玩家（`move_and_slide`） | 代码查询：刷新/种地/放物/脚步声等 |
| 本轮现状 | 三区全是可走地面 → 无碰撞 | 已有 `is_grass_tile`，需泛化 |

**冻结规则：**
1. **可走性 = 瓦片碰撞**：可走地面瓦片**不配**碰撞多边形；障碍瓦片（水体/密林/悬崖）**配**碰撞多边形。玩家阻挡交给物理引擎，不在代码里手写「这格能不能走」。
2. **地形语义查询 API（冻结签名，新增）**：
   ```gdscript
   ## scripts/systems/terrain_query.gd（新增，静态，冻结签名）
   static func terrain_type_at(ground: TileMapLayer, world_pos: Vector2) -> int:
       # 返回该格 terrain_id；空/无地形返回 -1。取代并吸收 is_grass_tile。
   ```
   `is_grass_tile` 保留为薄封装（`return terrain_type_at(...) == TERRAIN_GRASS`），**不破坏现有 game.gd 调用**。
3. **terrain_id 命名约定**：每区 tileset 的 terrain 必须登记进一个全局语义表（常量），如 `TERRAIN_GRASS/STONE/DIRT/WATER/TILLED/CLIFF`。语义 id 跨区含义一致（id 2 永远是草），但具体数值由各 .tres 定义——**语义用名字常量，不用魔法数字**。

### 2.3 契约 ③：可交互世界物体（Interactable）

> 树 / 岩石 / 树桩 / 杂草 / 未来农作物，统一模型，**绝不烘焙进地面瓦片**（否则「砍一棵特定的树」要改那一格瓦片，牵扯视觉+碰撞耦合，就是返工陷阱）。

**冻结模型：**
```
可交互物体 = StaticBody2D（碰撞层 world）
             + Interactable 组件（新脚本 scripts/objects/interactable.gd）
                 @export tool_type: String   # 砍伐/挖掘/清理/浇水/收获…
                 @export durability: int       # 需敲几下
                 @export yields: String        # 掉落 item_id（可空）
             + 视觉 Sprite2D（挂 Overlay 用 y_sort 处理树冠）
```

**生命周期：** 交互成功（耐久归零）→ `queue_free()` → 碰撞体消失 → 底下那块「本就可走」的地自动暴露。砍树/挖石 = 纯场景运行时行为，零瓦片改动。

> 本轮**不实现**砍伐逻辑（那是玩法），只冻结「可交互物 = 独立 StaticBody2D + Interactable 组件」这个结构，并把现有 cainos 树/岩石从「装饰 Sprite」逐步迁到该结构（§4 内不强制，作为迁移方向记录）。

### 2.4 契约 ④：假高度（悬崖 + 台阶缺口）

**星露谷真相：没有真 Z 轴。** 冻结如下：

1. **高地 = 平面**：高地块与低地块都是平铺瓦片，不引入重力/真分层。
2. **悬崖边缘 = 碰撞**：高地块朝外的边缘铺「悬崖瓦片」，配碰撞多边形挡玩家。
3. **台阶/坡 = 唯一缺口**：悬崖墙上留一格可走通道（台阶瓦片，无碰撞），玩家只能从这里上下。
4. **视觉高度 = y_sort + 配色**：高地块地面/边缘比低地块更亮或更暗，靠 y_sort 让角色在「高/低」之间正确遮挡。

**本轮只冻结契约，不画高度内容。** 塔楼的墙/台阶瓦片（`tower_wall_tileset.png` / `tower_step_tileset.png`）**素材已到位**，留给本里程碑（M4）实装时连接——本轮出于「先定玩家能否走上高台，再定墙/阶铺法与碰撞层」之考量**不铺**，避免赌错设计返工；其余悬崖/台阶素材缺口归 §6.2。

### 2.5 契约 ⑤：碰撞层命名（需写入 project.godot）

> 现状无任何命名层，全挤 layer 1。这是「不崩 + 可维护」的最低基建，成本极低。

**冻结（写进 project.godot `[physics]`）：**

| 层 | 名 | 承载 |
|---|---|---|
| 1 | `world` | 地形障碍瓦片碰撞 + Props 障碍 StaticBody2D（玩家 mask 含 1） |
| 2 | `player` | 玩家 CharacterBody2D |
| 3 | `npc` | NPC 身体（玩家默认不与其物理碰撞） |

**默认 mask 约定**：玩家 `collision_mask = 1`（只撞 world）；NPC `collision_layer=3`，`collision_mask=1`（撞 world 不撞玩家）。Area2D（门/采集物）用 mask 检测 player 层，不参与碰撞。

> 未来玩法需要「区分可砍障碍 vs 永久墙」时，**用 Interactable 组件区分，不加碰撞层**——碰撞层只表达「谁撞谁」，不表达「能不能砍」。

### 2.6 契约 ⑥：加区契约（为未来第 4 区「农场」预留）

**新区 = 新增一个满足 §2.1 骨架的 Area 子树 + 注册 area_id**。满足下面三条即自动接入，无需改 game.gd 逻辑：

1. 根节点名 → `to_snake_case()` 后即 `area_id`（如新区 `Farm` → `farm`）。
2. 骨架含 `Ground`（自己的 .tres）+ 出生 Marker + 门（`target_area` 指向既有区）。
3. 采集池等新池名需在 game.gd `_refresh_collectibles` 的池名列表注册（沿用 v0.1 §2.3）。

未来农场 = 多一个 Area 节点，走此契约，壳不动。

### 2.7 契约 ⑦：autotile 铺图约定（让「画图顺手」可复现）

1. **一区一 .tres**：每区 Ground 指向自己的 TileSet 资源（可共享，但默认一区一个，便于单独编辑）。
2. `texture_region_size = 32×32`，`terrain_set 0`，`MATCH_CORNERS_AND_SIDES`。
3. 透明空瓦片**跳过**（不 `create_tile`），避免误刷。
4. 每个地形（路/草/水/悬崖…）用独立 `terrain_id` + 名字，peering bit 表达真实接缝（连满 8 角的中心填充格必须有）。
5. 铺图用编辑器**地形刷** `set_cells_terrain_connect` 或手刷；烘焙工具只作一次性迁移助手，不覆盖手摆（沿用 v0.1 §3.3）。

### 2.8 契约 ⑧：NPC 活动（巡逻）——本轮只冻结结构

> 现状：`NPCBase` 已是 `CharacterBody2D`，四向 walk 动画帧全就位，有碰撞体 + `InteractZone`，**缺移动逻辑**。动态化不返工，只需补逻辑 + 定路径。

**冻结模型：**
1. **NPC 坐标 = 出生点 + 可选活动路径**，不再是一个固定点。路径作为 NPC 场景（`npc_xxx.tscn`）的子节点：
   - `PatrolPath`（Path2D）：固定路径点环，适合巡逻/采蘑菇（索拉雅、帕德温）。
   - 或 `PatrolArea`（Rect2 常量）：区域内随机游走，适合打扫/徘徊（主教）。
2. **碰撞层**：NPC = layer 3（`npc`），mask 只含 world（1）；玩家 mask 不含 npc → **NPC 巡逻永不卡玩家**。
3. **移动逻辑（未来实现）**：NPCBase `_physics_process` 沿路径 `move_and_slide`，到点停留随机秒循环；对话中暂停（复用 `_movement_locked`）。本轮不写。
4. **放大重排**（D6=B）：放大后重排「出生点 + 路径点」相对坐标，结构不变零返工。

**本轮落地**：仅第 2 条（碰撞层）。其余（路径定义、移动逻辑）归未来玩法。

---

## 3. 本轮施工范围（已拍板：先拆再铺，Pilot 先行）

> 依据 owner 拍板（D7）：**拆开（实例化 levels/*.tscn）纳入本轮，先拆再铺，Pilot 先行（先拆 Plaza）**。不写玩法、不动行为层。

### 3.1 基建（零风险，第一刀）

1. `project.godot` 写入 §2.5 碰撞层命名（`world/player/npc`）。
2. 三区根 `Node2D` 开 `y_sort_enabled = true`。
3. 新增 `scripts/systems/terrain_query.gd`（§2.2 冻结签名），`is_grass_tile` 改薄封装。

### 3.2 Pilot：拆 Plaza（先拆再铺的第一刀）

1. 把 `Areas/Plaza` 整个子树迁到 `scenes/levels/plaza.tscn`（根名 `Plaza`，含 Ground/Props/Spawn/Door/采集池/NPC 及坐标原样）。
2. main.tscn：`Areas/Plaza` 改为 `instance = plaza.tscn`，实例节点名保持 `Plaza`。
3. 回归验收（§3.5）→ 全绿才算「换壳零回归」。

### 3.3 复制拆到 TreehouseDistrict、StoneNestTower

- 各自迁到 `scenes/levels/treehouse_district.tscn` / `stone_nest_tower.tscn`，回归。
- **StoneNestTower 换瓦片**：Ground 的 `tile_set` 从内嵌 `TileSet_y6deb` 换成 `tower_groud.tres`（§6.1）。施工陷阱：先清 `tile_map_data` 再换源（旧坐标按旧源编码，直接换源无效）。

### 3.4 铺地面（autotile）

- Plaza / TreehouseDistrict：`forest_plaza.png` 已就位，**不换**。
- StoneNestTower：`tower_groud.tres`（石板）。
- 三区都是可走地面，无碰撞；障碍瓦片归未来（§6.2）。
- `wooded_path.tres`（森林）、`wooden_floor.tres`（艾莉森房间）**本轮不接**。

### 3.5 回归验证（零回归 = 完成定义）

- 三区门往返、spawn 正确、无连锁触发（door 冷却）。
- 蘑菇随机刷、浆果当日刷新（依赖 `is_grass_tile` → `terrain_type_at` 薄封装，行为不变）。
- 三 NPC 对话 + AI 上下文 `current_area` 正确。
- 四时段色调、午夜流程正常。
- 玩家走全图无穿模、无出界、采集不刷上路。

---

## 4. 鲁棒性规则（「不能运行崩掉」的落地）

> 壳的 ABI 只要被遵循，就不会因「改名/缺节点」崩。以下规则写进施工纪律：

1. **跨场景访问一律 `get_node_or_null` + 判空守卫**，缺失时 graceful 降级（跳过/兜底），绝不硬 `get_node` 崩。game.gd 已有此先例（[game.gd:373](成为艾莉森的一天/scripts/game.gd#L373)），全项目对齐。
2. **契约束节点缺失 ≠ 崩溃**：`Ground` 缺失则 `terrain_type_at` 返回 -1、刷新走兜底点；`Props` 缺失则无阻挡（可走，不崩）。
3. **语义用名字常量，不用魔法数字**：`TERRAIN_*`、碰撞层名、节点名都走常量/冻结名，避免「改一个数字忘了另一处」。
4. **物理碰撞只在帧末改**：开关碰撞体用 `set_deferred`（game.gd 已有先例），避免物理回调内改碰撞报错。

---

## 5. 未来阶段（本轮不做，契约保证无需返工）

| 阶段 | 内容 | 依赖契约 | 何时 |
|---|---|---|---|
| 加新区（森林 + 艾莉森的房间） | 森林=采集/开辟玩法；艾莉森房间=室内 | §2.6 加区契约 | 后期 |
| 障碍地面 | 水体/森林地被/悬崖瓦片集（配碰撞） | §2.2 可走性契约 | 后期，素材 §6.2 |
| 假高度实装 | 高地 + 悬崖 + 台阶 | §2.4 假高度契约 | 后期 |
| 可交互物实装 | 砍树/挖石逻辑 | §2.3 Interactable 契约 | 玩法层，走《玩法扩展》 |
| 经营玩法（采集/开辟/种地/工具/NPC 作息） | 全部玩法 | 三钩子 + 玩法文档 | 《玩法扩展》 |

---

## 6. 素材映射与缺口

### 6.1 瓦片集 → 区映射

| 外景 | 主地面瓦片源 | 状态 |
|---|---|---|
| Plaza（森林广场） | `forest_plaza.png`（内嵌 TileSet 地形集 0，已就位） | 不换 |
| TreehouseDistrict（树屋区） | 同 Plaza：`forest_plaza.png`（草地 + 路） | 不换 |
| StoneNestTower（石巢塔楼） | `tower_groud.tres`（石板） | 本轮接 |
| （未来）森林（新区，采集+开辟玩法） | `wooded_path.tres`（可走小径）+ 森林地被（障碍，素材待补） | 后期建区时接 |
| （未来）艾莉森的房间（新区） | `wooden_floor.tres`（木地板） | 后期建区时接 |

### 6.2 障碍地面素材缺口（可走性契约启用时需）

| 用途 | 缺什么 |
|---|---|
| 水体（不可走） | 水面 autotile（含碰撞） |
| 密林地被（森林限路） | 森林地被 autotile（含碰撞），与 `wooded_path` 路径形成「路可走/地被不可走」 |
| 悬崖/台阶（假高度） | 悬崖边缘瓦 + 台阶瓦（悬崖配碰撞，台阶无碰撞） |

> 参考 `美工音乐资产清单.md` 优先级；占位不阻塞开发。
>
> **塔楼墙/台阶非缺口**：`tower_wall_tileset.png`（立面墙+垂挂链条/火把）、`tower_step_tileset.png`（多级上坡+转角台阶）已提供，与 `tower_groud.tres`（石板顶面）同属塔楼高台，归假高度里程碑（M4）实装时连接，不属本轮铺地面范围。

### 6.3 cainos 遗留退场（沿用 v0.1 §6.4）

蘑菇图标、门/石柱/拱门/岩石/木桶/花瓶等 cainos 引用，替换一张删一张。**树/岩石优先迁到 §2.3 Interactable 结构**（为砍伐玩法铺路）。

**门视觉替换（D6 已拍板）**：三区场景门（`DoorXxx` 的视觉 Sprite）从 cainos 门换成 `assets/tilesets/tile_forest/tileset_forest.png` 图集内的树屋。（文件名已由 `tileeset_forest .png` 规范化为 `tileset_forest.png`。）

---

## 7. 回归与测试

沿用 v0.1 §7 清单，新增一条：

- **新增契约回归点**：`terrain_type_at` 薄封装后，`is_grass_tile` 行为不变（蘑菇仍只刷草坪）；三区 Ground 换 .tres 后 `_random_grass_pos` 仍能采样到草坪（否则走兜底点也不崩，但需人工确认没退化成永远兜底）。

---

## 8. 决策项

### 8.1 已拍板（已定不再议）

| # | 决策 | 结论 |
|---|---|---|
| D1 | Plaza 主地面瓦片源 | **`forest_plaza.png`**（已配好，即内嵌 TileSet 地形集 0） |
| D2 | 放大目标尺寸 | **约两倍**（→ 2560×1600 量级） |
| D3 | 运行时程序铺地/烘焙去留 | **A**：迁移期保留、最终移除 |
| D4 | Props 落地方式 | **A**：编辑器手摆为主，程序常量只作一次性迁移 |
| D5 | 各瓦片源 | 树屋区=**同 Plaza（forest_plaza）**；艾莉森的房间=**wooden_floor**；塔楼=**tower_groud** |
| D6 | NPC/门坐标 | **B**：随放大重排；门视觉换 `tileset_forest.png` 树屋；NPC 动态化走 §2.8 路径点巡逻 |
| D7 | 拆开（实例化） | **先拆再铺 + Pilot 先行**（先拆 Plaza 试点，零回归后复制到树屋/塔楼），纳入本轮 §3 |
| D8 | 地面可走性来源 | **烘焙进地面瓦片碰撞**（路无碰撞/障碍有碰撞），不再靠 Props 挡 |
| D9 | 后期新区 | **森林**（采集+开辟玩法）+ **艾莉森的房间**（室内）；现在只冻结加区契约（§2.6） |
| D10 | 高度模型 | **假高度**：平面 + 悬崖碰撞 + 台阶缺口（§2.4） |
| D11 | 本轮交付范围 | **只冻结契约 + 铺三区地面**（§3），不拆三区、不写玩法 |

### 8.2 待拍板

（无——D1–D11 已全部拍板。）

---

## 9. 里程碑与完成定义

| 里程碑 | 内容 | 完成定义 |
|---|---|---|
| M0 | 契约冻结 + 基建 | ✅ 碰撞层命名、y_sort、terrain_query.gd 落库（a3a5c3e） |
| M1 | Pilot 拆 Plaza | ✅ 拆成 plaza.tscn，§3.5 回归全绿（换壳零回归，ec48af8） |
| M2a | 复制拆树屋/塔楼 | ✅ 两区实例化（285e388） |
| M2b | 塔楼换瓦片 + 铺地面 | ✅ 地面换 tower_groud.tres、autotile 刷好、内嵌旧 TileSet/tile_map_data 死重移除（2e13489） |
| M3（未来） | 加新区（森林 + 艾莉森的房间） | 走 §2.6 加区契约 |
| M4（未来） | 障碍地面 + 假高度 | 素材 §6.2 到位后；**塔楼墙/台阶（tower_wall / tower_step）素材已到位，归此里程碑实装，本轮不铺** |
| M5（玩法） | 采集/开辟/经营玩法 | 《玩法扩展》文档，不在此列 |

---

## 10. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1 | 2026-09-03 | 首版，待审核。基于 main.tscn（322 节点）、scene_layout.gd、门导出值盘点。 |
| v0.2 | 2026-09-05 | 依据「后期复刻星露谷 ~80%」长期意图重写：核心立论「80% 星露谷 ≈ 三钩子」；冻结 7 项壳 ABI 契约（节点骨架/地形语义/可交互物/假高度/碰撞层/加区/autotile）；「拆 .tscn」降级为未来可选；新增已拍板 D8–D11；本轮范围收窄为「只冻结契约 + 铺三区地面」。 |
| v0.2.1 | 2026-09-06 | 依据执行结果回写：M2 拆分为 M2a/M2b 并标记已执行（含提交号）；塔楼墙/台阶（`tower_wall` / `tower_step`）素材已到位，拍板**归入假高度里程碑 M4，本轮不铺**（先定能否走上高台再定铺法与碰撞层）；校准「拆 .tscn = 未来不做」的陈旧表述（已执行）；塔楼 Ground 换 `tower_groud.tres` 并移除内嵌旧 TileSet/tile_map_data 死重。 |
