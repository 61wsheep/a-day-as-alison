# 文案录入预览稿 · 文案同学交付 v0.1（待审）

> **来源：** `D:\AIGameBuild\《成为艾莉森的一天》文案.md`（文案同学 2026-09-10 交付，共九章）
> **规则依据：**《文案资产清单》§二（交付清单）/§三（回落层三层）/§五.5（**Markdown 由 owner 统一录入工程，文案同学不碰 JSON/Git**）
> **双仓规则：** 设计文档 → `docs/design`；玩家文本 → `main`
> **本次已定：** ① 世界观按策划案归一化（保留文学内容，纠正"中世纪/禁现代物件"）② 先出本预览稿再写入

---

## 一、世界观归一化清单（逐条）

| # | 文案同学原文 | 改后 | 依据 |
|---|---|---|---|
| N1 | "贝戈尼亚是一个存在于时间之外的**中世纪**森林小镇" | "贝戈尼亚是一片存在于时间之外的森林小镇" + 补"有现代科技，但装潢与造物保持森林/原木形态" | 策划案 L15「**现代化**的森林，拥有现代科技，装潢保持原木风格」 |
| N2 | 空间表：树屋区/广场与集市/石巢塔楼/药园/公园 | 区域补 **大学**（大学是"天是什么"的研究地，S3 关键场景） | 策划案 L15/L85/L99/L145 |
| N3 | 禁区："禁：现代物件（电梯、电车、手机、路灯、身份证等）。全部设施保持原木风格。" | 改为"**禁：把现代科技写成冷峻的金属科幻**。电梯、电车、身份证、大学等现代元素都在，但一律以森林/原木形态呈现（藤蔓牵引的电车与藤笼电梯），是宫崎骏式魔法蒸汽朋克。" | 策划案 L15/L45；`ai/tian_system.txt` 已校准口径 |
| N4 | 索拉雅规则介绍词区域列表 | 补"大学" | 同 N2 |
| N5 | 开场叙事包（苔→绳/看/撞） | **不改**——其描述（树屋大楼/绳桥/提灯）与原木风格兼容，且未出现违禁表述；缺席≠矛盾 | — |
| N6 | 身份禁忌表、四条规则表、天=女巫魔力汇集真相 | **不改**——与策划案一致 | 策划案 L15；`tower_voice.json` 现有"女巫们汇集"选项互证 |

> 归一化的原则：**只动与世界口径冲突的表述，不动文学内容**。文案同学的语感（苔/绳/折痕/笑眼）全部保留。

---

## 二、去向映射表（章节 → 文件 → 格式 → 动作）

| 章节 | 去向 | 目标位置 | 格式 | 动作 |
|---|---|---|---|---|
| 一 开场叙事包（完整版 + 循环精简版） | 游戏 `main` | 无现成槽位 → **新建** `resources/data/opening.json` | JSON 旁白序列 | 新建 |
| 二 世界观设定（归一化后） | 设计 `docs/design` | **新建**《世界观精简设定_文案同学_v1.md》 | md | 新建 |
| 三 人物 ×4 | 设计 `docs/design` | 同上文档；与 `ai/npc_*.txt` 逐条对照 | md | 比对 |
| 四 索拉雅初见与引导词 | 游戏 `main` | `resources/dialogues/soraya.json` → `intro` / `confront` | JSON dialogue | **覆盖改写** |
| 五 情绪节点 ×3 | 游戏 `main` | `padwin.json` / `soraya.json` / `cactus_bishop.json` + 线索入口 | JSON dialogue | 覆盖/新增 |
| 六 塔楼之声 | 游戏 `main` | `resources/dialogues/tower_voice.json`（扩 choices）+ 判定规则入 prompt | JSON | 扩写 |
| 七 结局 ×2（恋人/高塔） | 游戏 `main` | `resources/endings/endings.json`（现有 3 条后） | JSON | 追加 |
| 八 物品与线索 | 游戏 `main` | `resources/data/items.json` 的 `desc` + **新建**线索描述槽 | JSON | **覆盖 desc** / 新增 |
| 九 UI 文案 | 游戏 `main` | 现无集中槽位 → **新建** `resources/data/ui_text.json` | JSON | 见 §四.3 |

---

## 三、覆盖对照（写入即覆盖，请重点看）

| 目标 | 现有内容 | 将被替换为 | 风险 |
|---|---|---|---|
| `soraya.json` → `intro` | 13 行现有初见（含"是不是女巫"、规则介绍） | 文案同学 §四 初见 + 引导词（更完整） | 中：现有 intro 已有分支/effects，需**合并而非纯替换** |
| `soraya.json` → `confront` | 6 行（day_min2 逼问） | 文案同学 §五 逼问索拉雅（防御→崩溃→觉醒） | 中：需保留 `soraya_awakened` flag 触发 |
| `items.json` → `desc` | 4 条简短 flavor（蘑菇/浆果/月桂叶/药瓶） | 文案同学 §八 蘑菇/浆果/草药瓶 flavor | 低：纯文本替换 |
| `endings.json` | 3 条（愚人/女祭司/节制） | **追加** 恋人/高塔（**不覆盖**） | 低：纯新增 |
| `ai/npc_*.txt` | 现有角色卡（Padwin 为 v0.2 定稿） | **不改**（人物段仅作设定文档） | 见 §四.1 |

---

## 四、三个待你确认点

### 1. 帕德温人设口径（年龄 / 身份）
- 文案同学：**约三十岁**、**中世纪贵族骑士**（未提"守卫团团长"）
- 我们的 v0.2 卡：**二十多岁、不到三十**、**守卫团团长**（你上周审定）
- **其余全一致**：举报动机、"我对你做了什么"、番茄过敏、肢体边界清单几乎逐条相同
- **已定（2026-09-10，owner）：以 v0.2 卡为准**。文案同学人物段仅作设定文档；年龄取"二十多岁"，身份以"守卫团团长"统一。`ai/npc_padwin.txt` 不改。

### 2. 塔楼之声分流表多出两个结局
- 文案同学判定表含 **月亮**（指向自己→解锁父母）与 **世界**（指向所有女巫→循环可破）
- 但《文案资产清单》D1 定的五结局是 **愚人/女祭司/节制/恋人/高塔**——月亮、世界**不在其列**
- 且现有 `tower_voice.json` 把"女巫们汇集"这一答**通向女祭司**，与文案同学分流表（→世界）**冲突**
- **需你定**：是扩结局（→6/7 个），还是改判词把月亮/世界**并入现有五结局**？

### 3. UI 文案的存放槽位
- 现状：UI 字符串**硬编码在 `scripts/ui/*.gd`**（如 `rental_ui.gd` 的身份证描述），无集中资源
- **已定（2026-09-10，owner）：方案 A**。已新建 `resources/data/ui_text.json`（v0.1，§九 全文入库，含 `_todo_migrate` 待迁移清单）；脚本硬编码串改为读它的迁移，随 §五 写入一并做。
- 理由：UI 后续会改版 → 文案与布局解耦；`assets/tilesets/ui_tarot/` 22 张牌框+牌背+判词背景+HUD 图标已到位 → 结局/占卜 UI 将重做，文案需预先在数据层。

---

## 五、通过后我将执行的写入清单（main 分支，一次提交）

1. 新建 `resources/data/opening.json`（开场双变体）
2. 改写 `resources/dialogues/soraya.json`（intro/confront 合并 effects）
3. 扩写 `resources/dialogues/tower_voice.json`（提问变体 + 分流）
4. 追加 `resources/endings/endings.json`（恋人/高塔）
5. 覆盖 `resources/data/items.json` 的 desc + 新建线索描述槽
6. （若选 A）新建 `resources/data/ui_text.json`
7. 新建 `resources/dialogues/` 情绪节点条目（padwin/soraya/cactus_bishop）
8. 编译 + 场景校验；报告验收

设计仓（docs/design）：新建《世界观精简设定_文案同学_v1.md》《人物设定_文案同学_v1.md》

---

## 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-10 | v0.1 | 首版：归一化清单 + 去向映射 + 覆盖对照 + 3 待确认点 |
| 2026-09-10 | v0.1.1 | 三点拍板：① v0.2 卡为准 ② 结局扩到 6（恋人/高塔）③ UI 用方案 A。录入已执行，见下。 |

---

## 六、执行记录（2026-09-10）

**游戏仓 `main` —— 已写入：**

| 文件 | 动作 |
|---|---|
| `resources/data/opening.json` | 新建（§一 完整版五拍 + 循环精简版）**未接运行时** |
| `resources/data/clues.json` | 新建（§八 六条线索描述；含新增 id `clue_tower_roster`） |
| `resources/data/ui_text.json` | 新建（§九 全文入库 + `_todo_migrate`） |
| `resources/data/items.json` | 覆盖 蘑菇/浆果/草药瓶 `desc`（§八） |
| `resources/endings/endings.json` | 追加 `lovers` / `tower`（高塔含 `text_unhoused`） |
| `resources/dialogues/soraya.json` | 改写 `intro`（§四）/ `confront`（§五 逼问） |
| `resources/dialogues/padwin.json` | `intro` 番茄分支改 A/B 双选（§五）；新增 `tomato_followup` |
| `resources/dialogues/cactus_bishop.json` | `intro` 补独白（§五）；新增 `scar_aftermath` |
| `resources/dialogues/tower_voice.json` | 提问变体（初次/后续）+ 新增「高塔」选项 |
| `scripts/autoload/game_manager.gd` | `CLUE_NAMES` 增补 `clue_tower_roster` |
| `scripts/ui/status_panel.gd` | 读 `clues.json` 显示线索描述 + 读 `ui_text.json` 空状态 |
| `scripts/ui/ending_ui.gd` | 未入住时用 `text_unhoused`（高塔首句替换） |

**校验：** JSON 9/9 通过；`magicai_compile_scripts` 65/65 无错；`main.tscn` 有效可实例化。

**未接（后续单独排期）：**
1. `opening.json` 尚无运行时消费者——第一轮开场仍是 `soraya.json` intro 的撞人旁白；接开场序列需改 `game.gd` 启动流程。
2. **恋人结局无触发**：`lovers` 正文已入 `endings.json`，但「番茄试探 → 傍晚倒门口 → 烧热水」这条 Padwin 线没有事件链（需新任务/场景）。
3. `clue_tower_roster`（塔楼阶下名录）无授予点。
4. `ui_text.json` 仅 `status_panel` 接了一部分；其余 `scripts/ui/*.gd` 硬编码串待 UI 改版时统一迁移（见 `_todo_migrate`）。

**设计仓 `docs/design` —— 已写入：**
- 《世界观精简设定_文案同学_v1.md》（§二 + 归一化 N1–N6）
- 《人物设定_文案同学_v1.md》（§三；帕德温按 v0.2 卡统一口径）
