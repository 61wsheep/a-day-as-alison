# 《天控全局》记忆系统 —— 调研笔记与构建方案

> 状态：v0.1 草案 · 2026-08-26
> 关联文档：`天控全局_第一回合切片_设计文档.md` v0.1（§9 风险 6：跨天记忆未解决——本文档补齐这块）
> 解决问题：① 天的跨天记忆（记仇/奖励始终如一）；② 区分不同 NPC 的记忆；③ 不破坏「天=导演 / Godot=裁判」契约。

---

## 第一部分：主流记忆系统是怎么构建的（调研）

### 1.1 两条学术/工业主线

| 流派 | 代表作 | 核心思想 |
|------|--------|----------|
| 记忆流 + 反思（游戏 NPC 向） | Generative Agents（斯坦福小镇, 2023） | 一切经历记成自然语言「记忆流」，按 **近期性 + 重要性 + 相关性** 三维度检索；重要事件累积到阈值就触发「反思」，生成更高层的抽象记忆 |
| 分层记忆（OS 类比，Agent 向） | MemGPT / Letta、MemoryOS | 上下文窗口 = RAM，外部存储 = 硬盘；记忆分核心层（永远注入 prompt）/ 回忆层（可搜索的对话历史）/ 归档层（长期事实库），由 Agent 自己决定换入换出 |

两者不是对立关系，而是**同一件事的两个侧面**：记忆流是"数据怎么记"，分层是"数据放哪层、什么时候进 prompt"。工程上通常两者结合。

### 1.2 通用分层架构（综合 MemoryOS / Mem0 / LangChain 等主流方案）

```
┌─────────────────────────────────────────────────┐
│ 第 0 层：工作记忆（Working Memory）               │
│   当前这次调用直接塞进 prompt 的内容              │
│   特点：容量小、用完即弃                         │
├─────────────────────────────────────────────────┤
│ 第 1 层：短期记忆（Short-term / Episodic）        │
│   结构化事件流：今天发生了什么（谁在何时何地做了啥）│
│   特点：按天滚动，原始、未加工                   │
├─────────────────────────────────────────────────┤
│ 第 2 层：中期记忆（Mid-term / Summary）           │
│   日结摘要：每天结束时把事件流压缩成摘要          │
│   特点：AI 生成、程序校验，替代原始流水账        │
├─────────────────────────────────────────────────┤
│ 第 3 层：长期记忆（Long-term / Core）             │
│   关系画像 + 反思结论 + 伏笔账本                  │
│   特点：小而精，每次调用都注入，是"人格一致性"的根│
└─────────────────────────────────────────────────┘
```

**关键机制（四个，缺一不可）：**

1. **写入（Admission）**：不是什么都能进长期记忆。事件要打分（重要性），低分留短期，高分晋升长期。
2. **检索（Retrieval）**：Generative Agents 的经典公式 = `score = 近期性(指数衰减) + 重要性(1-10) + 相关性(标签/语义匹配)`，取 Top-K 注入。
3. **压缩（Reflection/压缩）**：定期把一批原始记忆合成抽象结论（"玩家连续三天反抗我"→"这个玩家有反抗倾向"），抽象结论本身也作为记忆存回去。
4. **遗忘（Forgetting）**：用艾宾浩斯式衰减或热度淘汰，防止记忆无限膨胀拖垮检索质量。

### 1.3 多智能体 / 多 NPC 的记忆隔离

主流做法是**命名空间隔离 + 共享世界层**：

- 每个 Agent/NPC 有自己**私有的记忆流**（namespace 按 ID 隔离），只记"我看到的、我经历的"；
- 另有一层**公共世界状态**（谁都能知道的事实），大家共享；
- 检索时按 `owner_id` 过滤——A 永远搜不到 B 的私密记忆，除非事件发生在公共层。

**对《天控全局》的映射**：天 = 全知观察者（能看到一切），每个 NPC = 局部观察者（只知道自己和玩家之间的事）。这正好符合"天控全局"的世界观设定。

---

## 第二部分：《天控全局》记忆系统构建方案

### 2.1 设计原则（沿用既有契约）

- **AI 永远是内容生产者，不是状态写入者**：记忆的"事实层"（事件、计数、分数）由 Godot 写；AI 只产"解读层"（摘要、反思、态度结论），且经白名单校验后才落库。
- **记忆是给 prompt 用的，不是数据库工程**：不引入向量数据库。20 天一局的游戏，数据量极小（每天几十条事件），**标签 + 时间 + 重要性打分**的检索完全够用，别上 embedding。
- **玩家可感知优先**：记忆系统的好坏不看存了多少，看 NPC/天说出"你三天前……"这种话的频率。

### 2.2 总体架构

```
                    Godot（裁判，事实层写入与校验）
                              │
        ┌─────────────────────┼──────────────────────┐
        ▼                     ▼                      ▼
  第1层 事件流            第2层 日结摘要           第3层 长期记忆
  memory.events         memory.day_summaries   ┌─ 天：attitude_profile
  每天 append           午夜由审判面具生成      ├─ 每个NPC：relation_card
  (结构化JSON,程序写)   (AI产文,程序校验存)     └─ 伏笔账本 foreshadows
        │                     │                      │
        └──────────┬──────────┴──────────┬───────────┘
                   ▼                     ▼
        MemoryQuery.assemble()  ← 检索器（程序）
        输出 = 该次面具调用的"记忆注入块"
                   │
                   ▼
        天命面具 / 化身面具(NPC) / 审判面具 的 prompt
```

### 2.3 数据结构设计

#### 第 1 层：事件流（`heaven.memory.events`）

替代并扩展现有 `day_log`。每条事件：

```json
{
  "id": "evt_00042",
  "day": 3,
  "owner": "heaven",            // heaven=天看见的一切；soraya/padwin/...=该NPC亲历
  "type": "talk|visit|clue|defy|obey|gift|...",
  "tags": ["soraya", "plaza", "prophecy"],
  "fact": "玩家在广场与 soraya 对话，谈论了预言",   // 程序生成的客观描述
  "importance": 3,              // 1-10，由 Godot 规则表打分（见 2.5）
  "created_day": 3,
  "last_recalled_day": 0        // 供遗忘机制使用
}
```

> `owner` 字段就是"区分不同 NPC 记忆"的核心：`owner=soraya` 的事件只有 soraya 的对话能检索到；`owner=heaven` 的事件天和所有面具可见（NPC 可见其中与自己相关的子集）。

#### 第 2 层：日结摘要（`heaven.memory.day_summaries`）

午夜审判面具**追加一个输出字段** `memory_summary`（扩展现有 `judgment_schema.json`）：

```json
{
  "day": 3,
  "summary": "第三天。玩家继续试探边界，与 soraya 走近，对预言置若罔闻。",
  "key_events": ["evt_00042", "evt_00043"],   // AI 从当天事件中挑的，程序校验 id 存在
  "mood_shift": "watching→testing"
}
```

#### 第 3 层 a：天的态度画像（`heaven.memory.attitude_profile`）

一段**永远注入**所有面具 prompt 的短文本（≤150 字），由审判面具每晚提议更新、Godot 校验字数后落库：

```json
{
  "text": "玩家是个试探者：三次反抗预言，但善待村民，正在逼近塔楼的真相。我对他兴味大于怒意。",
  "updated_day": 3
}
```

这就是"记仇/奖励始终如一"的承重墙——它每天被重写一次，把散落的历史**压缩成一句人格结论**，保证天对玩家的态度跨天连贯。

#### 第 3 层 b：NPC 关系卡（`heaven.memory.relation_cards[npc_id]`）

每个 NPC 一张卡，程序化字段（Godot 写）+ 叙述字段（AI 提议、Godot 校验）：

```json
{
  "npc_id": "soraya",
  "affinity": 12,                 // 程序维护的好感度（沿用现有）
  "met_count": 5,                 // 见面次数（程序统计）
  "impression": "玩家信任我，向我透露过对循环的怀疑",   // AI 产，≤80字
  "unresolved": "玩家答应帮我找水井线索，还没兑现"      // AI 产，承诺/未竟事项
}
```

#### 第 3 层 c：伏笔账本（`heaven.memory.foreshadows`）

解决切片文档遗留的"伏笔追踪"问题。每条：

```json
{
  "id": "fs_003",
  "planted_day": 2,
  "text": "预言暗示塔楼钟声与循环有关",
  "status": "open|resolved|expired",
  "due_by_day": 8,               // 超过则 expired，防止伏笔烂尾
  "resolved_day": null
}
```

天命面具出牌时注入 open 状态的伏笔（最多 3 条），审判面具每晚可标记 resolve。**这保证天"说话算话"——博弈可信度的根基。**

### 2.4 检索器：`MemoryQuery.assemble()`

不用向量库，用确定性评分（Generative Agents 公式的简化版）：

```
score(evt) = importance × 2
           + 10 × exp(-0.3 × (current_day - evt.day))   # 近期性，指数衰减
           + 5  × 标签命中数(查询者npc_id / 地点 / 当前directive)
```

各面具的注入策略：

| 调用方 | 注入内容 | 预算 |
|--------|----------|------|
| 天命面具 | attitude_profile + 近3天日结摘要 + Top5事件(按score) + open伏笔×3 + 昨日顺从 | ≈600 tokens |
| 审判面具 | 今日全部事件 + 今日 compliance + attitude_profile + open伏笔 | ≈800 tokens |
| 化身面具(NPC对话) | 该NPC的 relation_card + owner=该NPC 的 Top5事件 + 昨日顺从/基调/明日种子（现有注入保留） | ≈400 tokens |

> 化身面具的检索**强制 `owner` 过滤**：soraya 只能检索 `owner in [soraya, heaven]` 且 tags 含 soraya 的事件——她从不知道玩家和 padwin 密谈了什么，除非那件事发生在公共场合（heaven 层）。

### 2.5 重要性打分规则表（Godot 计算，AI 不参与）

| 事件类型 | importance |
|----------|-----------|
| 顺从/反抗预言 | 7 |
| 发现新线索 | 8 |
| truth_proximity 跨档（如 0.3→0.6） | 9 |
| NPC 好感度跨档变化 | 6 |
| 普通对话/访问 | 2~3 |
| 天主动埋下的伏笔被玩家触及 | 8 |

### 2.6 遗忘与压缩

- 事件流：全量保留（20 天一局，撑死几百条 JSON，不需要删），但**检索分**随时间衰减——"遗忘"体现在想不起来，而不是删掉。
- 日结摘要：全量保留，只注入近 3 天。
- 伏笔：`due_by_day` 过期自动 expired，审判面具的 prompt 会提示"你有 N 条伏笔烂尾了"，驱动天自我修正叙事。

### 2.7 持久化

```
user://memory/events.jsonl          # 事件流，append-only
user://memory/day_summaries.json
user://memory/attitude_profile.json
user://memory/relation_cards.json
user://memory/foreshadows.json
```

沿用切片 v0.1 的调试级持久化思路；正式存档系统仍独立排期（这五个文件届时直接纳入存档包即可）。

### 2.8 落地文件变更清单

| 文件 | 变更 |
|------|------|
| `scripts/systems/heaven_memory.gd`（新） | 事件流/摘要/画像/关系卡/伏笔的 CRUD + 遗忘衰减 + 持久化 |
| `scripts/systems/memory_query.gd`（新） | `assemble(mask, context)` 检索器，§2.4 评分公式 |
| `scripts/autoload/game_manager.gd` | 事件打点接入（对话/访问/线索/compliance 结算处写事件）、importance 规则表 |
| `res://ai/judgment_schema.json` | + `memory_summary`、`key_event_ids`、`attitude_profile_update`、`resolve_foreshadow_ids` 四个字段 |
| `res://ai/oracle_schema.json` | + `plant_foreshadow`（可空：{text, due_in_days}） |
| `scripts/systems/ai_heaven_session.gd` | 两个面具的 prompt 组装处调用 `MemoryQuery.assemble()` |
| `scripts/systems/ai_dialogue_session.gd` | `_build_payload()` 注入 relation_card + 该 NPC 的检索事件 |
| `scripts/tests/test_heaven_memory.gd`（新） | 写入/检索/隔离/遗忘/跨天一致性测试 |

### 2.9 测试策略

- **隔离性测试**：写 10 条事件（分属 soraya/padwin/heaven），断言 soraya 的检索结果里**没有** padwin 的私密事件。
- **跨天一致性测试**：mock 三天反抗 → 断言第 4 天天命面具的 prompt 里 attitude_profile 含"反抗"语义、预言基调转 cold/testing。
- **伏笔闭环测试**：天埋伏笔 → 到期未应验 → 断言审判面具 prompt 收到"伏笔烂尾"提示。
- **规模压测**：模拟 20 天 × 每天 30 事件，断言单次 `assemble()` 输出不超 token 预算、耗时 <1ms。
- **回落测试**：AI 关闭时，事件流照常记录（程序写入），画像/摘要保持上一版，游戏可玩。

### 2.10 实施顺序建议（三步走）

1. **第一步（地基，无 AI）**：`heaven_memory.gd` + `memory_query.gd` + 事件打点 + importance 规则表。此时记忆系统已经能跑，只是没有 AI 摘要——先保证事实层正确。
2. **第二步（AI 接入）**：扩 judgment/oracle schema，接通日结摘要、态度画像、伏笔账本。
3. **第三步（NPC 侧）**：relation_card + 化身面具检索注入，让玩家在白天就能听到"你三天前答应我的事呢"。

---

## 附：调研参考来源

- Generative Agents (Park et al., 2023)：记忆流 + 三维度检索 + 反思机制，游戏 NPC 记忆的奠基之作
- MemGPT / Letta：OS 式分层记忆（Core/Recall/Archival），生产级实现
- MemoryOS (Kang et al., 2025)：三层存储 + 热度淘汰策略
- Mem0 / Zep：实体关系图谱式记忆（本项目 20 天规模不需要，备查）
