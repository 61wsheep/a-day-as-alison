# 下一步完善方向 · 文案录入后续 v0.1

> **背景：** 文案同学交付（《成为艾莉森的一天》文案.md）已于 2026-09-10 按 prompt 资产存放规则分类录入完毕。
> - 游戏仓 `main` → `089ab1b`（12 files，+285/−28）
> - 设计仓 `docs/design` → `e044e6a`（世界观/人物设定 + 录入预览稿）
>
> 本文件汇总**录入之后仍缺的机制**，供下一步排期参考。
> 相关文档：《文案录入预览稿_文案同学交付_v0.1.md》（§六 执行记录）、《文案资产清单.md》。

---

## 一、文案已入库、但「没有触发 / 没有消费者」

| # | 缺口 | 现状 | 缺什么 | 涉及文件 | 工作量 |
|---|------|------|--------|---------|--------|
| **A1** | 开场序列没接 | `opening.json` 已建，**无人读** | 第一轮播五拍（绳→苔→树→看→撞），第二轮起播循环精简版。需改启动流程 + 一个全屏旁白序列 UI | `resources/data/opening.json`、`scripts/game.gd`、`scripts/ui/dialogue_ui.gd`（或新组件） | 中 |
| **A2** | 恋人结局没有事件链 | `endings.json` 的 `lovers` 正文已入库，**无触发** | 「番茄试探 → 傍晚他倒在门口 → 烧热水 → 后半夜『我对你做了什么？』」整条 Padwin 线没有载体。需新任务/场景 + 傍晚计时触发 | `endings.json`、`resources/dialogues/padwin.json`、`scripts/systems/`、`quest_manager.gd` | 大 |
| **A3** | 塔楼名录无授予点 | `clue_tower_roster` 已在 `CLUE_NAMES` / `clues.json`，**无人 `discover_clue`** | 在塔楼场景加一块可交互木牌（进入／搜查时触发） | `scenes/`、塔楼交互脚本、`clues.json` | 小 |
| **A4** | 塔楼之声「第一轮无选项」的设计 | 文案 §六 要求：第一轮**没有**「TA 是谁」的选项，这是**设计好的挫败**。当前实现**保留了选项**（否则第一轮走不到任何结局） | 需你拍板：是接受现状，还是实现「第一轮只能做别的动作/问讯」 | `resources/dialogues/tower_voice.json` | 需决策 |

### 零碎欠账

- **`clue_forest_fake` 描述缺失**——文案同学本次未交付此条（节制线取得），`clues.json` 里已留占位「（待补）」。需补一句或回退用旧文案。
- **`ui_text.json` 只接了一部分**——目前仅 `status_panel.gd` 读取（线索空状态 + 线索描述）。其余 `scripts/ui/*.gd` 的硬编码串，建议**等 UI 改版时统一迁移**（文件内 `_todo_migrate` 已列清单），避免改版前做无用功。

---

## 二、AI 底座（真机体感痛点）

DialogueLab 探针（`D:\AIGameBuild\DialogueLab\`）已跑出结论，缺口明确：

| 项 | 结论 |
|---|------|
| **延迟主因** | **非流式**——全文生成完才返回，玩家等 20s+ |
| **流式的收益** | 实测**首字延迟 1.0–1.7s**（全文仍慢，但玩家立刻看到字在动） |
| **⚠️ 已探明的坑** | 流式下 SiliconFlow 会**弱化 `response_format: json_object`** → 开场那轮撞 512 token 上限 → JSON 截断解析失败。改流式**必须同时做 JSON 容错解析**，或把「长度限幅」与「首字渲染」拆开 |
| **上下文分层门控** | 是**省成本 / 省 prefill**，**不是**降延迟。别指望它对 20s 有帮助 |

**下一步动作：** 给 `scripts/autoload/ai_bridge.gd` 上流式（SSE 解析 + 增量吐字），并配套 JSON 容错。

---

## 三、建议顺序

1. **A1 开场序列**——玩家第一眼就看到的东西，收益最直接。
2. **B 流式改造**——真机体验的最大痛点。
3. **A2 恋人线**——最大，但可后置。
4. **A3 / A4**——小修 + 一个决策。

---

## 附：仓库注意事项（避免踩坑）

- **游戏仓的 git 根是 `D:/AIGameBuild`**，`成为艾莉森的一天/` 是其**子目录**，**不是**独立仓（无嵌套 `.git`）。
- 设计文档仓是**另一个独立仓** `D:\AIGameBuild-DesignDocs`（分支 `docs/design`）。
- 分支规则见 `CLAUDE.md`：游戏文件 → `main`；设计文档 → `docs/design`。
- 本轮提交采用了**文件级 `git add`**，因为工作区里另有未提交的改动（`scripts/game.gd`、`scenes/*.tscn`、`project.godot`、`export_presets.cfg`、散落的 `assets.zip`/`scenes.zip`、`DialogueLab/`、根目录 `《成为艾莉森的一天》文案.md`）——**这些未纳入本轮提交**，需要时单独处理。

---

## 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-10 | v0.1 | 首版：录入后续缺口 A1–A4 + AI 底座缺口 + 建议顺序 |
