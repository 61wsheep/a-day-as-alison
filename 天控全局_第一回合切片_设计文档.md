# 《天控全局》第一回合切片 —— 设计文档

> 状态：v0.1 草案 · 2026-08-25
> 关联文档：
> - `AI机制点子集.md` v0.2 ——「天」统一 Agent 四面具架构（本切片的地基）
> - `NPC对话AI正式接入_脑暴想法集.md` v2 —— 在确定性骨架上开缝（S1/S2 已落地）
> - 无AI试玩Demo v1.1 —— 当前确定性叙事骨架（本切片在其上叠加）
>
> 本切片是「天」从"生成文本"升级为"掌控游戏走向"的第一个落地目标。

---

## 1. 设计总纲

### 1.1 愿景

> **玩家表面在探索这个世界，实际上在和「天」博弈。** 天是一个有意识的存在——游戏规则的制定者、叙事的讲述者、世界的主宰者。玩家通过与整个世界的互动，与天进行一场持续多日的、不对称的对弈。

文本生成不是亮点（预写脚本做得到）；**天作为"有边界的导演"对玩家的行为产生真实、连贯、不可预测的反应，才是护城河。**

### 1.2 核心边界：天=导演，Godot=裁判

| | 天（AI，导演） | Godot（裁判） |
|---|---|---|
| 掌控 | 世界的一切回应与走向：塔罗出牌、预言、NPC 言行、午夜复盘、明日走向、结局裁决 | 数值、门控、天数、结局条件、兜底回落 |
| 产出 | 叙事文本 + 方向提议（明日种子、终结提议） | 状态平移（benevolence/engagement/…）、顺从/反抗判定、终结批准 |
| 类比 | 棋手 | 棋盘与裁判 |

**与既有契约的一致性**（沿用脑暴 v2.6 第 4 条与设计定论 2.1）：
- AI 永远是"内容生产者"，不是"状态写入者"——一切数值/flag/线索变更经 Godot 白名单校验。
- 玩家行为 → 数值平移（Godot 计算）；数值状态 → AI 决定叙事基调。

### 1.3 本切片在四面具中的位置

```
                          ┌──────────────────────┐
                          │        天             │
                          │   （唯一 Agent）       │
            ┌─────────────┼──────────┬───────────┤
            ▼             ▼          ▼           ▼
       🃏 天命面具     🎭 化身面具  📜 审判面具  ⚡ 裂缝面具
       （塔罗出牌）    （NPC 对话）  （午夜复盘）  （直接独白）
        ● 本切片       ● 已接入      ● 本切片     ○ 后续
```

本切片 = 天命面具 + 审判面具 + 天的状态层，跑通「出牌 → 应对 → 复盘 → 回应」一个完整回合。

---

## 2. 切片范围

### 2.1 一个完整回合（范围图）

```
Day N 早晨 ── TarotUI.open()
   ├─ 天·天命面具（AI）   读：天数/态度/观测窗口 → 出牌 + 预言诗 + directive tag + 基调
   │     Godot裁判：校验牌∈22张、校验 directive∈枚举 / AI挂→回落数据牌
   │     记录：daily_prophecy，重置今日 day_log
   │
   ├─ 白天 ── 玩家自由探索（现有引擎全复用）
   │     NPC对话(S1已接AI)  ←── 注入昨日顺从 + 天的基调 + 明日种子（NPC"知道你昨天干了啥"）
   │     Godot裁判：记 day_log（互动/线索/选择/访问）+ 对照 directive 算顺从/反抗
   │
   └─ 午夜 ── 入睡按钮
        ├─ 天·审判面具（AI）  读：今日 day_log/预言/顺从/态度 → 回顾叙事 + 明日种子 + 终结提议
        │     Godot裁判：apply_heaven_rules() 结算态度 / 终结只在软限窗口批准 / 硬上限20天兜底
        │     UI：先显示天的回顾 → 再点入睡
        │
        └─ Day N+1 早晨 ── 天命面具收到（昨日顺从 + 明日种子）→ 预言呼应昨天
```

### 2.2 明确不做（非目标，后续切片）

- 裂缝面具（直接独白）—— 后续
- 终局裁决（结局 AI 生成）—— 后续
- NPC 自主活动轨迹 —— 后续
- 伏笔追踪表 / 跨天深层记忆 —— 用户拍板"先不解决"（见 §9 风险）
- 存档 / 读档系统 —— 独立事项
- avoid 型 directive（"不要去 X"）—— 需要"相反项"概念，v0.1 只做正向 directive

---

## 3. 天的状态层（Godot 持有）

### 3.1 持久状态字段（跨天，存 `GameManager`）

| 字段 | 范围 | 初值 | 说明 |
|------|------|------|------|
| `heaven.benevolence` | -1.0 ~ +1.0 | 0.0 | 天对玩家的善意（负=敌意，正=慈爱） |
| `heaven.engagement` | 0.0 ~ 1.0 | 0.3 | 天对玩家的兴趣（低=冷淡，高=痴迷） |
| `heaven.truth_proximity` | 0.0 ~ 1.0 | 0.0 | 玩家对循环真相的理解深度 |
| `heaven.prophecy_resistance` | 0 ~ ∞ | 0 | 累计反抗预言次数 |

> 持久化：v0.1 存内存 + 写入 `user://heaven_state.json`（调试/测试用）。完整存档系统独立于本切片。

### 3.2 单日状态字段（每天早晨重置）

| 字段 | 内容 |
|------|------|
| `heaven.daily_prophecy` | 今日预言：`{card_name, prophecy, directive, tone, internal_note}` 或 null |
| `heaven.daily_compliance` | 午夜结算前为 null；结算后为 `"obey"` / `"neutral"` / `"defy"` / null（未设局） |
| `heaven.day_log` | 今日观测记录（见 §4.2） |

### 3.3 观测窗口（天"看见"的东西，喂给每个面具调用）

```
heaven.observation = {
  "last_day_log":    ...,   # 昨天的完整 day_log
  "last_compliance": ...,   # 昨天的顺从判定
  "last_judgment":   {...}, # 昨天的审判输出（day_summary / tomorrow_seed / internal_note）
  "internal_notes":  [...]  # 最近 1-2 天的天内部笔记（oracle + judgment）
}
```

v0.1 只保留最近 1 天完整信息——足够支撑"呼应昨天"，不建深层记忆。

### 3.4 行为 → 态度平移规则表（`apply_heaven_rules()`，Godot 计算）

午夜结算时调用。输入为当天观测，输出为状态平移。**AI 不参与数值计算。**

| 观测 | benevolence | engagement | truth_proximity | resistance |
|------|------------|------------|-----------------|-----------|
| 顺从预言（compliance==obey） | +0.04 | -0.02 | — | — |
| 反抗预言（compliance==defy） | -0.04 | +0.08 | — | +1 |
| 中性 / 未设局 | — | +0.01（天仍在看） | — | — |
| 发现新线索（每条） | — | +0.03 | +0.06 | — |
| 当天 NPC 好感净上升 | +0.02 | — | — | — |
| 当天 NPC 好感净下降 | -0.02 | +0.02 | — | — |
| 当天与 ≥2 个 NPC 对话 | — | +0.02 | — | — |

> ⚠️ 表中数值为 v0.1 初值，**必须在测试中迭代校准**（见 §9 风险 5）。所有字段 clamp 到定义范围。

### 3.5 顺从/反抗三态判定（Godot 裁判，不交 AI 自评）

- `daily_prophecy.directive == null` → 当天不设局，compliance = null。
- **visit A（地点）**：obey = day_log 记录了访问 A；defy = 访问了另一地点 B **且** 在 B 有 ≥1 次有效交互（对话/线索）；其余 neutral。
- **talk N（NPC）**：obey = 与 N 有过对话；defy = 与 N 之外的其他 NPC 合计对话 ≥2 次 **且** 与 N 对话 0 次；其余 neutral。

> 判定原则：**顺从/反抗是裁判的事实**（用机器可读 tag 客观对照），不是 AI 主观自评。预言诗保留诗意留白，directive 只是隐层机械信号——玩家永远不会直接看到 tag。

---

## 4. 天命面具（早晨 · AI 出牌）

### 4.1 输入（Godot 组装）

```
天数 current_day
benevolence / engagement / prophecy_resistance
观测窗口 heaven.observation（含昨日顺从、昨日审判、明日种子）
可用枚举：
  places = [plaza, tree_village, tower]
  npcs   = [soraya, padwin, cactus_bishop]
  22 张大阿尔卡那牌名清单
系统提示：res://ai/tian_system.txt（「天」角色与世界，已存在，复用）
```

### 4.2 输出 schema（新 `res://ai/oracle_schema.json`）

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "card_name": {
      "type": "string",
      "description": "从 Godot 提供的 22 张大阿尔卡那中选一张"
    },
    "prophecy": {
      "type": "string",
      "description": "2-4 句预言诗。必须是诗，不是任务指令；模糊、可多种应验、可被违背"
    },
    "directive": {
      "type": ["object", "null"],
      "description": "今日设局的机械方向（可空；空=今天天只观察不下局）",
      "properties": {
        "type":   {"type": "string", "enum": ["visit", "talk"]},
        "target": {"type": "string", "description": "visit→地点；talk→NPC。必须是 Godot 提供的枚举成员"}
      },
      "required": ["type", "target"]
    },
    "tone": {
      "type": "string",
      "enum": ["gentle", "curious", "watching", "testing", "cold"],
      "description": "今日天的态度基调标签，注入 NPC 对话语气"
    },
    "internal_note": {
      "type": "string",
      "description": "天对今日设局的内部意图（玩家不可见），进观测窗口"
    }
  },
  "required": ["card_name", "prophecy", "tone", "internal_note"]
}
```

### 4.3 Godot 校验与回落

| 情况 | 处理 |
|------|------|
| `card_name` 不在 22 张内 | 用随机数据牌的牌名兜底，预言仍用 AI 文本 |
| `directive.target` 不在枚举内 | 丢弃 directive（当天不设局），其余字段保留 |
| AI 不可用 / 超时 / 解析失败 | 完整回落数据牌（现有 `tarot_ui.gd` 随机抽牌 + data reading） |

### 4.4 UI 流程

`TarotUI.open()` → AI 可用时先显示加载态（**"天正凝视牌面……"**）→ AI 返回后展示 `card_name` + AI `prophecy` 文本 → 存入 `daily_prophecy`。数据牌路径与现状一致。

---

## 5. 审判面具（午夜 · 天复盘）

### 5.1 输入（Godot 组装）

```
天数 current_day
今日 day_log（完整）
今日 daily_prophecy（预言 + directive）
今日 daily_compliance（Godot 已算好的顺从/反抗）
benevolence / engagement / truth_proximity / prophecy_resistance
观测窗口（前情提要）
```

### 5.2 输出 schema（新 `res://ai/judgment_schema.json`）

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "day_summary": {
      "type": "string",
      "description": "200-400 字回顾叙事。天对今天的裁定与态度定格；第三人称为主，可隐约带第一人称情绪；是玩家稳定读到天态度的窗口"
    },
    "tomorrow_seed": {
      "type": "string",
      "description": "明日伏笔/走向种子，1-2 句；注入明日 oracle prompt 与明日 NPC 对话"
    },
    "should_end_loop": {
      "type": "boolean",
      "default": false,
      "description": "天提议终结（仅当真觉得该终结时 true）。Godot 只在天数窗口内批准，永不越权"
    },
    "internal_note": {
      "type": "string",
      "description": "天对今日的真实评价（玩家不可见），进观测窗口"
    }
  },
  "required": ["day_summary", "tomorrow_seed", "should_end_loop", "internal_note"]
}
```

### 5.3 Godot 校验与回落

| 情况 | 处理 |
|------|------|
| `should_end_loop == true` | 仅当 `current_day >= 12`（软限窗口）才批准进入终结流程；否则忽略。**硬上限 20 天无论如何强制终结**（现有 `reset_loop` 处加闸） |
| 状态结算 | 只由 `apply_heaven_rules()` 用客观观测计算，AI 的 `day_summary` 纯叙事不影响数值 |
| AI 不可用 / 超时 / 解析失败 | 回落固定模板（现有"午夜已至，石巢塔楼的方向传来钟声……"文案）+ 无明日种子 |

### 5.4 UI 流程

午夜 → 入睡按钮点击 → 显示加载态（**"天在审视这一天……"**）→ AI 返回后显示 `day_summary` 回顾面板 → 按钮"入睡（结束今天）"→ 点击后 `apply_heaven_rules()` + 存 `tomorrow_seed` + `reset_loop()`。

---

## 6. 跨天衔接

### 6.1 明日预言呼应昨天

次日早晨 oracle 的 prompt 注入：`昨日顺从判定` + `昨日审判 internal_note` + `tomorrow_seed`。天据此让新预言"接得上"昨天——顺从者得赞许的暗示，反抗者得警惕/兴味的暗示。

### 6.2 NPC 对话"知道你昨天干了啥"

在 `ai_dialogue_session.gd` 的 `_build_payload()` 开场块追加一行注入：

```
昨天：玩家[顺从/反抗/未理会]了天的预言；天今日基调 [tone]；明日种子（若有）：[tomorrow_seed]
```

这是"被注视的回应感"在白天的主体通道——第二天 NPC 随口一句"你昨天……没去"，玩家的博弈感就立起来了。

---

## 7. 落地文件变更清单

| 文件 | 变更 |
|------|------|
| `scripts/autoload/game_manager.gd` | + 天的状态层字段、+ `day_log` 记录、+ `apply_heaven_rules()` 规则表、+ compliance 判定、+ 预言/种子存储、+ 硬上限闸 |
| `scripts/ui/tarot_ui.gd` | + AI 出牌路径（加载态 + schema 校验 + 回落数据牌） |
| `scripts/game.gd` | + 午夜审判流程（先回顾后入睡 + 加载态 + 回落模板） |
| `scripts/systems/ai_heaven_session.gd`（新） | oracle / judgment 两个入口，复用 AIBridge + tian_system.txt，含 schema 校验与兜底链 |
| `res://ai/oracle_schema.json`（新） | §4.2 的 schema |
| `res://ai/judgment_schema.json`（新） | §5.2 的 schema |
| `scripts/autoload/event_bus.gd` | + 信号：`heaven_oracle_ready`、`heaven_judgment_ready`、`heaven_thinking`（或复用 `ai_thinking`） |
| `scripts/systems/ai_dialogue_session.gd` | + S1 prompt 注入昨日顺从/基调/明日种子（§6.2） |
| `scripts/tests/test_heaven_round.gd` + `scenes/tests/test_heaven_round.tscn`（新） | mock 驱动的完整回合测试 |
| `resources/tarot/major_arcana.json` | 不改内容，仅作为 22 张牌的校验集 |

---

## 8. 测试策略

- **Mock 双路径**：复用 `AIBridge.set_mock_responder()`，分别 mock"合法 schema"与"非法 schema/超时"两套响应。
- **完整回合测试**：day start → oracle mock → 程序化写入 day_log（模拟访问/对话）→ midnight → judgment mock → 断言：状态平移正确、compliance 正确、tomorrow_seed 已存、次日 oracle prompt 含昨日顺从。
- **回落测试**：AI 关闭（`--ai-off`）→ oracle 回落数据牌、judgment 回落模板，游戏仍完整可玩。
- **终结闸测试**：`should_end_loop=true` 在第 10 天被拒绝、第 14 天被批准、第 20 天强制终结。
- 扩展 `autotest.gd` 或独立 test scene，沿用现有 `test_*.gd` 模式。

---

## 9. 风险与缓解

| # | 风险 | 缓解 |
|---|------|------|
| 1 | AI 选 directive 与游戏现实不匹配 | Godot 枚举校验 + 不匹配则丢弃（不设局），绝不让非法 tag 进 day_log |
| 2 | 预言应验太机械，失去诗意 | 预言诗留白是硬约束（schema 描述 + tian_system 已有）；directive 只是隐层机械信号，玩家永远看不到 tag |
| 3 | 天控全局但玩家无感 | 观测尽早注入 NPC 对话（§6.2），让"被注视"在白天就有体感，不等午夜 |
| 4 | 成本/延迟：每天 +2 次 AI 调用 | oracle/judgment 用短输出 + 低 temperature；加载态文案兜底；回落路径保证无 AI 也能玩 |
| 5 | 态度平移规则需要校准 | v0.1 数值标"待调"，用完整回合测试数据迭代；规则表集中在 `apply_heaven_rules()` 一处 |
| 6 | **跨天记忆未解决**（用户拍板暂缓） | 本切片只保证"呼应昨天"；一旦要"记仇/奖励始终如一"，必须补观测窗口扩展 + 伏笔表，这是博弈体验的承重墙 |

---

## 10. 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-25 | v0.1 | 初稿：定「天=导演 / Godot=裁判」边界；定义第一回合切片（天命+审判+状态层）；schema、规则表、文件清单、测试策略 |
