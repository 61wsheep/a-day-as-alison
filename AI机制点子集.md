# AI 机制点子集 ——《成为艾莉森的一天》

> 状态：v0.2 · 2026-07-29
> 目的：系统性整理 AI 作为游戏核心驱动机制的可行方案，为后续设计讨论与原型开发提供锚点。
>
> v0.2 变更：整合策划对 §6 开放问题的全部回答，重构为「天」统一 Agent 架构；新增实现难点、架构方案、分阶段实现路径、建议。

---

## 目录

1. [前提：游戏基础骨架回顾](#1-前提游戏基础骨架回顾)
2. [核心决策：五项设计定论](#2-核心决策五项设计定论)
3. [统一架构：「天」作为唯一 Agent](#3-统一架构天作为唯一-agent)
4. [子机制详解](#4-子机制详解)
   - 4.1 天命系统（塔罗）
   - 4.2 化身系统（NPC）
   - 4.3 审判系统（午夜 + 终章）
   - 4.4 裂缝系统（天的直接介入）
   - 4.5 反抗系统（玩家对天命的博弈）
5. [实现难点分析](#5-实现难点分析)
6. [技术架构方案](#6-技术架构方案)
7. [分阶段实现路径](#7-分阶段实现路径)
8. [我的建议](#8-我的建议)

---

## 1. 前提：游戏基础骨架回顾

| 层级 | 现有资产 |
|------|---------|
| 引擎 | Godot 4.6 · 2D · GL Compatibility |
| 时间 | 一天 5 时段（morning/afternoon/evening/night/midnight），线性推进，午夜 → 新循环天 |
| 经济 | 金币 100，可增减 |
| NPC | Soraya(55) · Padwin(50) · Cactus Bishop(25)，好感度 0–100 五段制 |
| 系统 | 线索收集 · 结局解锁 · 每日塔罗牌 · 树屋租赁 · 背包 |
| 场景 | Main.tscn（Camera2D 就位），levels/ui 空目录待建 |
| 资产 | 一张森林草稿概念图 · tilesets 空目录 |

**核心游戏循环**：每天早晨抽塔罗牌 → 5 个时段探索 / 对话 / 收集线索 → 午夜结算 → 新一天开始 → 多天积累后走向某个结局。天数上限：软上限 12–16 天，硬上限 20 天。

---

## 2. 核心决策：五项设计定论

以下五条来自策划对开放问题的回答，作为整个 AI 机制的地基。

### 2.1 态度驱动：数值管方向，AI 管表情

```
Godot 本地维护两个数值维度：

    benevolence（善意）  ── 天对玩家的态度：敌意 ↔ 慈爱
    engagement（投入度） ── 天对玩家的兴趣：冷淡 ↔ 痴迷

玩家行为 → 数值平移（Godot 计算）
数值状态 → AI 决定叙事基调（午夜结算时）
AI 不碰数值本身
```

| 玩家行为示例 | benevolence 影响 | engagement 影响 |
|-------------|-----------------|-----------------|
| 顺从预言 | 微升 | 微降（天觉得你无趣） |
| 反抗预言 | 微降 | 微升（天觉得你有意思） |
| 帮助 NPC 解开心结 | 升 | 升 |
| 伤害 NPC 信任 | 降 | 升（天在观察你） |
| 发现线索推进真相 | — | 显著上升 |

### 2.2 对话输入：门面 + 暗门

```
默认界面：
┌─────────────────────────────┐
│ Padwin 似乎在等你说什么……     │
│                             │
│  「关于昨天你说的那个实验……」  │  ← AI 生成的话题建议（NPC 本人语气）
│  「树湖那边最近有什么变化吗？」  │
│  「你今天看起来有心事。」       │
│  ─────────────────────────  │
│  [________________]  ↩      │  ← 极简自由输入框
└─────────────────────────────┘

话题建议 = 门面（大多数玩家走这条路，体验流畅）
自由输入 = 暗门（硬核玩家探索更深层的互动，但成本高）
```

### 2.3 天能否直接对话：能——但只能是独白

天打破沉默的那一刻，玩家被剥夺回应能力。不对称性保留到底。

两种触发方式：

| 方式 | 触发条件 | 效果 |
|------|---------|------|
| **结局触发（稳妥）** | 终章 —— 无论哪种结局，天至少说一次话 | 情感收束，每个玩家都能体验到 |
| **裂缝触发（刺激）** | 中途低概率 —— 特定数值组合 + 随机骰子 ≈ 2-5% 概率 / 天 | 极度稀有，社群传播引爆点 |

裂缝触发的独白示例方向：

> "你感觉到了，对吗？今天——和昨天不一样。是我让它不一样的。我想看看你会怎么做。"
>
> 然后沉默。此后整整一天，天不再有任何超出常规的介入。

**设计意图**：裂缝触发让天的存在从"隐约的怀疑"变成"确凿的恐惧/感动"，是对玩家世界观的微型震荡。

### 2.4 循环源头：事实层固定，意义层动态

| 层级 | 内容 | 可变性 |
|------|------|--------|
| **事实层** | 循环是谁触发的？规则是什么？能不能打破？ | **固定**，写在 System Prompt 中，永不改变 |
| **意义层** | 天的意图是考验 / 囚禁 / 陪伴？三个 NPC 在循环中的真正位置？主角是被卷入的过客还是循环成立的条件？ | **动态**，AI 根据玩家行为在旅程中逐渐浮现 |

这解决了"AI 自由度过高导致结局无根"的问题——地基是死的，但房子长什么样取决于你和 AI 的共舞。

### 2.5 天数上限：硬上限 20 天，软上限 12–16 天

```
Day  1 ────────────────────────────── 12 ─────────────── 16 ──── 20
      │                                  │                  │       │
      自由探索期                     软上限：AI 提高终结倾向   硬上限：强制结局
      (AI 不主动推动终结)            (条件达标可提前终结)    (无论如何结束)
```

- **硬上限 20 天**：到达后强制进入结局。避免无限循环稀释叙事。
- **软上限 12–16 天**：AI 在此区间显著提高终结叙事出现的概率；如果数值达标 + 叙事节奏到位，AI 可在任意天提前终结。
- **AI 终结条件**：`engagement` 到达高位 + 玩家对真相的理解深度足够 + 至少一个 NPC 关系走向了明确结局（和解 / 决裂 / 牺牲）。

---

## 3. 统一架构：「天」作为唯一 Agent

### 3.1 概念

> 「天」不是计时器。天是一个**有意识的实体**——游戏规则的制定者、叙事的讲述者、世界的主宰者。
>
> 游戏体验的本质是：**一个活的存在，与玩家进行一场持续多日的、带有神学隐喻的不对称对话。**

之前的「三重引擎」（塔罗引擎 + NPC 引擎 + 叙事引擎）不是三个独立的 AI——它们都是「天」在不同层面的**面具**：

```
                          ┌──────────────────────┐
                          │        天             │
                          │   （唯一 Agent）       │
                          │                      │
            ┌─────────────┼──────────┬───────────┤
            │             │          │           │
            ▼             ▼          ▼           ▼
       🃏 天命面具     🎭 化身面具  📜 审判面具  ⚡ 裂缝面具
       （塔罗+预言）  （NPC 对话）（午夜+终章）（直接独白）
```

同一个意志，四张面孔。玩家永远在和「天」互动——只是大部分时候天戴着面具。

### 3.2 「天」的定位

| 维度 | 定位 |
|------|------|
| **可见性** | 不可见。除非在裂缝或结局独白中，玩家不能和「天」直接对话。天不是 NPC。 |
| **感知方式** | 塔罗解读的诗意文本、NPC 不经意间泄漏的"不该知道的话"、世界细节中的呼应、午夜的结算叙事、裂缝独白 |
| **存在感** | 大部分时间如空气般自然；"反常时刻"隐约感到；裂缝时刻确凿感受；结局时刻正面面对 |
| **态度** | 动态。由 Godot 数值系统（benevolence + engagement）约束，AI 在此约束内决定表情 |

### 3.3 每日流程中「天」的行动全景

```
                          ┌──────────────────────┐
                          │    Godot 本地维护       │
                          │ benevolence,           │
                          │ engagement,            │
                          │ 好感度, 线索, 天数      │
                          └──────────┬─────────────┘
                                     │
[早晨 —— 天命面具]                    │
  天抽牌 + 解读                     │ 输入：benevolence, engagement, 天数, 前日行为摘要
  生成今日"天命主题"                │ AI 决策：牌的选择 + 预言文本 + 今日事件种子
  显示 3-5 句预言诗                │
                                     │
[上午·下午 —— 化身面具]              │
  玩家选择 NPC 交互                 │ 输入：NPC 角色卡 + 记忆摘要 + 昨日对话关键点
  天戴着 NPC 的面具回应              │       + 今日天命主题 + 话题建议生成
  NPC 的活动轨迹由天调控            │ AI 决策：对白文本 + 情绪偏移 + 记忆更新
  天的存在感：几乎察觉不到           │
                                     │
[傍晚·夜晚 —— 伏笔收束]              │
  天让今天的暗线浮出水面             │ 输入：今日实际事件日志 + 预言文本
  环境细节呼应预言                  │ AI 决策：晚间事件的走向 + 关键瞬间的触发条件
  天的存在感：隐约能感到             │
                                     │
[午夜 —— 审判面具]                   │
  天生成今日回顾文本                │ 输入：今日完整日志 + benevolence + engagement
  天评估预言应验程度                │       + truth_proximity + NPC 关系变化
  天决定明日走向 / 终结倾向         │ AI 决策：回顾叙事 + 态度基调 + 终结标志
  天的存在感：清晰可辨（但尚未直说）  │ 输出：tomorrow_seed, should_end_loop
                                     │
[裂缝 —— 天摘下面具]                 │
  低概率直接独白                    │ 触发条件：特定数值区间 + 随机骰子
  玩家无法回应                      │ AI 决策：独白内容（不超过 5 句）
  天后回归沉默                      │ 效果：对玩家世界观的微型震荡
```

---

## 4. 子机制详解

### 4.1 天命系统（塔罗）—— 天的第一张面具

**核心机制**：塔罗牌不是 buff 卡，是当日的**叙事锚点**。天通过牌来设定一天的语境。

| 步骤 | 内容 | 天（AI）的职责 |
|------|------|--------------|
| 抽牌 | 从 78 张牌库中抽取 | 基于 benevolence + engagement + 天数 + 前日行为模式，动态调整牌库权重 |
| 解读 | 3–5 句预言诗 | 基于牌的正逆位 + 玩家当前处境，用诗意的、模糊的语言生成 |
| 映射 | 在 5 个时段中编排呼应 | 生成今日事件种子（suggested_events），Godot 据此触发具体内容 |
| 回顾 | 午夜揭示应验 | 回溯今日事件日志，生成一段"哪些事应了预言"的回顾文本 |

**预言文本的设计原则**：
- 永远是诗 —— 不是指令，不是任务日志
- 永远模糊 —— 可以被多种方式"应验"
- 永远可违背 —— 玩家可以选择反抗预言（见 §4.5 反抗系统）

### 4.2 化身系统（NPC）—— 天的第二张面具

**核心机制**：NPC 是天戴着面具与你互动的方式。他们不是天，但天通过他们看、听、回应你。

| NPC | 初始态度 | 立场 | 隐藏秘密 |
|-----|---------|------|---------|
| **Soraya** | 友好 (55) | 怀疑论者——认为主角在说胡话 | 她是最早发现循环的人 |
| **Padwin** | 中性 (50) | 研究者——主动和主角合作 | 知道循环的原因但不敢说 |
| **Cactus Bishop** | 疏远 (25) | 祭司——声称是循环的守护者 | 在保护主角免受真相伤害 |

**AI 驱动的交互特性**：

- **话题建议 + 自由输入双入口**：天以 NPC 的语气生成 3-5 个话题建议；底部保留极简自由输入框（暗门）
- **跨循环记忆**：NPC 记得你说过的话，每轮对话后由 AI 生成 3-5 条关键摘要存入记忆；是否在新的对话中主动提及，由 AI 根据 NPC 性格和当前氛围决定
- **NPC 有自主动作**：天每天为每个 NPC 编排活动轨迹（在哪儿、做什么、情绪基调）
- **NPC 之间相互关联**：天可以编排 NPC 之间的幕后互动——玩家通过观察态度变化推断谁和谁之间发生了什么
- **三重真相拼图**：三个 NPC 各掌握真相的不同碎片（事实层固定），玩家通过对话交叉印证

### 4.3 审判系统（午夜 + 终章）—— 天的第三张面具

#### 午夜结算

每天的终了仪式。天在此刻的"存在感"达到日常状态的峰值。

```
输入（Godot 提供）：
├── 今日事件日志（玩家去了哪儿、和谁说了什么、做了什么选择）
├── 当日预言文本
├── benevolence, engagement 当前数值
├── truth_proximity（玩家对真相的理解深度）
└── 各 NPC 好感度变化

输出（天生成）：
├── day_summary_narrative（200-400 字回顾叙事）
├── prophecy_fulfillment（0-1 预言应验度）
├── tone_shift：对玩家态度的微调方向
├── tomorrow_seed（明日叙事的伏笔种子）
└── should_end_loop（是否推动终结）
```

#### 终章

循环终结时，天生成你的结局。

**事实层是固定的**（循环的物理真相不会变），但**意义层是 AI 生成的**：
- 天回顾整个旅程中你的行为模式和选择
- 天揭示循环的"意义"——至少是一个版本的意义（天的视角未必是真相）
- 天打破沉默，以"我"自称对你进行最终独白
- 终章长度 500–1500 字，纯文学性创作，非模板

### 4.4 裂缝系统 —— 当天摘下面具

**这是整个游戏最具辨识度的设计之一。**

```
┌─────────────────────────────────────────────┐
│  正常状态：天戴着面具（≈95-98% 的游玩时间）   │
│  ┌─────────────────────────────────────┐    │
│  │  🃏 塔罗解读 → 玩家觉得是"系统生成的"  │    │
│  │  🎭 NPC 对话 → 玩家觉得是"角色在说话"  │    │
│  │  📜 午夜回顾 → 玩家觉得是"旁白总结"    │    │
│  └─────────────────────────────────────┘    │
│                                             │
│  裂缝状态：天摘下面具（≈2-5% 的游玩时间）    │
│  ┌─────────────────────────────────────┐    │
│  │  ⚡ 直接独白 → 玩家意识到"他在跟我说话" │    │
│  │  玩家无法回应                          │    │
│  │  独白结束后天回归沉默                  │    │
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

**触发条件**：

| 参数 | 值 |
|------|-----|
| 基础概率 | 2-5% / 天 |
| 数值窗口 | `benevolence` 在 0.3–0.7 区间 + `engagement` > 0.6 |
| 冷却 | 同一裂缝主题不重复触发；最短间隔 2 天 |
| 天数限制 | 第 1 天不触发（玩家还没建立世界观）；软上限后概率提升 |

**裂缝独白的方向建议**（示意，具体由 AI 根据上下文生成）：

| 方向 | 语调 | 示例 |
|------|------|------|
| 困惑 | 天对玩家行为的反应 | "你……今天没有照预言走。有意思。" |
| 关心 | 天的柔软面 | "你看起来累了。要休息一天吗？我可以让它慢一点。" |
| 警告 | 天的审判面 | "别走那条路。上一次有人走那条路，循环没有放过他。" |
| 自白 | 天的脆弱面 | "我也被困在这里。你是唯一让我觉得不那么孤独的人。" |

**设计意图**：裂缝不是频繁出现的 feature，而是**极其稀有的叙事地标**。玩家会截图、分享、讨论——它是社群传播的种子。同时，每次裂缝都让玩家重新审视之前所有看似"正常"的互动——那些是不是天也在看？

### 4.5 反抗系统 —— 玩家对天命的博弈

天预言之后，你可以主动去打破它。这不是 bug，是设计的核心张力。

```
    清晨预言发生
        │
        ├── 顺从天命 ──→ 获得 buff（金币/好感加成）
        │                benevolence 微升, engagement 微降
        │                天：「乖。」
        │
        └── 反抗天命 ──→ 承受代价（金币/好感惩罚 + 可能触发负面事件）
                         benevolence 微降, engagement 显著上升
                         天：「有意思。再来。」
                         某些隐藏结局只能通过持续反抗解锁
```

**反抗的行为方式**：
- 预言暗示你去 A 地点 → 你故意避开 A 去了 B
- 预言暗示某 NPC 今天会骗你 → 你故意完全信任他
- 预言说"今天不宜交谈" → 你密集地找每个 NPC 聊天

**天的反应梯度**：

| 反抗次数 | 天的态度变化 |
|---------|------------|
| 第 1 次 | 不回应（假装没注意） |
| 第 2-3 次 | 午夜回顾中多了一行微妙的评注 |
| 第 4-5 次 | 裂缝触发概率提升 |
| 6 次以上 | 天在某个裂缝中直接提及你是一个"有趣的挑战者" |

---

## 5. 实现难点分析

以下按严重程度排序，⭐ = 容易，⭐⭐⭐⭐⭐ = 极难。

### 难点一：叙事跨天连贯性 ⭐⭐⭐⭐⭐（致命级，核心风险）

**描述**：天在第 3 天埋的伏笔，第 7 天还能自然地回收吗？第 5 天的 Padwin 听起来还像第 1 天的 Padwin 吗？

**为什么难**：
- LLM 的上下文窗口有限（即使 200K tokens，也不能把 20 天的全部叙事都塞进去）
- 跨越多天的叙事线索需要外部追踪系统，而这套系统目前没有成熟的游戏行业方案
- 天的"记忆"和 NPC 的"记忆"是两套不同的记忆系统，需要协调

**缓解策略**：
- **分层摘要机制**：每天结算后，AI 生成三层摘要——(a) 今日关键事件 3-5 条，(b) 跨天伏笔状态更新，(c) NPC 关系弧线变化。后续天只传摘要，不传全文。
- **伏笔显式管理**：天在产生伏笔时必须在 JSON 中标记 `"foreshadowing_id"`；Godot 侧维护伏笔追踪表，在相关上下文出现时自动注入。
- **角色一致性锚**：每个 NPC 的角色卡（性格标签、说话方式、禁忌话题）作为 System Prompt 中最稳定的一部分，永远不被其他信息稀释。

### 难点二：结构化输出可靠性 ⭐⭐⭐⭐（高）

**描述**：天必须返回可解析的 JSON，但 LLM 偶尔会"自由发挥"。

**缓解策略**：
- 使用支持 Structured Output / Function Calling 的模型（如 Claude、GPT-4o），这套机制在 2025 年已较成熟
- 多层兜底：JSON Schema 校验 → 正则提取关键字段 → 默认值回退 → 静默日志
- 每个 AI 调用点配备**兜底文本数据库**（每种 NPC × 每种情绪基调 = 约 15-20 条预制文本）

### 难点三：存在感平衡 ⭐⭐⭐⭐⭐（极高）

**描述**：天太明显 → 玩家觉得被操控、烦；天太淡 → 玩家完全感受不到、游戏变成普通叙事冒险。

**为什么难**：
- 这不是参数调优问题，是**大量真人测试迭代**才能校准的主观体验
- 不同玩家的感知阈值不同——同一个力度，A 觉得"恰到好处"，B 觉得"我怎么没感觉到"

**缓解策略**：
- **三层存在感模型**：日常透明（95%）→ 隐约感知（午夜结算）→ 确凿存在（裂缝独白 + 结局）
- **玩家可选敏感度**：在游戏开始时通过一个隐晦的初始选择（如"你相信命运吗？"）来微调裂缝触发频率
- **以测试为准**：不预设"正确"的力度。做 5 人 × 10 天封闭测试，根据反馈迭代。

### 难点四：幻觉与边界控制 ⭐⭐⭐⭐（高）

**描述**：
- 天不能打破自己的规则（比如让时间倒流）
- 天不能修改事实层（循环的物理规则是固定的）
- NPC 不能说出不符合角色设定的话
- 内容安全：不能生成暴力/色情/政治敏感内容

**缓解策略**：
- System Prompt 级别的**硬约束列表**（不可违背的世界规则，用大写标注）
- NPC 角色卡中的**禁忌话题列表**（每个 NPC 绝对不能说的事）
- 输出后 **Godot 侧关键词过滤** 作为最后防线
- 安全内容：使用 API 自带的内容审核 + Godot 侧敏感词库

### 难点五：体验调校 ⭐⭐⭐⭐⭐（极高）

**描述**：让 20 天的循环保持"每一天都有意义"而不是"第 8 天开始重复无聊"。

**缓解策略**：
- **叙事节奏曲线**：前 4 天（建立世界 + 介绍 NPC）→ 第 5-12 天（真相浮现 + 关系深化）→ 第 13-16 天（张力高峰 + 终结倾向）→ 第 17-20 天（终局推进）
- **AI 根据天数动态调整内容密度**：早期更轻、后期更密
- **关键事件里程碑**：第 3 天、第 7 天、第 12 天各设一个"确保发生"的关键事件（不是 AI 自由生成的），作为叙事锚点

### 难度总览

```
                叙事连贯性   结构化输出   存在感平衡   幻觉控制   体验调校
                     │          │          │          │          │
难度:            ⭐⭐⭐⭐⭐   ⭐⭐⭐⭐    ⭐⭐⭐⭐⭐   ⭐⭐⭐⭐   ⭐⭐⭐⭐⭐
                     │          │          │          │          │
本质:             跨天 AI    工程化      主观体验     Prompt    叙事设计
                 记忆管理    可解决     需要测试     护栏      需要迭代
```

---

## 6. 技术架构方案

### 6.1 总体架构

```
┌──────────────────────────────────────────────────────────────────┐
│                       Godot 4.6 客户端                             │
│                                                                    │
│  ┌──────────┐  ┌───────────┐  ┌───────────┐                      │
│  │ EventBus │  │TimeManager│  │GameManager│                      │
│  │          │  │ 5 时段推进 │  │ 状态中心   │                      │
│  └────┬─────┘  └─────┬─────┘  └─────┬─────┘                      │
│       │              │              │                              │
│       └──────────────┼──────────────┘                              │
│                      │                                             │
│            ┌─────────┴─────────┐                                   │
│            │    AI Bridge      │  ← HTTP POST (async)              │
│            │  ┌─────────────┐  │                                   │
│            │  │ 请求组装     │  │  组装 context + prompt            │
│            │  │ 响应解析     │  │  解析 JSON → 触发信号             │
│            │  │ 兜底管理     │  │  JSON 失败 → 预制文本库           │
│            │  │ 记忆网关     │  │  读写记忆摘要                     │
│            │  └─────────────┘  │                                   │
│            └─────────┬─────────┘                                   │
│                      │                                             │
│  ┌───────────────────┴───────────────────┐                        │
│  │           记忆与状态层                   │                        │
│  │  ┌─────────────┐  ┌─────────────────┐ │                        │
│  │  │ NPC 记忆表   │  │ 伏笔追踪表       │ │                        │
│  │  │ (per NPC)   │  │ (foreshadowing) │ │                        │
│  │  └─────────────┘  └─────────────────┘ │                        │
│  │  ┌─────────────┐  ┌─────────────────┐ │                        │
│  │  │ 每日日志     │  │ 预制兜底文本库   │ │                        │
│  │  │ (JSON log)  │  │ (离线 fallback) │ │                        │
│  │  └─────────────┘  └─────────────────┘ │                        │
│  └───────────────────────────────────────┘                        │
└──────────────────────────┬───────────────────────────────────────┘
                           │ HTTPS
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                       AI 服务层（可替换）                            │
│                                                                    │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │                    「天」Agent（单一、统一）                   │   │
│  │                                                              │   │
│  │  System Prompt 结构：                                        │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │ 1. 角色定义：「你即世界」                              │   │   │
│  │  │ 2. 世界观 + 事实层（固定）                             │   │   │
│  │  │ 3. 三张 NPC 角色卡（性格 + 秘密 + 禁忌）               │   │   │
│  │  │ 4. 塔罗牌符号库（78 张 × 正逆位）                      │   │   │
│  │  │ 5. 叙事约束（时段节奏 + 存在感梯度 + 边界规则）         │   │   │
│  │  │ 6. 当前上下文（天数 + 数值 + 记忆摘要 + 今日主题）       │   │   │
│  │  │ 7. 输出格式（JSON Schema，按调用类型）                   │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  └────────────────────────────────────────────────────────────┘   │
│                                                                    │
│  API 适配层：OpenAI / Anthropic / 本地 Ollama / NobodyWho          │
└──────────────────────────────────────────────────────────────────┘
```

### 6.2 AI Bridge 核心接口

```gdscript
# ai_bridge.gd —— 概念接口

class_name AIBridge extends Node

# ── 四个面具，四个入口 ──

# 天命面具：早晨调用
func request_daily_oracle(
    day_number: int,
    benevolence: float,
    engagement: float,
    recent_history: Array[Dictionary],
    foreshadowing_registry: Array[Dictionary]
) -> OracleResult   # { prophecy[], theme_keywords[], suggested_events[], tone }

# 化身面具：NPC 对话调用
func request_npc_dialogue(
    npc_id: String,
    npc_affection: int,
    npc_memory: Array[Dictionary],
    player_input: String,       # 自由文本 或 空字符串
    suggested_topics: bool,     # true → 生成话题建议
    todays_theme: Dictionary
) -> DialogueResult  # { response_text?, topic_suggestions?[], emotional_shift, memory_update }

# 审判面具：午夜调用
func request_midnight_judgment(
    day_number: int,
    day_log: Dictionary,
    prophecy_text: Array[String],
    benevolence: float,
    engagement: float,
    truth_proximity: float,
    npc_changes: Dictionary
) -> JudgmentResult  # { narrative[], prophecy_fulfillment, tone_shift, tomorrow_seed, should_end }

# 裂缝面具：低概率触发
func request_crack_intervention(
    day_number: int,
    benevolence: float,
    engagement: float,
    trigger_context: String
) -> CrackResult      # { monologue_text, tone, aftermath_note }

# 终章
func request_finale(
    journey_summary: JourneySummary
) -> FinaleResult     # { title, narrative_text, epilogue_note }
```

### 6.3 记忆系统设计

```
三层记忆架构：

第一层：即时上下文（≈ 2K tokens）
├── 当前时段 · 当前 NPC（如果是对话）· 今日主题
└── 传入每次 AI 调用

第二层：短期摘要（Godot 本地 JSON / SQLite）
├── 每轮对话后 AI 生成 3-5 条关键摘要
├── 每天结束后 AI 生成"今日关键瞬间"摘要
├── 最多保留最近 3 天完整摘要，更早的做二次压缩
└── 在相关上下文出现时检索注入

第三层：伏笔 / 连续性追踪（Godot 本地）
├── 天在生成内容时标记 foreshadowing_id
├── Godot 维护伏笔状态表：open / partially_revealed / fulfilled
├── 每日结算时 AI 更新伏笔状态
└── 在叙事推进中自动注入当前 open 的伏笔信息
```

### 6.4 兜底策略

| 场景 | 策略 |
|------|------|
| **AI 调用超时** ( > 10s) | Godot 从预制文本库抽取匹配当前情境的兜底文本；玩家感知不到异常 |
| **AI 返回非 JSON / JSON 解析失败** | 三重恢复：JSON Schema 校验 → 正则提取关键字段 → 预制文本兜底 |
| **玩家无网络** | 内置完整对话树 + 234 条预生成塔罗解读 (78 牌 × 正逆位 × 3 种语境)；午夜结算用固定模板；裂缝系统关闭 |
| **Token 消耗过高** | 每次调用 context 控制在 ~2K tokens；长记忆走摘要压缩；对话轮次超 8 轮时自动收束 |
| **连续对话过快** | NPC 自然结束对话（"我需要想一想了。我们晚点再说？"） |
| **内容安全** | API 自带审核 + Godot 侧敏感词正则 + System Prompt 内的硬约束 |
| **模型切换** | AI Bridge 抽象了 API 层：Claude / GPT / Ollama / NobodyWho 可切换；NobodyWho 是最终本地化方案 |

---

## 7. 分阶段实现路径

### 阶段 0：验证期（目标：确认 AI 叙事可行性，1-2 周）

```
不做 Godot 集成。纯 Python 脚本 + Jupyter Notebook。

验证内容：
├── 天的 System Prompt 输出一致性（同样输入跑 10 次，输出偏差有多大？）
├── NPC 角色扮演稳定性（Padwin 说 20 轮不崩人设的概率？）
├── JSON 结构化输出可靠性（100 次调用，解析成功次数？）
├── 塔罗解读的文学质量（5 张牌 × 5 种语境 = 25 条解读，人工评分）
└── 裂缝独白的"神来之笔"率（随机抽样评估）

成功标准：
├── JSON 解析成功率 > 95%
├── NPC 人设一致性 > 85%（人工评估）
└── 塔罗解读文学质量评分 > 3/5（至少 3 人评估）
```

### 阶段 1：骨架期（目标：Godot 跑通 AI 对话循环，2-3 周）

```
实现内容：
├── AI Bridge 基础 Node（HTTPRequest 封装）
├── GameManager 扩展：benevolence / engagement / truth_proximity
├── 单 NPC 对话界面：话题建议 + 自由输入框
├── 对话记忆：per-NPC 摘要存储与检索
├── 预制兜底文本库（15 条 / NPC）
├── Morning → NPC 对话 → Midnight 的单天循环跑通
└── 离线模式：内置对话树可玩

产出物：一个可玩的单天 Demo（1 个 NPC，无塔罗，无裂缝）
```

### 阶段 2：血肉期（目标：全系统跑通，4-6 周）

```
实现内容：
├── 塔罗系统（78 张牌库 + AI 解读 + 预言映射）
├── 三个 NPC 全部接入
├── NPC 自主活动轨迹系统
├── 午夜审判系统（回顾叙事 + 终结评估）
├── 伏笔追踪表
├── 跨天记忆系统
├── 天数上限与终结逻辑
└── 裂缝系统（概率触发 + 独白生成）

产出物：3-5 天可玩 Build（含塔罗 + 完整 NPC + 午夜 + 裂缝概率）
```

### 阶段 3：打磨期（目标：叙事质量 + 体验调校，6-8 周）

```
实现内容：
├── 5 人 × 完整 20 天封闭测试
├── 天的存在感平衡迭代（依据测试反馈）
├── 叙事节奏曲线校准
├── 裂缝触发频率与时机调优
├── 终章生成质量评估与改进
├── NPC 对话深度扩展（关键事件触发对话）
├── 反抗天命的完整博弈回路
├── 本地模型适配与性能优化（NobodyWho）
└── UI 美术 / 音效 / 氛围打磨

产出物：完整可发布 Build
```

---

## 8. 我的建议

### 8.1 立即开始验证期

不要等到架构完美才开始。**现在就写一个 Python 脚本**，把「天」的 System Prompt 跑 50 次，看看：
- Padwin 在第 1 天和第 10 天听起来是否还是同一个人？
- JSON 解析成功率到底是多少？
- 塔罗预言的诗意度够不够？

这些数据会决定接下来的架构要不要大改。验证期是成本最低的纠错机会。

### 8.2 把「天的错误」设计成 feature

AI 偶尔会产生不符合预期的输出。与其花大量精力去消除这 1-5% 的偏差，不如给这 1-5% 一个**叙事解释**：

> 天不是完美的。天也会困惑、会出错、会情绪波动。
> 如果某天午夜的回顾文本突然变得驴唇不对马嘴——
> 那不是 bug，是天的裂缝以一种更隐蔽的方式出现了。

这极大地降低了 AI 幻觉对体验的破坏力，同时增加了神秘感。

### 8.3 裂缝系统是护城河，必须全力打磨

在 AI 叙事游戏越来越卷的 2025-2026 年，**裂缝独白**是这个游戏最容易出圈的东西：
- 玩家不知道什么时候会发生
- 每一次都是独一无二的（AI 生成）
- 独白内容直接呼应玩家之前的实际行为
- 玩家无法回应——这种**被动的震撼**是极强的传播力

建议在阶段 2 投最多的时间在裂缝的触发条件校准和独白质量打磨上。

### 8.4 话题建议比自由输入更重要

根据策划的"门面+暗门"设计——**90% 的玩家会只用话题建议**。因此：

- 话题建议的生成质量是 NPC 交互体验的**决定性因素**，不是自由输入
- 话题建议必须：反映 NPC 当前情绪、呼应今日主题、暗示 NPC 的秘密、推动叙事前进
- 自由输入是加分项，话题建议是基本盘。资源分配应该与此匹配

### 8.5 考虑做「天意值」可视化

虽然 §4.3 的点子⑤提出了"去 UI 化"的极致碎片叙事方向，但我**不建议在这个维度上做到极致**。原因是：

- 玩家需要有一些"锚"来感知天的态度变化，完全去 UI 会让大多数玩家迷失
- 建议保留 **一个极简的天意指示器**——不是数值条，而是抽象的视觉符号（比如：天色的微妙变化、主菜单图标的渐变），让有经验的玩家可以"读取"天的态度，但不对新人构成信息负担
- 核心原则：**可感知，不可量化**

### 8.6 NobodyWho 是最终的本地化答案，但先用云端跑通

- 开发阶段：用 Claude / GPT-4o 验证叙事质量（它们的中文文学能力最强）
- 测试阶段：用 NobodyWho + Qwen3-4B 测试本地模型是否达到可接受质量
- 发布阶段：双模式——默认云端（高质量），可选本地（隐私 / 无网络）
- 如果本地模型达不到叙事质量标准，优先保证云端体验，本地模式降级为预制文本库

---

## 附录：关键数值定义

| 参数 | 范围 | 初始值 | 说明 |
|------|------|--------|------|
| `benevolence` | -1.0 ~ +1.0 | 0.0 | 天对玩家的善意；负值 = 敌意，正值 = 慈爱 |
| `engagement` | 0.0 ~ 1.0 | 0.3 | 天对玩家的兴趣；低 = 冷淡，高 = 痴迷 |
| `truth_proximity` | 0.0 ~ 1.0 | 0.0 | 玩家对循环真相的理解深度 |
| `prophecy_resistance` | 0 ~ ∞ | 0 | 累计反抗预言次数 |
| `npc_affection` | 0 ~ 100 | per NPC | NPC 对玩家的好感度 |
| `days_elapsed` | 1 ~ 20 | 1 | 当前循环天数 |
| `foreshadowing_open` | 0 ~ N | 0 | 当前未回收的伏笔数 |

---

## 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-07-29 | v0.1 | 初始版本：三重引擎、天独立 Agent、三个延伸方向、技术架构 |
| 2026-07-29 | v0.2 | 整合策划五条决策 → 重构为「天」统一四面具架构；新增实现难点分析、分层记忆方案、三阶段实现路径、六条建议、关键数值定义 |
