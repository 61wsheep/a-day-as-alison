# 玩法扩展 · 设计与工程实现文档 v0.1

> 状态：待审核。审核通过后按里程碑顺序开发，每个里程碑完成后演示确认再进入下一个。
> 代码仓库：`D:\AIGameBuild`（main 分支），Godot 项目目录 `成为艾莉森的一天/`。
> 本文档的所有文件路径均相对 Godot 项目根目录。

---

## 0. 总览

### 0.1 四个新玩法与难度排序

| 排序 | 玩法 | 难度 | 一句话 |
|---|---|---|---|
| ① | 采集蘑菇、浆果（扩展为物品采集+售卖） | ★★ | 已有蘑菇采集雏形，改造为"进背包→找买家售卖" |
| ② | 修建房屋的低价劳动力（动森式） | ★★★ | 接订单→攒钱→房屋分阶段升级，依赖订单系统 |
| ③ | 塔罗占卜支线（为居民答疑解惑） | ★★★★ | 依赖订单/任务系统 + 对话系统 conditions/effects 扩展 |
| ④ | 草药瓶制作（无配方、条件定价） | ★★★★★ | 依赖背包 + 天气系统 + 时间系统 + 定价算法 |

### 0.2 依赖关系（决定开发顺序）

```
物品/背包系统 ──┬── ① 采集售卖
               ├── ③ 占卜支线（任务道具）
               └── ④ 草药瓶（材料消耗）
订单/任务系统 ──┬── ② 建房劳动力
               └── ③ 占卜支线
天气系统 ────────┴── ④ 草药瓶定价
```

**结论：先修两座底座（背包、订单），玩法按 ①→②→③→④ 落地。天气系统排在 ④ 之前。**

### 0.3 里程碑

| 里程碑 | 内容 | 交付物 |
|---|---|---|
| M1 | 背包系统 v0.1 + 采集改造（蘑菇/浆果进背包）+ 售卖 | 可采、可看背包、可卖钱 |
| M2 | 订单/任务系统 v0.1 + 占卜支线（③） | 居民发布占卜委托，完成后好感度+金币 |
| M3 | 天气系统 v0.1 | 每日天气影响世界表现与后续定价 |
| M4 | 建房劳动力（②） | 攒钱→房屋阶段升级→视觉变化 |
| M5 | 草药瓶制作（④） | 采草药→制作→条件定价售卖 |

M2 与 M4 可互换顺序；M5 必须最后。

---

## 1. 现状盘点（已存在、可直接复用的东西）

写代码前已核对过仓库，以下系统**已经存在**，不要重复造轮子：

| 已有 | 位置 | 复用方式 |
|---|---|---|
| 金币 / 好感度 / 线索 / 剧情标记 | `scripts/autoload/game_manager.gd`（gold, npc_affection, clues_found, flags） | 背包数据也挂这里或新建 autoload |
| 每日塔罗抽牌 + 日运 luck | `scripts/ui/tarot_ui.gd` + `resources/tarot/major_arcana.json`（22 张大阿尔卡那，luck ∈ {-1,0,1}） | ④的定价可直接读 `GameManager.daily_luck`；③复用同一副牌 |
| 蘑菇采集（碰到即拾取、直接加金币） | `scripts/collectible.gd`（Area2D，gold_value，循环重置时由 `game.gd._refresh_collectibles()` 重新出现） | M1 改造点：拾取改为进背包而不是直接加钱 |
| 时段系统 | `scripts/autoload/time_manager.gd`（morning/afternoon/evening…） | 天气/制作时长基于它 |
| 事件总线 | `scripts/autoload/event_bus.gd`（gold_changed / day_started / toast / clue_found / tarot_drawn 等信号） | 新系统一律走 EventBus 发信号，不直接互调 |
| 对话 JSON（conditions/effects 机制） | `resources/dialogues/*.json` | ③的支线对话沿用此格式扩展 |
| AI 对话会话 | `scripts/systems/ai_dialogue_session.gd`、`ai_bridge.gd` | ③可选：AI 生成居民困惑的"后日谈"文本 |
| 状态面板（Tab 打开） | `scripts/ui/status_panel.gd` | 背包 UI 可作为其新分页，或独立按键 I |
| 租房 UI 模式 | `scripts/ui/rental_ui.gd`（CanvasLayer 弹窗范式） | 订单板、制作台 UI 照搬此范式 |
| 新瓦片集 | `assets/tilesets/forest_plaza.png`（树/灌木/花/长椅/路灯/告示牌/树桩/原木） | 浆果丛可用灌木改色；摊位用原木+告示牌拼 |
| 日循环重置 | `game.gd` 的 loop_reset / _refresh_collectibles | 采集点每日刷新沿用此钩子 |

**已知限制：当前无存档系统**（GameManager 注释已标明 user:// 持久化随存档系统排期）。M1 背包只做跨天内存保留，重启游戏不保留。文档涉及的"持久化"均指跨天（loop_reset 不清空），存档另行排期。

---

## 2. 底座系统一：物品 / 背包（M1 前半）

### 2.1 数据定义：`resources/data/items.json`（新建）

```json
{
  "items": [
    {
      "id": "mushroom",
      "name": "森林蘑菇",
      "kind": "forage",
      "base_price": 10,
      "stack_max": 20,
      "icon": { "atlas": "res://assets/tilesets/forest_plaza.png", "region": [x, y, w, h] },
      "desc": "藏在草丛里的普通蘑菇，镇上的常客。"
    },
    {
      "id": "berry",
      "name": "红浆果",
      "kind": "forage",
      "base_price": 6,
      "stack_max": 30,
      "icon": { "atlas": "...", "region": [...] },
      "desc": "酸甜的小浆果，索拉雅偶尔会买来酿酒。"
    },
    {
      "id": "herb_moonleaf",
      "name": "月光草",
      "kind": "herb",
      "properties": ["cool", "night"],
      "base_price": 4,
      "stack_max": 20,
      "desc": "只在傍晚后散发微光的草药。"
    },
    {
      "id": "potion_blank",
      "name": "未命名草药瓶",
      "kind": "potion",
      "base_price": 0,
      "stack_max": 5,
      "desc": "封存着植物的性情——它的价值由酿它的那一刻决定。"
    }
  ]
}
```

字段约定：
- `kind`: `forage`（采集品）/ `herb`（草药）/ `potion`（成品）/ `quest`（任务道具，不可卖）。
- `properties`：草药的"性质"标签，④的定价算法读它。首批标签集：`warm / cool / night / day / dry / damp`。
- `icon.region`：程序切图，与 SceneLayout 里现有道具包围盒做法一致；新图标先用瓦片集改色占位，正式美术走外包清单（见 §7）。

### 2.2 运行时：`scripts/autoload/inventory.gd`（新建 autoload）

```gdscript
extends Node
## 背包：Dictionary[item_id] = count。跨 loop_reset 保留，重启不保留（v0.1）。

signal changed(item_id: String, new_count: int)

var items: Dictionary = {}

func add(item_id: String, count: int = 1) -> bool   # 返回 false = 超 stack_max 拒收
func remove(item_id: String, count: int = 1) -> bool
func count_of(item_id: String) -> int
func all_items() -> Dictionary                       # UI 用快照
```

- 在 `project.godot` 的 autoload 列表注册（顺序在 GameManager 之后）。
- 物品元数据通过 `scripts/systems/item_db.gd`（新建，普通类）从 items.json 读取，提供 `get_item(id) -> Dictionary`。

### 2.3 背包 UI：`scripts/ui/inventory_panel.gd`（新建）

- 范式照搬 `rental_ui.gd`：CanvasLayer，打开时暂停玩家移动（`get_tree().paused` 方案与 status_panel 保持一致——实现时先读 status_panel 的锁定方式复用）。
- 布局：左侧网格（每格：图标 + 数量），右侧详情（名称/描述/单价）。
- 打开按键：`I`，同时并入 status_panel 的第 2 个分页（Tab 内左右切换）——二选一，实现时选改动小的；**倾向独立 I 键**，status_panel 不动。
- 打开/关闭/对话中锁定，均发/听 EventBus 信号；新增信号：

```
# event_bus.gd 追加
signal inventory_opened()
signal inventory_closed()
signal item_sold(item_id: String, count: int, price_total: int)
```

### 2.4 采集改造：`scripts/collectible.gd`

- `@export var item_id: String = "mushroom"`（替换现有 gold_value 直加逻辑）。
- `_on_body_entered` → `Inventory.add(item_id)` 成功才消失；失败（满组）弹 toast「背包满了」并保留。
- 场景里新增浆果丛采集点：`scenes/` 中森林区（现有区域见 SceneLayout 常量）摆 3–5 个，视觉 = 灌木贴图改红色调（程序 `modulate`，不等新美术）。
- 每日刷新沿用 `game.gd._refresh_collectibles()`，无需改动。

### 2.5 售卖：索拉雅收购（M1 最简单的出口）

- 与索拉雅对话新增选项「卖点东西」（写在 `resources/dialogues/soraya.json`，conditions: 背包有 forage 类物品）。
- 进入简易售卖界面（`scripts/ui/sell_panel.gd`，列表多选→一口价卖出）：`price = base_price × (1 + 0.2 × daily_luck)`，整数化。塔罗日运影响售价的设定从"蘑菇"泛化为"所有采集品"，`tarot_ui.gd` 里相关文案同步改。
- 成交：`Inventory.remove` + `GameManager.add_gold` + EventBus `item_sold`。

### 2.6 M1 测试

- `scenes/tests/test_inventory.tscn`（新建）：add/remove/stack_max/满组拒收。
- `scenes/tests/test_forage_sell.tscn`（新建）：模拟拾取蘑菇→索拉雅售卖→金币正确增加、物品正确扣减、daily_luck 系数生效。
- 回归：现有 test_sleep_no_house / test_status_panel / sim_multi_day 必须全过。

---

## 3. 底座系统二：订单 / 任务（M2 前半）

### 3.1 数据定义：`resources/data/quests.json`（新建）

```json
{
  "quests": [
    {
      "id": "tarot_padwin_01",
      "title": "帕德温的旧怀表",
      "giver": "padwin",
      "kind": "tarot_reading",
      "unlock": { "day_min": 3, "affection_min": { "padwin": 55 } },
      "brief": "帕德温总在黄昏盯着怀表发呆。为他占卜一次，替他问问'那一天'。",
      "steps": [
        { "type": "tarot_reading_for", "npc": "padwin",
          "question": "我该放下那一天吗？" }
      ],
      "rewards": { "gold": 30, "affection": { "padwin": 10 }, "flags": ["padwin_watch_resolved"] }
    },
    {
      "id": "build_house_01",
      "title": "搭个像样的屋顶",
      "giver": "notice_board",
      "kind": "build_stage",
      "unlock": { "treehouse_owned": true },
      "brief": "广场告示牌：招低价劳动力，自备材料修屋顶。",
      "steps": [
        { "type": "deliver", "item": "log", "count": 4 },
        { "type": "pay", "gold": 50 }
      ],
      "rewards": { "flags": ["house_stage_2"] }
    }
  ]
}
```

step.type 枚举（v0.1 只实现这五种）：`tarot_reading_for` / `deliver` / `pay` / `talk_to` / `gather`。

### 3.2 运行时：`scripts/autoload/quest_manager.gd`（新建 autoload）

```gdscript
extends Node
signal quest_offered(quest_id: String)
signal quest_updated(quest_id: String)
signal quest_completed(quest_id: String)

var active: Dictionary = {}      # id -> {step_index, progress}
var completed: Array[String] = []

func available_quests() -> Array # 按 unlock 条件过滤（day/affection/flags/treehouse_owned）
func accept(quest_id: String) -> void
func report(event: Dictionary) -> void   # 各系统上报：{"type":"deliver","item":"log",...}
func is_completed(quest_id: String) -> bool
```

- `unlock` / `rewards` 求值器读 GameManager（day、npc_affection、flags、treehouse_rented）。
- 奖励落地：gold → add_gold；affection → npc_affection；flags → flags[k]=true；发 EventBus `clue_found` 同款 toast。

### 3.3 入口 UI：告示牌订单板

- 场景：广场告示牌道具（已有贴图）加交互区 → 打开 `scripts/ui/quest_board_ui.gd`（rental_ui 范式）。
- 列表 = `available_quests()`，显示标题/简介/奖励；接受后进入「进行中」页。
- NPC 头上的任务提示（❗）：v0.1 不做头顶图标，用对话选项「有什么需要帮忙的吗？」兜底，避免场景编辑大改。

### 3.4 M2 测试

- `scenes/tests/test_quest.tscn`：解锁条件求值、accept/report/complete 全流程、奖励正确发放、重复完成被拒。

---

## 4. 玩法③：塔罗占卜支线（M2 后半）

### 4.1 体验流程

1. 满足 unlock 后，索拉雅/帕德温/主教对话出现新选项「需要我为你占卜吗？」（写进各自 dialogue json，conditions 挂 quest active 状态）。
2. 进入占卜界面：复用 `tarot_ui.gd` 的抽牌演出，但改为「为对方抽一张」——从 major_arcana.json 抽牌后，**不是**日运结算，而是进入解读对话。
3. 解读对话两段式：
   - **数据段**（必做）：每张牌为每个 NPC 预写一句解读（数据结构见 4.2），离线可玩。
   - **AI 段**（可选增强）：走现有 `ai_dialogue_session.gd`，prompt 注入「你刚抽到 X 牌，向 NPC 提问者解读」，失败回落数据段。**v0.1 只做数据段**，AI 段留接口。
4. 解读完成 → `quest_manager.report({"type":"tarot_reading_for","npc":...})` → 发奖励 → 解锁该居民的一段「生活故事」文本（存 quests.json 的 `story_unlock` 字段，状态面板新增「故事」分页展示，或先在对话里回放——**v0.1 用对话回放，不动 status_panel**）。

### 4.2 数据扩展

- `resources/tarot/major_arcana.json` 每张牌追加（可选字段，缺失时回落通用解读）：

```json
{ "id": "tower", ...,
  "npc_readings": { "padwin": "倒塌的不一定是家，也可能是困住你的那一天。",
                    "soraya": "...", "cactus_bishop": "..." } }
```

- 每位居民首批各 1 条支线（共 3 条 quests），文案量：每支线 ≈ 15 行对话 + 1 段故事（约 150 字）。**文案走文案同学流程，程序先放占位文本并在文档/资产清单标注。**

### 4.3 测试

- `scenes/tests/test_tarot_quest.tscn`：数据段全流程（抽牌→解读→report→奖励→故事解锁）；牌缺失 npc_readings 时回落通用解读。

---

## 5. 底座系统三：天气（M3）

### 5.1 设计

- 每天 `day_started` 时 roll 一次天气：`sunny / cloudy / rainy / foggy`，权重 [40, 30, 20, 10]；`daily_luck` 微调权重（luck>0 时 sunny+10、foggy-10）。
- 运行时：`scripts/autoload/weather_manager.gd`（新建），暴露 `current_weather`、`temperature`（由天气+时段查表：如 sunny×afternoon=28℃，rainy×evening=16℃）。
- 世界表现 v0.1（轻量，全程序实现零美术）：
  - rainy：全屏半透明蓝灰 ColorRect + 简单粒子（GPUParticles2D 雨天，参数写死）；
  - foggy：低透明度白色渐变遮罩；
  - HUD 时钟旁加天气小图标（emoji 占位 ☀☁🌧🌫，正式图标入资产清单）。
- EventBus 追加：`signal weather_changed(weather: String)`。

### 5.2 测试

- `scenes/tests/test_weather.tscn`：分布 sanity（roll 1000 次各天气比例在权重±5% 内）、温度查表正确、信号在 day_started 后发出。

---

## 6. 玩法②④：建房劳动力（M4）与草药瓶（M5）

### 6.1 建房劳动力（M4）

**体验**：买下/租下树屋后，告示牌刷出 build_stage 系列任务（3 阶段：屋顶→墙面粉饰→小花园）。每阶段 = 交材料（log/stone，新增采集点：树桩产 log、岩石产 stone——贴图已有）+ 付钱「请低价劳动力」。完成阶段 → 房屋外观变化。

**实现路径**：
- quest kind=`build_stage`，steps 用已有 `deliver` + `pay`，**零新任务类型**。
- 房屋阶段视觉：树屋场景预置 3 套外观节点（stage1/2/3），`game.gd` 按 flags 切换 visible——v0.1 用「瓦片集拼的棚子→木屋→带花园」三张贴图占位（程序拼接），正式 sprite 入资产清单。
- 新增采集点：树桩（log）、岩石（stone），复用 collectible.gd + item_id，与 M1 完全同构。

**测试**：`test_build_stage.tscn`——材料不足时步骤不推进；完成 stage 后外观节点切换正确。

### 6.2 草药瓶制作（M5）

**体验**：森林新增草药采集点（月光草/暖阳花/雨菇 3 种，各带 properties 标签）。树屋里新增「制作台」交互：任选 1–3 株草药投入 → 选择「酿制时长」（短/中/长，消耗时段）→ 产出草药瓶。**无配方**：价格不由组合表决定，而由酿制时刻的世界状态决定。

**定价算法（v0.1 定稿，可直接实现）**：

```
base     = Σ(投入草药 base_price) × 1.5
性质契合  = 草药 properties 与当前天气/时段匹配数 × 8
          （cool↔rainy/foggy、warm↔sunny、night↔evening、day↔morning、damp↔rainy、dry↔sunny）
时长系数  = 短 1.0 / 中 1.25 / 长 1.5
温度系数  = 1 + (temperature - 18) × 0.01      # 18℃为基准，clamp [0.85, 1.15]
运气      = 1 + daily_luck × 0.1
扰动      = randf_range(0.9, 1.1)
price    = clamp(round((base + 性质契合) × 时长系数 × 温度系数 × 运气 × 扰动), 5, 200)
```

- 运行时：`scripts/systems/potion_craft.gd`（新建普通类，纯函数 `calc_price(herb_ids, weather, temperature, duration, luck, rng) -> int`，可单测）。
- 产出物 `potion_blank` 进背包，名称在售出时按最高匹配性质生成（如「雨夜草药瓶」——名称模板表放 items.json 旁 `potion_names.json`）。
- 售卖出口同 M1（索拉雅收购，kind=potion 走另一句台词）。
- UI：制作台面板（rental_ui 范式）：左侧背包草药多选，右侧时长三按钮 + 「开始酿制」，进度用时段推进实现（酿制中锁定交互，推进 1–2 个时段后完成弹窗）。

**测试**：`test_potion.tscn`——纯函数定价各系数单测（喂固定 rng）；匹配数计算；clamp 上下限；酿制流程时段推进。

---

## 7. 资产清单（需要额外开发/外包的）

### 7.1 程序可占位（不阻塞开发）

| 用途 | 占位方案 |
|---|---|
| 浆果丛 | forest_plaza 灌木 modulate 红色 |
| 草药×3 | 花贴图 modulate（紫/黄/蓝） |
| 树桩/岩石采集点 | 新瓦片集已有贴图，直接摆 |
| 雨/雾天气 | 粒子 + 遮罩，程序实现 |
| 房屋三阶段 | 瓦片集程序拼接 |
| 天气图标 | emoji 占位 |

### 7.2 需要新美术（走美工外包流程，与玩法并行不阻塞）

| 资产 | 规格 | 用途 | 优先级 |
|---|---|---|---|
| 物品图标 ×8（蘑菇/浆果/3 草药/草药瓶/log/stone） | 32×32 png，透明底 | 背包网格 | 高（M1 就要） |
| 背包 UI 皮肤（九宫格面板+格子） | 参照现有 UI 色调 | 背包/售卖/制作台复用 | 高 |
| 天气图标 ×4 | 16×16 | HUD | 中 |
| 房屋三阶段 sprite | 与树屋现尺寸对齐 | M4 | 中 |
| 制作台道具贴图 | 瓦片集风格 32×32 | M5 | 低 |

### 7.3 需要新文案（走文案同学流程）

| 内容 | 量级 | 里程碑 |
|---|---|---|
| 物品名称+描述 ×8 | 每条 1–2 句 | M1 |
| 占卜支线 ×3（对话+故事） | 每条约 15 行+150 字 | M2 |
| 通用牌解读 22 张（npc_readings 缺省回落用） | 每张 1 句 | M2 |
| 订单板公告文本 ×5 | 每条 2–3 句 | M2/M4 |
| 草药瓶名称模板 ×6 | 模板句 | M5 |

### 7.4 不需要新场景

全部玩法落在现有广场/森林/树屋场景内，**零新场景**。

---

## 8. 里程碑拆解（可直接照做的任务卡）

### M1 背包+采集售卖（预计最大的一批，先做）

1. `resources/data/items.json` + `scripts/systems/item_db.gd`
2. `scripts/autoload/inventory.gd` + 注册 autoload + EventBus 信号
3. 改造 `collectible.gd`（item_id 化）+ 森林摆浆果点
4. `scripts/ui/inventory_panel.gd`（I 键）
5. `scripts/ui/sell_panel.gd` + soraya.json 售卖选项 + tarot_ui 售价泛化文案
6. 测试 ×2 + 回归

### M2 订单+占卜支线

1. `resources/data/quests.json` + `quest_manager.gd`
2. `quest_board_ui.gd` + 告示牌交互
3. 三 NPC 对话 json 加占卜选项 + major_arcana npc_readings
4. 数据段解读流程 + 故事回放
5. 测试 ×2 + 回归

### M3 天气

1. `weather_manager.gd` + 温度表 + EventBus
2. 雨/雾表现 + HUD 图标
3. 测试 ×1 + 回归

### M4 建房

1. log/stone 物品 + 树桩/岩石采集点
2. 3 个 build_stage 任务数据
3. 树屋三阶段外观节点 + flags 切换
4. 测试 ×1 + 回归

### M5 草药瓶

1. 3 草药物品+采集点、properties 标签
2. `potion_craft.gd` 纯函数 + `potion_names.json`
3. 制作台交互 + UI + 时段推进
4. 测试 ×1 + 回归

---

## 9. 风险与说明

1. **无存档**：所有新系统（背包/任务/天气/房屋阶段）v0.1 均为内存态，重启丢失。存档系统建议单独立项，届时各 Manager 补 `serialize()/deserialize()` 即可（本设计的数据结构已按可序列化设计）。
2. **AI 依赖**：③的 AI 解读段是增强不是必需，数据段兜底保证离线可玩；与现有 AI 断联回落策略一致。
3. **文案依赖**：占位文案会先进游戏，外包文案到位后替换 json 即可，不动代码。
4. **审核节点**：每个里程碑完成后我会演示并等确认，再进下一个。
