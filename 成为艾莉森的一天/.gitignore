# Godot 4+ specific ignores
.godot/
/android/

# macOS system files
.DS_Store

# Bundled Godot docs (239MB, re-downloadable via skill setup)
.codebuddy/skills/godot-docs-search/references/

# AI API 密钥（本地密钥不进版本库）
ai/api_key.txt

# Cainos 素材原始压缩包（cainos/ 目录已提交，zip 不入库）
assets/tilesets/cainos.zip

# ============================================================
# 美术源文件与压缩包 —— 只入库导出后的 PNG
# ------------------------------------------------------------
# 为什么必须挡在入口：素材一旦进了 Git 历史，删掉文件也没用，
# 历史里那一份永远在，仓库只增不减。挡在这里是唯一干净的做法。
# （已提交的大文件能靠 git filter-repo 救回，但代价是重写全仓库历史、
#   所有协作者必须重新克隆 —— 不是能随手做的事。）
#
# 美工同学注意：这些文件不会出现在 GitHub Desktop 的改动列表里，
# 这是正常的，不是你操作错了 —— 你只需要交 PNG。
# ============================================================
*.psd
*.psb
*.aseprite
*.ase
*.kra
*.xcf
*.clip
*.csp
*.procreate
*.blend
*.blend1
*.tif
*.tiff

# 交付压缩包（走网盘交接，不入库；内容由主程归档到 assets/ scenes/）
*.zip
*.7z
*.rar
