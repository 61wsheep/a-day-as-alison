# AI 回复慢 —— 现在是什么状况、该怎么治（通俗版）

> 写给非技术视角的讨论纪要。想把"游戏对话为什么越来越慢、值不值得做 RAG / prompt 分层"这件事讲明白。
> 所有"实测"数字来自对真实接口的一次次实测（对话实验室 DialogueLab + 游戏内探针），不是拍脑袋。

---

## 一句话结论

游戏每跟你聊一句，都要把一份 **"约一万字的说明书"** 寄给云端 AI，然后**干等着它把整段话写完才显示**。慢主要由两部分组成：一部分是"它读题"，另一部分是"它写字"。**现在的大头是写字（占了 7 成以上的等待时间），不是读题。** 所以：

- 想让人"感觉快"，第一优先是**让字一边生成一边显示（流式）**——实测第一个字 1 秒左右就到，只是现在被憋着不显示；
- 想让人"真省"、并顺带修一个**剧情剧透 bug**，第二优先是**别把 NPC 用不到的内容塞给 NPC**；
- 你说的"治本"，正确方向不是"每次写短一点"，而是**让"每次要看的内容"不再随文案总量一起无限涨**——靠"按需取用"而不是"把书越写越薄"。

---

## 一、现在到底是怎么回事？（先打个比方）

把每次 AI 对话想象成这个流程：

> 你（游戏）给远方一位**新来的临时工**寄了一本书，请他回答一个问题。
> 他每次都要**从第一页开始把整本书读完**，然后**一个字一个字给你写回信**。
> 更麻烦的是：他**记不住上次**，所以每次都得把整本书重新寄一遍、重读一遍。

具体到我们的游戏，一次普通对话大约是这样一包东西：

| 组成部分 | 体量 | 是什么 |
|---|---|---|
| 系统说明书 | ≈ 8,500 字 | 「天」的世界规则书 ~2,100 字 ＋ 这个 NPC 的完整人物卡 ~6,300 字 |
| 本轮现场内容 | ≈ 1,800 字 | 今天第几天、好感多少、最近聊了啥、玩家刚说的话 |
| **合计每轮** | **≈ 1 万字** | 每次点对话，都要把这包东西重发一遍 |

我们实测了两次真实对话的耗时：

- 开场第一句：**等整段话回来 = 7.7 秒**
- 第二句：**5.5 秒**
- 但**第一个字其实 1.0~1.7 秒就到了**（引擎不支持流式，所以这些时间你全在干等，什么都看不见）

也就是说：**如果你能"看见"它开始写字，等待体感立减七成。** 剩下那几秒，是它把话说完整的时间——这一部分你省不掉（除非让它写少点，或用写得快的模型）。

还有一个容易误会的地方，先说破：云端有种"缓存"机制，可以让你**省钱**、让**读题**变快。但实测发现——**缓存命中 ≠ 回复变快**。有一次缓存明明全命中了，整轮还是 22.6 秒才回，因为绝大部分时间都花在"它写那 486 个字"上了。所以缓存救不了"等待"，别指望它。

---

## 二、慢的两半：读题 vs 写字

把一次回复的耗时拆成两段，这是全文最关键的一张图：

```
【读题】←───────→【写字】←────────────→
 电脑把一万字说明书
 从头扫一遍，搞懂背景         电脑一个字一个字把回答"写"出来
 (这是 CPU 一次性干完的)      (这是"蹦字速度"，目前 ~20~34 字/秒，最慢的一环)
```

- **读题（prefill）**：说明书越长越慢。以后文案越写越多（故事背景、主线剧情…），这部分会**持续变慢**。
- **写字（生成）**：**回复写得越长越慢**，而且它不受说明书长短影响——哪怕你只寄一页纸，它该写 500 字还是得慢慢写 500 字。**这一半现在占大头。**

这就是为什么上一轮我们做的提速（已经提交入库了）长这样：
- 单轮回复上限 **1024 → 512 字**（少写字）
- 保留的聊天上下文 **10 轮 → 5 轮**（少读题）
- 提示词里明确"话要短"

方向没错，但你能感觉到"效果不明显"——因为**它治的是读题和写作量，而大头本来就不在写作量上**。光砍到 512，它还是得一个字一个字蹦出来给你等。

---

## 三、现在最值得做的三件事

### 第 1 步：流式输出（体感收益最大）

**做法**：让 AI 一边生成一边把字送到屏幕上（现在是要等整段写完才一次性显示）。
**收益**：玩家 1~2 秒内就看到"它开始说话了"，不再是对着空白干瞪 5~8 秒。这是**立竿见影**的一项。
**困难**：
- 现在回复是"一整块 JSON"（含 AI 说的话 + 好感变化 + 记忆，要程序解析）。边走边显示 = 要处理"半截话"，解析逻辑得重写，还要防"中途被截断导致解析失败"（流式会削弱"必须给合法 JSON"的约束，之前实测就出过截断 → 整轮作废）。
- 钱不会省（按字数计费不变），只是把等待"藏"到了玩家眼前。
- Godot 这边接收方式要改。

**一句话：最难改、但最值得改的一项，建议最先立项。**

### 第 2 步：别把 NPC 用不到的东西交给 NPC（省钱 + 顺手修剧透 bug）

**问题**：现在**每一轮 NPC 对话**都背了一整套"天的规则书"（约 2,100 字）——里面写着"这个小镇其实是天的收容所""循环是真实存在的""第 20 天循环终结"……这些**是世界级剧透**，只该"天"自己知道。结果：
- NPC 每轮白白多读 2,100 字（读题变慢）；
- 更糟的是，NPC 其实**不该知道这些**——它可能"说出不该说的话"，破坏设定。

**做法**：把"天的规则书"拆开。NPC 对话只带 NPC 需要的部分；只有塔罗预言、午夜审判、裂缝独白这些"天亲自说话"的时刻，才带完整的"天"规则。

**收益**：每轮对话读题直接少 ~2,000 字；顺带修掉"NPC 偶然剧透"的隐患。**这一项其实是目前"一次能省最多"的。**

### 第 3 步：治本——让"每轮必看的内容"不再随总量涨

你之前的担心是对的：人物设定 + 之后的故事背景、主线剧情一直加，**如果每次都整包塞进去，只会越来越慢**。

但治本不是"让 AI 每次少看点"，而是**改变结构**：

> 每次**必须带**的，只留一小份"永远不变的核心"（你是谁、说话腔调、红线）。庞大的背景、故事、秘密，全部改成**用到才取**：今天第几天 / 好感多少 / 你发现了哪条线索 / 你在哪个地点——符合条件才把对应那一段塞进去。

这跟你提到的 RAG 是一件事的两个版本：RAG 是"让 AI 按意思相似自动找段落"，门控/分层是"让游戏按规则决定取哪段"。**在"一局 20 天"这种量级下，规则门控就够用了**（游戏里已经有一层这样的记忆检索系统 MemoryQuery）。向量库那套要接 embedding 模型、要存向量、要多不少代码，在数据量长大到"塞不进门控规则"之前，收益抵不过复杂度——**可以先不做，等真涨到几千条再上。**

---

## 四、还有什么办法可以压延时？（对照表）

| 办法 | 省的是 | 收益 | 困难 / 代价 |
|---|---|---|---|
| **流式输出** | 体感 | 第一个字 1~2 秒出现，**体感最大** | 解析半截话、防截断，Godot 侧重写接收 |
| **输出限短**（512 上限 + "话短"提示） | 写字量 | 已做；中等 | 砍太狠 AI 变冷淡，要留余量 |
| **按需取用 / 门控**（第 2、3 步） | 读题量 | 每轮省 ~2,000 字起；随文案增长持续有效 | 可能"该带没带"→ AI 瞎编（见第五节） |
| **换更快的模型跑日常对话** | 写字速度 | 若快的模型 1 秒出字，体感很强 | 质量可能下降；需先试；"天"的审判/预言仍用最聪明的那个 |
| **减少"记忆/历史"重复注入** | 读题量 | 已做（10 轮→5 轮） | 记性变差，要靠 memory_update 补 |
| **预取 / 预热** | 等待 | 预测玩家下一步 → 提前发请求，点击时答案已到 | 复杂；可能白花钱；适合"确定性高的下一步" |
| **UI 缓解**（打字动画、音效、先过场） | 体感 | 把等待藏起来 | 治标不治本，但便宜 |
| **失败兜底 / 重试** | 稳定性 | 已做一半 | —— |

**一句话排序：流式（体感）> 别送天的规则给 NPC（省钱+修剧透）> 按需取用（防未来涨慢）> 换快模型（后议）。**

---

## 五、你问的关键问题：把"prompt 分层"交给 AI 自己，能不能做到"不多交也不少交"？

> 即：让 AI 自己决定"这段我用不到，别给我"——**做得到吗？**

**直接回答：做不到，也不该这么做。**

### 为什么 AI 自己决定不了？

回到那个比方——他是一位**每次都是新来的临时工**：

1. **他根本不知道书里有什么。** 你要他判断"哪一章我用不上"，他得**先把整本书读完**才知道有哪些章——那等于一页没省。
2. **他记不住上次的判断。** 这一轮他觉得"番茄那段用不上"，下一轮同样的情境他又可能觉得"用得上"，**行为会飘**，同一件事这次对、下次错。
3. 现在每次对话都是**独立的一次考试**（无状态），不是"同一个熟手越聊越懂你"。指望他记得"我上次省了哪段"，本身就建立在不成立的假设上。

所以"交不交、交多少"，**只能由知道答案的一方来决定——游戏本身。**

### 游戏能保证一半：不多交（这是可以确定的）

游戏本来就知道：**今天是第几天、好感多少、你拿到过哪些线索、你在哪个区域。** 拿这四把尺子一量人物卡里"第 N 天起且好感 60+""好感 80+ 才可流露"那些段落，**该给哪段、不给哪段，是一翻规则就确定的事**——不会飘，可以保证"用不到的不交"。

### 难点在另一半：不少交（只能逼近，不能保证）

危险在于：玩家聊到一句**被你省掉的段落**里才有的内容时，AI 没有依据，就会**瞎编**。比如你把"番茄过敏"那段按好感门控省了，结果玩家正好问到番茄——AI 不知道他该"皱眉后退、避而不谈"，可能就随口编个故事出来。

缓解的办法是"防守"，不是"消灭"：
1. **人物最核心的"身份 + 说话腔调 + 高压线（❌绝不说什么）"永远带着，任何情况不能省**——这是人格的地基；
2. **门控别太严，宁可多给一档、不要少给一档**——省过头省出穿帮，得不偿失；
3. **真机反复实测**：发现哪次答错了，就把缺的那段补进"常带核心"或放宽那条门控；
4. 心里有数：**实测下来，人物卡那 ~6,300 字里，门控能砍的只有约 13%**（第一天）。因为大头是"语气例句、说话腔调、话题池"这类**每轮都得有的人格血肉**，不是按天解锁的秘密。**真正的大头在"跨界误送"——把天的 2,100 字规则书整本塞给了 NPC，那才是该砍的第一刀。**

### 小结这一节

> **"不多交"能保证（游戏按规则门控）；"不少交"只能逼近（保留人格核心 + 放宽门控 + 实测补漏）。**
> 把省空间的重心放在"**别把天的书借给 NPC**"，而不是在"人物卡内部抠细节"——前者一刀省 2,000 字还修剧透，后者抠半天只有 13%。

---

## 六、发散：再往远想几步

- **写作量也能降**：让 AI 用短句、少铺垫；回复模板化（比如先给一句动作描写再给一句台词），能压住"写字"时间。
- **换脑子**：日常寒暄用"写得快的模型"，只有塔罗、审判这种要文学性的才用最聪明的模型。质量需要先试，但这是除流式外**唯一能直接加速"写字"**的路。
- **把等待藏进玩法**：AI 回答的空当正好让角色做点动作、放段环境音、或先走一段必然的过场——玩家以为在读剧情，其实在等网络。
- **失败要体面**：超时了给一句本地兜底台词 + 悄悄重试（已有雏形），别让玩家对着"……"发呆。
- **一次只动一个变量**：流式和内容压缩会互相干扰判断（流式下 JSON 更容易截断）。建议先单独上流式、测稳，再回头做"内容分层"，别一次全上、出了问题不知道怪谁。

---

## 七、附：上一轮说好要提交的改动，去哪了？

（记录一下，免得下个会话找不到。）

- 你要我提交的 ① 提速改动（`ai_bridge.gd`：cancel 死锁轮询修复、max_tokens 透传、网络遥测、key 清洗；`ai_dialogue_session.gd`：单轮 512 上限、5 轮记忆窗）**已经全部入库**——由并行运行的另一个 Claude 会话打包进提交 **`3e58dba`**（2026-09-05 18:02）随地形 autotile 一批提交。`git diff HEAD` 为空，关键行核对无误，改动完整无缺。
- 顺带纠正一个认知：**`d:\AIGameBuild` 是单一 git 仓库**，游戏目录 `成为艾莉森的一天/` 是它的子目录，不是嵌套独立仓库。设计文档分支 `docs/design` 的实体在工作区 `d:\AIGameBuild-DesignDocs\`。
- 设计文档按仓库约定走 `docs/design` 分支（即本文件所在目录）。
