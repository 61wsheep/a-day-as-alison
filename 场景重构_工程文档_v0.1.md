# 场景重构 · 工程文档 v0.1

> **状态：待审核。** 审核通过后，本文档作为「三张外景重构 + 原创瓦片集 + 放大地图」这一工作流的**唯一施工参考**。此后所有相关改动必须先对照本文档：护栏（§0.3）不得违反，契约束（§2）不得静默改名，决策待定项（§8）未拍板前不展开对应施工。
>
> **仓库/分支：** 代码 `D:\AIGameBuild`（main，Godot 工程在 `成为艾莉森的一天/`）；文档 `D:\AIGameBuild-DesignDocs`（docs/design）。本文档内所有路径均相对 Godot 工程根目录。
>
> **引擎约束：** Godot 4.6 · 2D · GL Compatibility。所有游戏代码改动落在 main 分支、`feat/` 功能分支合回；本文档本身落在 docs/design。

---

## 0. 总览

### 0.1 任务一句话

把现行的「单张 main.tscn 内烘焙三个外景区」重构为「**三张独立外景 .tscn，由 main 实例化**」，每张图用**原创可平铺瓦片集**替换占位视觉，并把地图画得更大。

重构只动「壳」，不动机芯：玩法、AI 对话/记忆、时间/白天色调、背包/任务/天气、结局路由等行为逻辑一律不动。

### 0.2 目标 / 非目标

| 目标 | 非目标（本轮不做，避免主线漂移） |
|---|---|
| 三个外景区各自独立成 .tscn，main 只实例化 + 保留行为层 | 不改玩法逻辑 / 不加新玩法（玩法走《玩法扩展》文档） |
| 地面与装饰换用原创可平铺瓦片集，退出 cainos 占位 | 不接 AI 记忆 / 对话 JSON 的任何改动 |
| 每张外景按新尺寸放大 | 不做室内场景（树屋内部/艾莉森房间另有文档） |
| 建立「契约束」保证换壳后行为零回归 | 不动 Player / UI CanvasLayer 的根结构 |
| 把 y_sort 遮挡与相机极限按区补全 | 不动 EventBus 信号契约 |

### 0.3 主线护栏（硬性不变量）

> 以下是「防止跑偏 / 减少遗忘」的核心清单。任何一步改动前先过一遍；违反任一条必须回写本文档再动。

1. **区名 ↔ area_id 映射冻结**：节点显示名 `Plaza / TreehouseDistrict / StoneNestTower` 保持不变；其 `to_snake_case()` 即 `plaza / treehouse_district / stone_nest_tower`。门 `target_area`、`GameManager.current_area`、AI 上下文里出现的区名都依赖这套映射。改名 = 全线炸。
2. **每张外景 .tscn 的根节点名 = 区名**；main 的 `Areas` 下用 `PackedScene` 实例化，且实例节点名仍是区名。
3. **契约束子节点名冻结**（详见 §2）：`Ground / Props` 等 game.gd 用字符串路径读取的名字，缺一不可。**允许**在外景 scene 内部增删内容节点；**禁止**改/删契约束节点名。
4. **门只搬配置、不改脚本**：场景切换门 = `Area2D` + `door.gd`（`@export target_area/target_spawn`），只发 `door_entered`；租赁门 = `Area2D` + `treehouse_door.gd`（`@export house_id`），只发 `game_action rental:*`。门脚本里没有目标坐标，迁移安全。
5. **NPC 归属冻结**：Soraya → Plaza；Padwin / CactusBishop → TreehouseDistrict；TianAltar → StoneNestTower。NPC 随外景迁移，`npc_id` 与相对坐标原样保留。
6. **Player 与 UI 留在 main 根**：Dialogue / Tarot / Rental / Ending / Status / HUD / Inventory / Sell / Keybind / QuestBoard / TarotDivination / History 等 CanvasLayer 不进任何外景 scene。
7. **EventBus 信号零改动**；采集池节点名 `Mushrooms / Berries` 与 `collectible` 分组不变。
8. **烘焙工具不覆盖手摆内容**：迁移完成后，外景内容的日常编辑发生在各外景 .tscn 内；`bake_terrain` 类工具只作一次性迁移助手（§3.3）。

### 0.4 相关文档

| 文档 | 关系 |
|---|---|
| `玩法扩展_设计与工程实现_v0.1.md` | 新玩法（背包/订单/天气/建房/草药瓶），**依赖本重构铺好的地面与采集点**；其素材占位方案与本文档 §6 对齐 |
| `美术音乐工作文档.md` / `美工音乐资产清单.md` | 素材来源与规格约定；本重构所需新素材缺口见 §6.3 |
| `艾莉森的房间_设计与工程实现_v0.1.md` | 室内场景另立，本轮不冲突（树屋门是外景到室内的跳板，见 §1.1） |
| 本仓库 `CLAUDE.md` | 分支 / 提交规范，硬性遵守 |

---

## 1. 现状盘点（写代码前已核实的仓库事实）

### 1.1 场景拓扑（main.tscn 提取）

三外景都在 `Areas` 下，是**三个平铺 Node2D**，不是实例化子场景：

```
Areas/
├─ Plaza            (Node2D)
│   ├─ Ground       (TileMapLayer，已烘焙地形 cell)
│   ├─ Props        (Node2D，烘焙成的几十个 Sprite2D + StaticBody2D 碰撞)
│   ├─ PlayerSpawn / SpawnFromEast / SpawnFromWest   (Marker2D)
│   ├─ DoorEast / DoorWest   (Area2D + door.gd)
│   ├─ Mushrooms×3 / Berries×4   (Area2D 采集池)
│   ├─ NoticeBoard  (Area2D + notice_board.gd，告示牌，开订单板)
│   └─ Soraya       (实例 npc_soraya.tscn)
├─ TreehouseDistrict
│   ├─ Ground / Props / SpawnFromPlaza
│   ├─ DoorWest (door.gd) + DoorOak / DoorCedar / DoorWillow (treehouse_door.gd)
│   └─ Padwin (npc_padwin.tscn) / CactusBishop (npc_cactus_bishop.tscn)
└─ StoneNestTower
    ├─ Ground / Props / SpawnFromPlaza
    ├─ DoorSouth (door.gd)
    └─ TianAltar (npc_tian.tscn)
```

**门拓扑（实测导出值，迁移时原样照搬）：**

| 门节点 | 所在区 | 脚本 | 目标区 (`target_area`) | 出生点 (`target_spawn`) / `house_id` |
|---|---|---|---|---|
| DoorEast | Plaza | door.gd | `treehouse_district` | `SpawnFromPlaza` |
| DoorWest | Plaza | door.gd | `stone_nest_tower` | `SpawnFromPlaza` |
| DoorWest | TreehouseDistrict | door.gd | `plaza` | `SpawnFromEast` |
| DoorSouth | StoneNestTower | door.gd | `plaza` | `SpawnFromWest` |
| DoorOak / DoorCedar / DoorWillow | TreehouseDistrict | treehouse_door.gd | —（发 `game_action`） | `house_id = oak / cedar / willow` |

main 根还挂着：Player、DaylightModulate(CanvasModulate)、各 UI CanvasLayer。NPC 名字与 `Areas` 区名不冲突（NPC 挂在区内）。

### 1.2 地形与道具来源

- **程序生成 + 烘焙**：`scripts/systems/scene_layout.gd` 以 `40×24` 瓦（每瓦 32px → 每区 **1280×768**）铺三种地面（石子路/土路/石板），再用 `_make_prop` 从图集切 Sprite2D。`scripts/tools/bake_terrain.gd`（+ `scenes/tools/bake_terrain.tscn`）把生成结果固化进 main.tscn。
- 运行时 `game.gd._ready` 走 `paint_area_grounds / build_all_props`，**判空兜底**（烘焙过的区跳过）。
- 烘焙工具注释明言：**重复运行 = 重新随机布局，会覆盖手动摆放**。

### 1.3 与「放大」直接相关的现状

- **相机极限三区共用同一 rect**：`game.gd._ready` 里 `_world_rect = Rect2(0,0,1280,768)`，一次性调 `player.setup_camera_limits(world_rect)`；玩家移动时按这个 rect clamp。三区必须同尺寸才能这么写。
- 蘑菇/浆果随机刷新按 `_world_rect` 采样（`game.gd._random_grass_pos`，用 `is_grass_tile` 判定草坪）。
- **当前无任何 y_sort / z_index 使用**：Props 是平铺 Sprite2D，靠子节点顺序遮挡。
- NPC、门、道具坐标 = 烘焙后手摆/硬编码在 tscn 与 scene_layout 常量里（如树屋三栋 x≈300/640/960、石柱 x≈300/980）。

### 1.4 素材现状

| 素材 | 尺寸/形态 | 状态 | 备注 |
|---|---|---|---|
| `forest_plaza.png` | 384×1040 原创瓦片集 | ✅ 已接入 | 现行地面的图集，含道具区（SceneLayout 已切） |
| `cainos/*` | 免费占位包 | ⚠️ 遗留 | 门/石柱/拱门/岩石/蘑菇图标等仍有引用，逐一替换退场 |
| `tile_plaza1.png` | 2560×1600 = 80×50 瓦(32px) | 📦 待接 | 已有编辑器的 `plaza_tiles1.tres`（约 4010 行 TileSet 定义），疑似广场主力 autotile 集 |
| `tile_plaza.png` | 128×816（4×~25 瓦竖条） | 📦 待接 | 已有 `plaza_tiles.tres`；信息量小，用途待定（§8 D1） |
| `tile_forest/*` | 地板/木墙/木路/林路等 | 📦 待接 | 疑似树屋区/林地区地面素材 |
| `tile_collectile items.png` | 96×160 采集品图标 | 📦 待接 | 可作物品图标源（背包/售卖 UI 用） |
| `*_tileset.png` ×4 | 576×384 角色帧 | ✅ 已用 | 玩家与 NPC 角色，与地图重构无关 |

### 1.5 代码引用面（会被本次重构影响的清单）

生产代码（非测试）引用三区结构/区名处，迁移后**若契约束成立则无需改动**，但必须回归：

| 文件 | 引用方式 | 依赖的契约束 |
|---|---|---|
| `scripts/game.gd` | `SceneLayout.paint/build(areas)`、`switch_area` 用 `area.name` 匹配、`areas.get_node_or_null("Plaza/Mushrooms"/"Plaza/Berries")`、`_world_rect` 采样、`_refresh_collectibles` | §2 Ground/Props/池/门/Marker 名 |
| `scripts/systems/scene_layout.gd` | 按 `Plaza/TreehouseDistrict/StoneNestTower` 字符串生成地形与 Props | 会被逐步替换/裁撤（§3.3） |
| `scripts/autoload/game_manager.gd` | `current_area` 默认 `"plaza"` | 区名映射（§0.3-1） |
| `scripts/systems/ai_dialogue_session.gd` | 读 `gm.current_area` 注入 AI 上下文 | 区名字符串不变 |
| `scripts/autotest.gd` | 期望 `current_area == treehouse_district / stone_nest_tower` | 区名映射 |
| 测试目录 9 个文件 | 引用区名 / 门 / 采集池 / 坐标 | 见 §7 |

---

## 2. 契约束：每张外景 .tscn 必须满足的接口

> **为什么要有这张表：** game.gd 等行为层靠**字符串路径**和**节点名匹配**工作。只要每个外景 scene 的根命名 + 契约束子节点名保持，拆成实例后行为层一行不用改。这张表就是「壳的 ABI」。

### 2.1 通用骨架（三张外景都带）

| 节点路径（相对外景根） | 类型 | 脚本/说明 | 谁读它 |
|---|---|---|---|
| `(根)` | Node2D，**名 = 区名** | — | game.gd `switch_area` |
| `Ground` | TileMapLayer | `tile_set` 指向该图自己的 .tres；供草坪判定/刷新 | game.gd `is_grass_tile`、SceneLayout（迁移期） |
| `Props` | Node2D | 手摆装饰 + StaticBody2D 碰撞子节点 | 烘焙（迁移期） |
| `*Spawn*` | Marker2D | 区各自出生锚 | game.gd `switch_area` 按名查找 |
| `DoorXxx`（场景门） | Area2D + door.gd | `target_area / target_spawn` 按 §1.1 表 | door 发 EventBus |
| `DoorOak…`（租赁门） | Area2D + treehouse_door.gd | `house_id` | treehouse_door 发 EventBus |
| NPC 实例 | PackedScene | 名与 `npc_id` 原样 | AI/对话层 |

### 2.2 分区分项

| 区 | 特有契约束节点 |
|---|---|
| **Plaza** | `PlayerSpawn`、`SpawnFromEast`、`SpawnFromWest`、`Mushrooms`（含 `collectible` 组 Area2D 子项）、`Berries`、`NoticeBoard`（Area2D + notice_board.gd + 视觉子节点）、实例 `Soraya` |
| **TreehouseDistrict** | `SpawnFromPlaza`、`DoorWest`(door.gd)、`DoorOak / DoorCedar / DoorWillow`(treehouse_door.gd)、实例 `Padwin`、`CactusBishop` |
| **StoneNestTower** | `SpawnFromPlaza`、`DoorSouth`(door.gd)、实例 `TianAltar` |

### 2.3 契约维护流程

- 允许：在外景 scene 内部自由增删**内容**节点（花/树/长椅/碰撞/新采集点等）。
- 禁止：改名/删除契约束节点；否则必须同步改 `game.gd`（含 `_refresh_collectibles`、采样逻辑）并**回写本文档 §2**。
- 新增采集点沿用 `collectible.gd` + item_id，组仍为 `collectible`，放对应 `Mushrooms`/`Berries` 或新区池（新池要在 game.gd `_refresh_collectibles` 的池名列表注册）。

---

## 3. 目标架构

### 3.1 拆分后结构

```
scenes/
├─ main.tscn                          # 行为层：Player、Daylight、全部 UI CanvasLayer
│   └─ Areas/
│       ├─ Plaza            = instance scenes/levels/plaza.tscn            (节点名仍 Plaza)
│       ├─ TreehouseDistrict= instance scenes/levels/treehouse_district.tscn
│       └─ StoneNestTower   = instance scenes/levels/stone_nest_tower.tscn
└─ levels/
    ├─ plaza.tscn                     # 现在躺着一个旧空壳（forest_draft 背景草稿），会被整份替换
    ├─ treehouse_district.tscn        # 新建
    └─ stone_nest_tower.tscn          # 新建
```

每张 `levels/*.tscn` 内部：自己的 `Ground`（tile_set 指自己的 .tres）+ 手摆 `Props` + 契约束 Marker/门/采集池/NPC。**外景内容日常编辑只发生在这三个文件里**。

### 3.2 main 保留 vs 剥离

| 留在 main | 迁进 levels/*.tscn |
|---|---|
| `Areas` 骨架 + 三个实例 | 各区 Ground / Props / 采集池 |
| Player、DaylightModulate | 各区 Door / Spawn Marker |
| 全部 UI CanvasLayer | 各区 NoticeBoard / NPC 实例 |
| 区切换逻辑、时段色调、午夜流程、结算 | — |

### 3.3 开发工作流变化（烘焙工具的角色转变）

- **迁移期**：可用现有 `scene_layout.gd` + 临时 bake 脚本把某区生成物一次性导出，作为新外景 scene 的**起点**，再在编辑器手调。
- **目标态**：日常不再跑「整张 main.tscn 烘焙」，因为那会覆盖手摆。SceneLayout 的运行时兜底最终移除（§8 D3）；可保留一个**按区作用**的开发期 bake 辅助脚本（不覆盖手摆、只铺空白区）。

---

## 4. 迁移实施步骤（Pilot 先行）

> 节奏对齐项目习惯：**每步完成演示确认再进下一步**。全程在 feat 分支上做，合回 main。

### Step 0 — 定稿契约 + 基线
- [ ] 本文档 §2 契约束审核通过。
- [ ] 跑回归基线：`autotest`（含跨区行走）见 §7 清单，记录当前绿 / 红状态，作为后续对比基准。
- 验收：基线全绿，§8 决策项 D1–D3 已拍板（否则跳过放大部分先做零回归拆分）。

### Step 1 — Pilot：Plaza 拆成独立 scene（先不放大、先不动瓦片）
1. 复制当前 `Areas/Plaza` 全部子内容（含 DoorEast/West 的 §1.1 导出值、采集池、NoticeBoard、Soraya 实例及其坐标）到一个根名为 `Plaza` 的新场景，存 `scenes/levels/plaza.tscn`（**整份替换旧空壳**）。
2. main.tscn：`Areas/Plaza` 改为 `instance = plaza.tscn`，实例节点名保持 `Plaza`。
3. 回归验收：
   - [ ] `autotest` 绿；
   - [ ] 门往返 Plaza↔TreehouseDistrict、Plaza↔StoneNestTower 正常（含 spawn 正确）；
   - [ ] 蘑菇随机刷、浆果当日刷新正常；告示牌开订单板；与 Soraya 对话正常；
   - [ ] `_world_rect`/相机未改（仍 1280×768）也成立——证明「换壳零回归」。
- 若此步不能全绿 → **问题出在契约束**，回查 §2，不进入下一步。

### Step 2 — 放大 + 换原创瓦片（仍在 Plaza）
1. Ground 的 `tile_set` 换成 §6 选定的 plaza 新 TileSet（推荐先试 `plaza_tiles1.tres`），按目标尺寸重铺（尺寸见 §8 D2）。
2. 相机/刷新联动改造（§5.2）：`switch_area` 里按当前区 rect 调 `setup_camera_limits`，刷新采样改当前区 Ground。
3. Props 手摆对齐新布局；确认 y_sort（§5.4）。
4. 验收：新瓦片下三种时段色调、雨天占位正常；放大的区域里门/出生点/刷新范围不越界；NPC 能到达交互点；走完全地图无穿模/无出界。
5. 沉淀：把 Plaza 的做法写进 §2/§5 的**模板说明**，供后两张图照抄。

### Step 3 — 复制到 TreehouseDistrict、StoneNestTower
- 各自：独立 scene → 换各自 TileSet → 放大 → 手摆 → 回归（门、租赁树屋、塔楼问答、NPC 对话、跨区 AI current_area）。
- TreehouseDistrict 用 `tile_forest/*`（§8 D5）；StoneNestTower 石板方案待定。

### Step 4 — 收尾与清理
- [ ] 移除运行时 SceneLayout 生成/烘焙兜底（§8 D3 生效后）。
- [ ] 清理 cainos 遗留引用（含蘑菇图标等，§6.4）。
- [ ] 全量回归 + 本文档 §7 手测清单过一遍。
- [ ] 更新相关资产清单文档、本文档状态为「已实施」。

---

## 5. 放大设计（放大的真正成本点，不是铺地）

1. **每区独立世界 rect**：契约束新增「外景 scene 暴露自身尺寸」——实现上各 level 脚本提供 `map_rect()`（由 Ground used_cells 或显式常量），main 的 `switch_area` 切换时据此更新玩家相机 limits 与刷新采样边界。
2. **相机**：`player.setup_camera_limits` 现为纯属性赋值、可多次调用，故只需在 `switch_area` 每次调一次（当前只在 `_ready` 调）。出生点传送后立即收敛到新区。
3. **随机刷新**：`game.gd` 蘑菇采样与 `_refresh_collectibles` 从「共享 `_world_rect`」改为「当前区 Ground + 该区 rect」，草坪判定 `is_grass_tile` 已支持任意层，无需改 SceneLayout。
4. **y_sort 遮挡**：每张外景的根 Node2D 开 `y_sort_enabled = true`，角色与较高装饰（树/路灯/告示牌/塔楼柱）纳入排序；`Props` 内 Sprite2D 用 `y_sort_origin` 或子分组表达高度，避免大地图下绕树穿帮。
5. **目标尺寸**：以新素材瓦数定（如 `tile_plaza1` 80×50 瓦 = 2560×1600，约为现区两倍）。具体由 §8 D2 拍板；尺寸变化必须在 §2 契约束注明 map_rect 契约。

---

## 6. 素材映射与瓦片集落地

### 6.1 瓦片集 → 地图映射（含待定）

| 外景 | 主地面 TileSet 候选 | 依据 | 状态 |
|---|---|---|---|
| Plaza（森林广场） | `plaza_tiles1.tres`（源 `tile_plaza1.png`，80×50 瓦） | 编辑器已建成大 TileSet，尺寸恰可撑起放大后的广场 | 待拍板 §8 D1 |
| TreehouseDistrict（树屋区） | `tile_forest/*`（木地板/木墙/木路） | 新素材，语义贴合 | 待拍板 §8 D5 |
| StoneNestTower（石巢塔楼） | 石板类素材（候选：`tile_plaza1` 内石材块 / 需补） | 现行是纯石板铺法 | 待拍板 §8 D5 |
| 全地图过渡装饰 | `forest_plaza.png` 的树/灌木/花可继续复用，或按需切 | 已是原创 | — |

### 6.2 装饰 Props / 占位清单

- 树/灌木/花/长椅/路灯/告示牌/树桩/原木：`forest_plaza.png` 道具区已在 SceneLayout 切好（N_TREES/N_BUSHES…），新外景可继续以 **AtlasTexture region 常量**或编辑器 Sprite 复用。
- 浆果丛：可灌木 modulate 红色（《玩法扩展》占位方案），或从新素材切正式贴图。

### 6.3 素材缺口（需外协/补画，另走美工清单）

| 用途 | 缺什么 |
|---|---|
| StoneNestTower 地面 | 石板/石砖 autotile（若无现成素材） |
| 新采集点（树桩 log/岩石 stone/草药×3） | 若 `tile_collectile items.png` 不含，需补图标 |
| 塔楼石柱/拱门等 | 是否继续用 cainos struct，还是补原创 |
> 参考 `美术音乐工作文档.md` 的优先级与占位规则；占位不阻塞开发。

### 6.4 cainos 遗留逐一退场清单

- 蘑菇图标（SceneLayout `MUSHROOM_ICON_REGION` ← cainos plant.png）：改从原创集或 `tile_collectile items.png` 切。
- R_DOOR/R_PILLAR/R_ARCH/R_ROCK_*/R_BARREL/R_VASE（cainos props/struct）：逐张替换为原创，替换一张删一张引用。

---

## 7. 回归与测试

### 7.1 改动前先 grep 的引用面（防漏）

```
关键字（Godot 工程根，--include=*.gd / *.tscn）
  TreehouseDistrict / StoneNestTower / "Plaza" / current_area
  Areas/  / PlayerSpawn / SpawnFrom / DoorEast / DoorWest / DoorSouth / DoorOak|Cedar|Willow
  Mushrooms / Berries / NoticeBoard / _world_rect / setup_camera_limits
```

### 7.2 必须全绿的测试

| 测试/工具 | 验证点 | 拆分后是否需改 |
|---|---|---|
| `scenes/autotest.tscn`（autotest.gd） | 跨区行走、current_area 期望 | 需核对（§0.3-1 区名不变则只改坐标相关） |
| `test_forage_spawn.gd` | 蘑菇/浆果草坪刷新、当日重置 | 刷新改「当前区」后需跟随 |
| `test_forage_sell.gd` / `test_sell_dialogue.gd` | 采集→售卖链路 | 采集池/坐标相关需跟随 |
| `test_interaction.gd` | 交互（告示牌/租赁门等） | 依赖契约束节点 |
| `test_tarot_quest.gd` | 占卜支线 | 依赖 NPC 位置 |
| `test_history*.gd` / `test_heaven_session.gd` | AI 会话历史 | 依赖 current_area 文本 |
| `sim_multi_day.gd` | 多日循环 | 依赖区/刷新 |

### 7.3 手测清单（真机 / 编辑器）

- [ ] 三区两两门往返，spawn 点正确、无连锁触发（door 冷却）
- [ ] 蘑菇随机刷、浆果当天刷新、午夜重置后恢复
- [ ] 告示牌开订单板；树屋三扇租赁门按 E 开租；塔楼午夜问答
- [ ] 三 NPC 对话 + AI 上下文（跨区时 current_area 正确）
- [ ] 四时段色调过渡、午夜面板两阶段
- [ ] 放大后地图四边不可出界、采集不刷上路、玩家无穿模

### 7.4 测试兜底点（重构后需改为相对坐标）

- `game.gd._random_grass_pos` 兜底点 `(891,500)`、`_mushroom_pool` 路径——放大后按新区采样代替。
- 现有测试内出现的广场魔法坐标（如刷新测试断言点）改为相对 spawn/区原点的写法。

---

## 8. 决策待定项（需 owner 审核拍板）

> 未拍板前不展开对应施工。每项给了推荐与影响，供审核参考。

| # | 决策 | 选项 | 推荐 | 影响 |
|---|---|---|---|---|
| D1 | Plaza 主地面用哪个瓦片集 | A. `plaza_tiles1.tres`(tile_plaza1 大集) / B. `plaza_tiles.tres`(tile_plaza) / C. 新切文件 | **A**（已是成品大 TileSet，尺寸=放大目标） | 决定 Plaza 地面像素与风格 |
| D2 | 放大目标尺寸 | A. 沿用 1280×768 只换皮 / B. 2560×1600（80×50 瓦）/ C. 其他 | **B**（≈现区两倍，贴合 tile_plaza1） | 决定相机/刷新改造幅度 |
| D3 | 运行时程序铺地/烘焙去留 | A. 迁移期保留、最终移除 / B. 长期双轨 | **A** | 决定 scene_layout 生命周期 |
| D4 | Props 落地方式 | A. 编辑器手摆为主 / B. 保留程序常量布局 | **A**（手摆，B 只做一次性迁移） | 决定放大的美观可控度 |
| D5 | TreehouseDistrict / StoneNestTower 瓦片源 | 用 tile_forest / 待补石板 | 树屋用 tile_forest；塔楼需确认素材 | 决定两张图素材缺口 |
| D6 | NPC/门坐标 | A. 保持原值 / B. 随放大重排 | **B**（跟随放大重新落位） | 影响 §4 Step 2/3 |
| D7 | 是否建关卡模板 .tscn | A. 复制骨架再改 / B. 模板复用 | 先 A，三张齐了若重复度高再抽 B | 影响工程整洁度 |

---

## 9. 里程碑与完成定义

| 里程碑 | 内容 | 完成定义（演示确认后算过） |
|---|---|---|
| M0 | 契约束 + 基线 | §8 待定项就位、基线测试绿 |
| M1 | Pilot Plaza 独立 scene | 换壳零回归（§4 Step1 验收清单全过） |
| M2 | Plaza 放大 + 原创瓦片 | §4 Step2 验收过、模板沉淀 |
| M3 | TreehouseDistrict / StoneNestTower | §4 Step3 验收过 |
| M4 | 清理收尾 | cainos 退场、烘焙兜底移除、§7 全量回归绿、文档回写 |

---

## 10. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1 | 2026-09-03 | 首版，待审核。基于 `main.tscn`（322 节点）、`scene_layout.gd`、门导出值实测盘点。 |
